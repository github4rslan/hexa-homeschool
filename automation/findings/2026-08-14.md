# Scout findings — 2026-08-14

DECISION: all

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           → build EVERY item below (default)
    B1, F2, F5    → build ONLY these IDs
    skip          → build nothing tonight
  Leave it as `all` and do nothing to run fully autonomous.
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright (MCP) + standalone Playwright script for admin/tutor auth  ·  Base URL: https://edway.uk
Day focus (UTC Friday): B-polish rubric. Standing priorities also run: full child lesson pass/fail + wrong-answer/interaction delight.

Coverage this run: marketing `/` (desktop iw 1280 + mobile iw 390, whole page, 0 console errors),
`/signup` + `/login` (inspect-only, labels + autocomplete correct), parent dashboard (SMOKE),
full child lesson end-to-end as Ivy (warm-up + English `eng_ks3_grammar`), admin + tutor live
auth re-verified read-only (owner priority this run), curriculum correctness re-audit + 3 authored
questions. `/api/health` 200 `{db:up}`. type-check + lint GREEN, `npm audit` 0 vulnerabilities.

Live child pass/fail (all PASS):
- Warm-up (spaced-rep review, 2 questions): correct = star burst + "Spot on!"; wrong (rounding
  486→400) = calm amber panel (hue 95, NOT red) + supportive re-explain. PASS.
- English lesson: explainer worked-example + glossary chips ("action word", "verb") + narration;
  practice wrong#1 calm violet settle (oklab b = -0.20, NOT red) + "Ivy, not quite yet" +
  "Why isn't that right?" + "2 tries left"; wrong#2 See-it unlocks + hints escalate to 2 rungs +
  "1 try left"; See-it = Eddie step walkthrough highlighting clue words; correct = star burst +
  green settle (a = -0.19); mastery 3/3 → certified (trophy + confetti); certificate renders
  ("Ivy · Grammar & Clauses · Awarded 14 August 2026 · hash"). Keyboard: radio reachable, Space
  selects (aria-checked true). PASS across the whole loop. 0 console errors.

---

## Bugs (Critical → Low)

- [x] **B1 · Low · a11y · child `/learn` layout is missing the global reduced-motion provider** — `src/app/(child)/learn/layout.tsx`
  - Repro / evidence: `ReducedMotionProvider` (the `<MotionConfig reducedMotion="user">` wrapper
    added by the 2026-08-12 build) is mounted in `src/app/(marketing)/layout.tsx` and
    `src/app/(dashboard)/layout.tsx`, but NOT in the child learn layout (grep: only those two files
    reference it). The child layout renders `<main>{children}</main>` with no motion config.
  - Why it matters: child mode is by far the most animation-heavy surface (celebration bursts,
    correct settles, See-it choreography, phase-bar fills, mascot) AND it is the SEND-critical
    surface (Ivy carries a dyslexia flag; Children's Code expects reduced-motion honoured). The
    build's own rationale was that the globals.css `0.01ms` rule does NOT neutralise framer-motion
    JS transitions, so the `MotionConfig` wrapper is the safety net. Every child animation that does
    not individually call `useReducedMotion()` still plays for a reduce-motion child, and there is no
    global default to catch future components.
  - Fix: wrap the learn layout's `{children}` (or the `<main>`) in `<ReducedMotionProvider>` (the
    existing client wrapper), exactly as the dashboard/marketing layouts do. One import + one wrapper.
  - Risk: minimal. It is a client wrapper used inside a server layout already (proven pattern); it
    only sets the framer-motion default, so components that self-guard are unchanged.

## Features & upgrades (High value → Low)

### Curriculum & GCSE exam readiness (headline lane — EPIC 2: command-word items for uncovered topics)

Correctness re-audit (EPIC 1): re-derived all 17 `EXAM_STYLE_QUESTIONS` in `curriculum.seed.ts`
(reverse %, recipe proportion, standard form, speed, neutralisation, personification, solve-linear,
Pythagoras, probability, V=IR, simile/metaphor effect, nth term, hexagon angle, gradient,
acceleration, Mr of water). Every canonical answer is correct and unambiguous. No correctness bug.

The three items below are authored IN FULL (SeedQuestion shape) for Mechanic to transcribe verbatim.
Each has exactly one defensible, spec-cited answer; distractors map to real exam misconceptions.
Targets chosen because these topics currently hold only single-step recall MCQs (no command-word
practice), and Ivy is actively working `sci_cells` this week.

- [x] **F1 · curriculum · `sci_cells` "Calculate the magnification" (AQA 8464 Biology 4.1.1.5)** (size: S)
  - The win: microscopy magnification (`magnification = size of image ÷ size of real object`) is a
    guaranteed AQA calculation skill and `sci_cells` has zero calculation items today.
  - Question (transcribe verbatim):
    ```ts
    {
      topic_tag: "sci_cells",
      subject: "science",
      tier: 3,
      key_stage: 4,
      kind: "mastery",
      prompt:
        "A cell has a real width of 0.05 mm. Under a microscope its image measures 10 mm across. Calculate the magnification.",
      options: ["×200", "×20", "×0.005", "×10"],
      correct_index: 0,
      explanation:
        "Magnification = size of image ÷ size of real object = 10 mm ÷ 0.05 mm = 200. Both lengths are in the same unit, so the magnification has no units and is written ×200.",
      hints: [
        "Use the equation magnification = size of image ÷ size of real object.",
        "Divide 10 mm by 0.05 mm, keeping both lengths in the same unit.",
      ],
      misconceptions: [
        "",
        "Looks like you divided 10 by 0.5. The real width is 0.05 mm, so divide 10 by 0.05.",
        "That is the real size ÷ the image size (0.05 ÷ 10). Magnification is image ÷ real, so 10 ÷ 0.05.",
        "That is only the image size in mm. Divide the image size by the real size to get the magnification.",
      ],
    }
    ```
  - Risk: none beyond a normal seed upsert (natural key topic_tag + prompt).

- [x] **F2 · curriculum · `sci_body` "Explain why arteries have thick walls" (AQA 8464 Biology 4.2.2.2)** (size: S)
  - The win: a true "Explain" command-word item; `sci_body` is all recall today. Trains the
    artery/vein/capillary contrast that the real paper tests.
  - Question (transcribe verbatim):
    ```ts
    {
      topic_tag: "sci_body",
      subject: "science",
      tier: 3,
      key_stage: 4,
      kind: "mastery",
      prompt:
        "Explain why arteries have thick, muscular and elastic walls.",
      options: [
        "To withstand the high pressure of blood pumped from the heart",
        "To store the body's supply of oxygen",
        "To slow the blood down for gas exchange with cells",
        "To stop blood flowing backwards using valves",
      ],
      correct_index: 0,
      explanation:
        "Arteries carry blood away from the heart at high pressure, so their walls are thick, muscular and elastic to withstand and keep up that pressure without bursting.",
      hints: [
        "Think about how hard the heart pushes blood into the arteries.",
        "Arteries carry blood away from the heart, so what does that mean for the pressure inside them?",
      ],
      misconceptions: [
        "",
        "Arteries carry oxygen-rich blood but do not store oxygen; the thick walls are about coping with pressure.",
        "Blood is slowed for gas exchange in the capillaries, not the arteries.",
        "Valves that prevent backflow are found in veins, where the pressure is low.",
      ],
    }
    ```
  - Risk: none beyond a normal seed upsert.

- [x] **F3 · curriculum · `maths_quadratics` "Solve x² + 2x − 15 = 0" (Edexcel 1MA1 A18)** (size: S)
  - The win: a proper "Solve" factorising quadratic with a sign trap; existing `maths_quadratics`
    items are simpler (difference of two squares, one positive-root factorisation). This adds the
    mixed-sign case the exam favours.
  - Question (transcribe verbatim):
    ```ts
    {
      topic_tag: "maths_quadratics",
      subject: "mathematics",
      tier: 5,
      key_stage: 4,
      kind: "mastery",
      prompt: "Solve x² + 2x − 15 = 0.",
      options: [
        "x = −5 or x = 3",
        "x = 5 or x = −3",
        "x = 3 or x = 5",
        "x = −5 or x = −3",
      ],
      correct_index: 0,
      explanation:
        "Find two numbers that multiply to −15 and add to +2: those are +5 and −3. So x² + 2x − 15 = (x + 5)(x − 3) = 0, giving x = −5 or x = 3.",
      hints: [
        "Find two numbers that multiply to −15 and add to +2.",
        "Set each bracket equal to 0: x + 5 = 0 and x − 3 = 0.",
      ],
      misconceptions: [
        "",
        "Check the signs: the brackets are (x + 5)(x − 3), so the roots are −5 and 3, not 5 and −3.",
        "Each root has the opposite sign to the number in its bracket: (x + 5) gives −5, (x − 3) gives +3.",
        "The +2x means the two numbers add to +2, so one must be positive: +5 and −3.",
      ],
    }
    ```
  - Risk: none beyond a normal seed upsert.

### Delight & child engagement (owner standing priority — three distinct moments, all within the calm-wrong law)

- [x] **F4 · delight · supportive wrong-answer treatment in the warm-up (spaced-rep review)** (size: S) — `src/components/child/warmup-player.tsx`
  - The win: the main lesson gives a miss a calm, named, supported feeling (soft settle, "Ivy, not
    quite yet", See-it beckon). The warm-up does NOT: a wrong answer only swaps to a static amber
    option tint plus an impersonal "Good try, here's the idea again" panel (lines 106-158). The
    warm-up is the child's FIRST interaction of the day, so it should feel just as kind.
  - Change: on a wrong warm-up pick, add a gentle calm-law settle on the chosen option (a one-time
    `scale 1 → 0.99 → 1` breathe, never red/shake/buzzer), personalise the copy with the child's
    first name ("Good try, Ivy, here's the idea again"), and keep the amber tint. Gate all motion
    behind `useReducedMotion()` (already imported). The correct path already celebrates, so this
    only touches the wrong branch.
  - Risk: tiny; self-contained component, no scoring change (grading already fires on `choose`).

- [x] **F5 · delight · press (whileTap) feedback on the child hub quest cards** (size: S) — `src/components/child/quest-cards.tsx`
  - The win: children use Edway on touch, where `:hover` never fires. The active quest card
    (`<Link>` at line 229) has `hover:scale-[1.01]` and a `group-hover` arrow nudge but NO press
    response, so a tap feels dead until the page navigates. A subtle press-down makes the tap feel
    alive and responsive (the moment of pure interaction the owner asked to cover, not just answers).
  - Change: render the active card as a `motion(Link)` (or wrap) with `whileTap={{ scale: 0.98 }}`
    guarded by `useReducedMotion()` (already imported at line 54). Leave the resting/coming-soon/
    certified non-link cards unchanged.
  - Risk: minimal; keep the existing `child-touch` 64px target and focus ring intact.

- [x] **F6 · delight · inline mascot (Eddie) reaction on a lesson miss** (size: M) — `src/components/child/practice-player.tsx`
  - The win: on a wrong answer the child sees supportive text and the See-it beckon, but the warm
    character (Eddie) only appears once they open See-it. A small inline Eddie "let's look at this
    together" nod on the miss makes the support feel human at the exact moment of the wobble. This is
    the owner's "reacting mascot" ask, and stays strictly inside the calm-wrong law.
  - Change: add a small self-contained Eddie glyph/expression beside the calm nudge line that slides
    in gently on an incorrect check (a warm nod on a miss, a proud beat on the correct settle),
    reusing the existing Eddie asset from the See-it panel. No red, no shake, no buzzer; must be
    mute-able and fully neutralised under `prefers-reduced-motion`; no new network/CDN in `(child)`.
  - Risk: keep it non-blocking (never delays input) and inside the existing calm feedback region so
    it cannot be mistaken for a "wrong" signal.

### North-star: exam-condition fidelity (EPIC 4)

- [x] **F7 · feature · mark-weighted mock questions + honest grade boundaries surfaced to the parent** (size: L) — `src/lib/engine/mock-exam.ts`, `src/lib/engine/mock-paper.ts`
  - The win: the mock already frames calculator vs non-calculator and a Foundation/Higher window
    (`mock-paper.ts`), but every question still counts as one mark. Real papers are mark-weighted
    (a "show that" or multi-step item is worth 3-4 marks) and report an honest grade against
    published boundaries. Weighting marks + surfacing a boundary-derived grade to the PARENT (never
    a pass/fail label to the child) makes a mock genuinely rehearse exam day and gives the parent a
    truthful readiness read.
  - Change: add an optional per-question `marks` (default 1) to the mock item shape and total by
    marks; add a deterministic `gradeForMarks(subject, tier, marksPct)` table (approximate published
    1MA1 / 8700 / 8464 boundaries, clearly labelled "approximate") consumed by the existing
    `MockGradeReveal`; keep the child-facing result calm and unlabelled. Pure + unit-testable.
  - Risk: keep boundaries clearly "approximate / indicative" (they shift yearly); do not let the
    grade leak into any `(child)` copy as pass/fail. Live-drive needs a child with an unlocked mock
    (>=10 certified) — gate + unit-test first, per the mock-fidelity note.

---

## Summary
- Bugs: 1 (0 critical / 0 high / 0 med / 1 low)  ·  Features: 7
- Site health: type-check GREEN, lint GREEN, `npm audit` 0 vulnerabilities, `/api/health` 200,
  0 console errors on every surface walked, no mobile overflow (iw 390, scrollW 380). Admin AND
  tutor live re-authentication VERIFIED working (both admin credential sets reach `/admin`; tutor
  reaches `/tutor` "Assigned sessions"; session gates before login, `/logout` clears it, 0 console
  errors). The first admin attempt's `/login` stall was a measurement timing flake, disproven on
  re-test. Owner's admin/tutor auth-check ask: PASS.
- Top 3 to ship: **F1** sci_cells magnification (real AQA calc skill, zero calc items today) ·
  **F4** kind wrong-answer feeling in the warm-up (first interaction of the day should feel as
  supported as the lesson) · **B1** reduced-motion provider on the child layout (SEND / Children's
  Code safety net, one-wrapper fix).
