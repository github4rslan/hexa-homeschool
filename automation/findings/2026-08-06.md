# Scout findings — 2026-08-06

DECISION: all

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           → build EVERY item below (default)
    B1, F2, F5    → build ONLY these IDs
    skip          → build nothing tonight
  Leave it as `all` and do nothing to run fully autonomous.
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright  ·  Base URL: https://edway.uk  ·  Desktop innerWidth 1280 (verified) · Mobile innerWidth 390 (verified)

Coverage this run: marketing (`/`, `/pricing`, `/how-it-works`, `/safety`, `/for-parents`),
auth (`/login`, `/signup` inspected — no junk accounts), `/api/health` (200 `{ok,db:"up"}`),
parent (dashboard, schedule, portfolio, tutoring, settings, Ivy's child profile),
child mode as Ivy — **took the full Maths "Number & Place Value" lesson end-to-end**
(Learn → Practise 4/4 → Mastery 3/3 → certified), plus spaced-rep warm-up and the mock
gate, admin (overview/finance/escalations, read-only), tutor (read-only). Logged out of
every account + closed the browser. Static: `type-check` PASS, `lint` clean,
`npm audit` **0 vulnerabilities**.

**Health of the site is excellent** — zero console errors on every surface in both
viewports, no horizontal overflow anywhere (scrollWidth ≤ innerWidth on all pages), no
dead-ends, every recently-shipped feature re-verified live (see below). So the bug list
is deliberately short and honest; the value this run is in the feature lane.

**Re-verified LIVE and working** (do not re-propose): F5 work-evidence photo → topic
dropdown on the child profile; the 08-03 batch — English derived question figures,
SEND-aware reading default (Ivy's Easy-read font checked ON by default, dyslexia flag,
no label shown to child), parent audio note ("Hear it read to you" on the child hub,
"Proud of you, Ivy — one quest at a time."), child "today I learned" reflection surface;
web-push milestones (VAPID now set — settings shows the milestone toggle); the calm-wrong
law (misconception-specific nudge, "Why isn't that right?", no red/buzzer), star-burst
celebration, Learn→Practise→Mastery phase bar, per-topic certificate + badge shelf.

---

## Bugs (Critical → Low)

- [x] **B1 · Low · Child profile shows the same working-level info twice** — `src/app/(dashboard)/dashboard/children/[childId]/page.tsx`
  - Repro / evidence: Ivy's profile (`/dashboard/children/6a5d…116d`) renders a
    **"Current standing"** block (Mathematics — *Working at GCSE level* — 4 topics
    certified; English/Science — *Working at primary level* — 1 each) AND, further down,
    a **"Working level"** block that lists the same three subjects with the identical
    "Working at {band} level" line plus an explanatory sentence. Two sections, same data.
  - Root cause: the 2026-08-05 B1 fix changed "Current standing" from diagnostic-only
    ("Not yet assessed") to lesson-based band — which is exactly what the pre-existing
    "Working level" section already shows, so the two now overlap.
  - Fix: merge into one section — keep "Current standing" (band + certified count per
    subject) and fold the "Working level" explanatory sentence into it; delete the
    now-redundant standalone "Working level" block (or vice-versa). Presentation only.
  - Risk: none beyond layout; both read the same `bandBySubject` / `certifiedBySubject`.

## Features & upgrades (High value → Low)

- [x] **F1 · feature/ui · Derived question visuals for rounding, place-value & standard form** (size: M)
  - The win: this is Ivy's **current active Maths topic** (Number & Place Value —
    `maths_number`, on her plan every Mon/Thu). I took the whole lesson: "Round 486 to
    the nearest 100", "value of 6 in 3,652", "Write 0.0045 in standard form",
    "Round 7,482 to the nearest thousand". `deriveMathVisual` (`src/lib/child/math-visual.ts`)
    covers percent / groups / fraction / number-line(`a±b`) / array / algebra_tiles — but
    has **no** deriver for rounding, place value, or standard form, so every one of these
    questions falls through to the generic AI figure. The accepted math-visual chain is the
    proven pattern; this closes the biggest content-shaped gap for her band.
  - Change: add three tail derivers in `math-visual.ts` + renderers in
    `components/child/math-visual.tsx`, unit-tested like the existing kinds:
    (1) **rounding number-line** — parse "round N to the nearest 10/100/1000", mark N
    between the two multiples, show which it snaps to (reuse the number_line renderer);
    (2) **place-value columns** — parse "value of D in N", draw Th/H/T/O columns with D
    highlighted; (3) **standard form** — parse "write X in standard form", show the
    decimal-point shift and resulting `a × 10ⁿ`. Run each new deriver *after* the numeric
    ones so no collision (they require distinct keyword shapes).
  - Risk: low — new tail derivers only fire on their keyword; existing kinds untouched.
    Honest `alt` on each; AI fallback still serves when a prompt doesn't match.

- [x] **F2 · feature · Attach the actual work-evidence photos to the LA portfolio** (size: M)
  - The win: F5 lets a parent tag a photo of the child's written working to a mastered
    topic, and `lib/compliance/portfolio.ts` already adds a line
    `"Photo of written working — {topic}"` to the dossier's Implementation evidence — but
    it's **text only**: the dossier names the photo without a viewable link, so an LA
    reviewer can't actually see the artefact. Embedding the real image makes the strongest
    kind of home-ed evidence usable.
  - Change: thread the tagged `MediaDoc` Cloudinary secure URLs (ownership-checked, via
    repo) into `buildPortfolio` / the dossier shape (`workEvidenceTopics` → objects with
    `{ title, url }`), render them as linked/thumbnailed evidence in the portfolio view and
    the generated output. Keep the hash canonicalisation backward-compatible (append photo
    URLs to the evidence array deterministically so existing dossier hashes still verify).
  - Risk: portfolio hash/test stability — add the new field optionally and order it
    deterministically (same discipline as the F3 certs-in-portfolio change).

- [x] **F3 · ui · Replace the remaining bare "Loading…" text with skeletons** (size: S)
  - The win: after route navigation completes, the **dashboard main island** and the
    **lesson bootstrap** still flash a plain "Loading…" text for ~1–2s (observed both this
    run) — an in-page Suspense fallback, not the route `loading.tsx` skeletons that were
    already added. A generation-behind detail on an otherwise polished app.
  - Change: swap the inner Suspense fallbacks (dashboard overview data island;
    `learn/lesson` client bootstrap) for the existing skeleton components. reduced-motion
    is already globally neutralised, so no extra guard needed.
  - Risk: cosmetic only.

- [x] **F4 · feature · "Explain it another way" on the explainer** (size: M)
  - The win: the distractor-aware "Why isn't that right?" (checker-gated `/api/tutor`)
    only appears *after a wrong answer*. A child who simply doesn't get the worked example
    has no calm way to ask for a re-explanation. A single opt-in "Show me another way"
    control on the Explainer would re-teach the same canonical step in fresh words —
    genuinely new capability, fully within the existing safe pipeline.
  - Change: reuse the F2-style checker-gated `/api/tutor` call (distress gate first,
    render only when `aiVerified`, human-authored fallback otherwise) from a small control
    in `components/child/*` Explainer/StepReveal; feed it the worked-example scenario as
    context. No new route, no new curriculum authored (the Checker still gates tone/accuracy).
  - Risk: child-safety pipeline — must keep distress-gate-first + Checker ≥95% + human
    fallback; never stream raw model output. No AI-authored answers.

- [x] **F5 · feature · Readiness trajectory that isn't empty for lesson-only families** (size: M)
  - The win: the child profile's **Readiness trajectory** says "Run a mock exam to see the
    trajectory. We'll chart progress here once there are at least two assessments." — but
    mocks are locked until **10/10 topics certified** per subject (honest, but months away
    for a young learner). So a family doing daily lessons faithfully sees an empty
    trajectory for a long time. Charting **certification milestones over time** (dates Ivy
    certified each topic — already in `competence_matrix` / `parent_events`) gives an
    honest upward progression immediately, complementing (not replacing) mock-based
    prediction once unlocked.
  - Change: extend the trajectory data source to include a deterministic
    "topics-certified-over-time" series when `< 2` evaluations exist; label it clearly as
    progress (not a predicted grade). Pure, no AI.
  - Risk: keep the assessment-integrity story intact — this is *progress evidence*, never a
    predicted-grade fabrication; label accordingly.

- [ ] **F6 · feature · Child "See it" teaching animation for rounding & place value** (size: L)
  - The win: distinct from F1 (the *question figure*) — the in-lesson **"See it"** worked
    walkthrough for rounding/place-value questions currently falls to the generic
    strategy/text panel (`lib/child/teaching-animations.ts` derives animations for
    equations/grammar/science/choice, not number/place-value). A choreographed
    place-value-column build and a rounding number-line "snap" would teach the method
    visually for Ivy's active topic.
  - Change: add a `place_value` / `rounding` teaching-animation type to the
    `normalizeTeachingAnimation` fallback chain + a stage renderer, same shape as the
    accepted `fraction_bars` animation type.
  - Risk: low — it's a new type at the end of a fallback chain; the AI path is generic over
    animation type, so no exhaustive switch to break. Larger because it's a full animated
    stage.

- [ ] **F7 · feature · Surface a first Learning-insight sooner** (size: S)
  - The win: the child profile's **Learning insights** still reads "Still learning Ivy's
    rhythm" despite many completed lessons and daily check-ins — the `MIN_SAMPLES`
    thresholds in `lib/engine/insights.ts` gate every pattern, so an engaged family may
    never see an insight. One early, safe insight (e.g. certified-this-week / a
    best-time-of-day hint at a lower sample floor) makes the panel feel alive.
  - Change: add a low-threshold "getting started" insight in `buildInsights` (still
    deterministic, still pattern-not-judgement, still no child profiling), or tune the
    existing floors. Unit-tested.
  - Risk: keep it descriptive, never evaluative of the child (Children's Code).

- [ ] **F8 · stack · React patch bump + audit re-verify** (size: S)
  - The win: React is pinned at 19.0.0; 19.x has since shipped patches. Bump to the latest
    19.x and re-confirm `npm audit` stays at 0 and the build is green — routine hygiene on
    a currently-clean tree (Next 15.5.x is already current).
  - Change: bump `react` / `react-dom` to latest 19.x, `npm install`, verify type-check +
    build + audit. No app code.
  - Risk: minor React behavioural changes — gate on the full build + a live smoke.

---

## Summary
- Bugs: 1 (0 critical / 0 high / 0 med / 1 low)  ·  Features: 8
- Top 3 to ship:
  - **F1** — rounding / place-value / standard-form question visuals (closes the derived-figure gap on Ivy's active Maths topic).
  - **F2** — embed the real work-evidence photos (not just a text line) into the LA portfolio.
  - **F4** — calm "Explain it another way" on the explainer via the existing checker-gated pipeline.
