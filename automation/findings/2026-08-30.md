# Scout findings — 2026-08-30

DECISION: all

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           → build EVERY item below (default)
    B1, F2, F5    → build ONLY these IDs
    skip          → build nothing tonight
  Leave it as `all` and do nothing to run fully autonomous.
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright (MCP tools only) · Base URL: https://edway.uk

Day-of-week focus: Sunday, latest-stack deep-dive, plus the standing max-depth
child pass (all 4 interaction types + resilience) and every-day lanes (bugs,
curriculum, delight, journeys) regardless of focus. No owner priority idea was
supplied this run, so the ranking below is Scout's own judgement of value.

Desktop pass confirmed at `innerWidth` **1280** (`browser_resize` 1280×800).
Mobile pass confirmed at `innerWidth` **390**, `scrollWidth` **380** (no
horizontal overflow) on `/`, `/dashboard`, `/learn/my-stuff` and a lesson.
Whole-page scroll confirmed on marketing pages and by driving lessons through
to completion in both viewports.

**Credential-handling note:** no incidents this run. The MCP browser profile
did not carry an active session at the start (homepage showed "Sign in"), but
a session for the SMOKE parent turned out to already be live once I reached
`/dashboard`; this run inherited real progress from an **earlier same-day
attempt that died mid-task before committing anything** (Ivy's feed showed "2
topics mastered today" from that prior session, harmless, expected, and it
confirms the data-silo test-family writes are safe to inherit). For the admin
and tutor logins I used a **blind DOM-value-set + `form.requestSubmit()`**
technique exclusively: `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,
"value").set` to write the email/password into the fields via `browser_evaluate`,
then `form.requestSubmit()`, never `browser_type` (no live keystroke of a
secret) and never a `browser_snapshot`/`browser_navigate` call while a
password field held a value (I only read `.length`, never `.value`, from the
password field in my own tool output). This fully avoided the class of
incident flagged in past runs' credential-handling notes, without needing any
new tooling; see the memory entry at the end of this report.

---

## Bugs (Critical → Low)

- [ ] **B1 · Medium/High · Focus is never moved after a question is answered: the "Check answer" button unmounts and browser focus resets to `<body>`, forcing a keyboard-only child to Tab from the very top of the page through the ENTIRE nav to reach the next primary action, on every single question** — `src/components/child/practice-player.tsx:2153-2172` (the `revealed || isCorrect` ternary swaps the "Check answer" `<Button onClick={check}>` for a separate "Keep going"/"Start mastery"/"Finish" `<Button onClick={next}>`, two different elements, not one relabelled node)
  - Repro / evidence: as Ivy, keyboard-only (Tab/Arrow/Enter, no pointer) on `maths_algebra_linear` ("Solve 3x + 7 = 22"): Tab to Speak, Unmute, Listen, then radio "x = 3" (confirmed visible 4px violet focus ring via computed `box-shadow`); `ArrowDown` moved AND selected "x = 5" (correct native roving-radiogroup behaviour); Tab to "Check answer" (enabled); `Enter` produced the correct celebration. The NEXT `Tab` press landed on `document.body` (`activeElement.tagName === "BODY"`), and the Tab press after that landed on the **"Edway" logo link at the very top of the page**, confirmed via `document.activeElement` reads after each key press. Reaching "Keep going" from there requires Tab-ing through Exit, Exit lesson, Speak/Unmute/Listen, See it, four disabled (skipped) radios, then Keep going: roughly 8 extra presses a mouse/touch user never pays. This repeats on **every question, in every lesson**, compounding badly over a session with 5 to 15 questions.
  - Root cause: no focus management anywhere in `practice-player.tsx` (confirmed by grep: zero `.focus()` calls in the whole file, only two unrelated `optionRefs.current[next]?.focus()`/`inputRefs.current[target]?.focus()` calls for `fill_blank` digit-navigation). When "Check answer" is replaced by a different "Keep going" button via the ternary, React unmounts the focused DOM node; the browser's default behaviour when the focused element is removed is to reset focus to `<body>`, so the next Tab press restarts the tab sequence from the top of the document, not from where the child was.
  - Fix: give the primary post-answer CTA a stable `ref` and call `.focus()` on it in an effect keyed on `revealed || isCorrect` becoming true (a few lines, no library needed), so keyboard/switch-access focus lands directly on "Keep going"/"Start mastery"/"Finish" the moment it appears. See **F5** below for a more systemic version of this fix using `react-aria`'s focus-management primitives, which the owner has pre-approved for exactly this kind of SEND-grade custom-control problem.
  - Risk: Low, a `ref` plus one `useEffect` calling `.focus()`; verify it doesn't fight the existing narration/hint entrance animations, and that `Escape`/other keys still behave normally. Live-verify by repeating the exact keyboard repro above and confirming the very next Tab (not 8 Tabs) lands on the primary CTA.

- [ ] **B2 · Medium · The fixed "Exit lesson" pill (fixed 2026-08-29 as B2) still overlaps question content on mobile for taller questions; the earlier `pt-20`/`pt-24` padding fix does not fully hold** — `src/components/child/focus-frame.tsx:54` (`"mx-auto flex min-h-[85svh] w-full max-w-2xl flex-col justify-center pt-20 sm:pt-24"`)
  - Repro / evidence: mobile 390×844, `sci_states` (drag_drop, "Match each state of matter to how its particles behave", the figure-bearing three-slot question, the **exact same repro 2026-08-29's B2 used**). After placing the third chip via tap-to-place, the page settled at `window.scrollY = 776.67` (confirmed stable across a 1s wait, not mid-animation), and a `getBoundingClientRect()` check showed the fixed Exit-lesson pill (`top: 14, bottom: 78, left: 16, right: 156`) **measurably overlapping** the question `<h1>` (`top: -58, bottom: 148, left: 45, right: 335`); a screenshot confirms the heading text "...state of matter to how its particles behave." rendering visually behind the pill's semi-opaque backdrop. This is the identical geometric overlap class 2026-08-29's B2 fixed and live-verified, but for a taller question (a visual figure plus 3 chips plus 3 drop slots plus Check-answer button), it reproduces again.
  - Root cause: the shipped fix adds a fixed 80px top padding to the `justify-center` wrapper, which only shifts where centring starts; it does not prevent overflow. When total content height still exceeds the available centred area (as it does here: figure plus 3 slots plus button is taller than the shorter mcq-style questions the fix was verified against), `justify-center` still lets the content bleed symmetrically past both the top and bottom of its box, including back up past the reserved padding, once a later focus-driven scroll (moving focus to a newly-enabled/newly-focused control lower on the page) repositions the viewport. A fixed top-padding number cannot generalise to arbitrarily tall question content.
  - Fix: a padding-only approach isn't robust here. Two more durable options: (a) add `scroll-margin-top` matching the pill height plus gap (e.g. `scroll-mt-20 sm:scroll-mt-24`) directly on the question's heading/content container so any browser-driven scroll-into-view, not just the initial centred layout, respects the reserved space; or (b) drop `justify-center` in favour of top-alignment (`justify-start` plus the existing top padding) whenever the rendered content's measured height exceeds the container's, so tall content never centres past the safe zone in the first place. Option (a) is the smaller, more surgical change and directly targets the actual trigger (a scroll event, not the initial layout).
  - Risk: Low-Medium, re-verify both the original 2026-08-29 short-question repro (no regression) and this run's taller `sci_states` repro at 390×844 before calling it fixed; also spot-check a couple of the other figure-bearing drag_drop/tap_reveal questions (`maths_geometry`, `eng_devices`) at the same viewport.

- [ ] **B3 · Medium · "Show me another way" on the Explainer hardcodes `wasCorrect: true`, so the Teaching Agent opens with "That's absolutely correct!" even though the child hasn't answered anything yet: confusing framing on content they are seeing for the first time** — `src/components/child/explain-another-way.tsx:59` (`wasCorrect: true` hardcoded in the `/api/tutor` POST body); `src/lib/ai/teaching-agent.ts:147-149` (`generateExplanation`'s binary `framing` ternary has no "first exposure, no answer given yet" branch)
  - Repro / evidence: as Ivy, on the `eng_analysis` ("Language Analysis") Explainer, before any practice question, tapped "Show me another way". The checker-passed AI response opened: "That's absolutely correct! Analysing how writers create effect through word and structure is essential because..." There was no prior answer, right or wrong, for anything to be "correct" about; the phrase reads as though the child had just submitted something. This is a genuine, live-reproduced first-exposure-content bug, not a one-off; I re-verified the mechanism by reading the calling code, which hardcodes `wasCorrect: true` unconditionally on every "Show me another way" tap (`explain-another-way.tsx:59`), which feeds `teaching-agent.ts`'s `framing` ternary (`req.wasCorrect ? "The student answered correctly. Affirm briefly..." : "The student answered incorrectly or is stuck..."`); there is no third state for "this is a fresh re-explanation, no answer exists yet".
  - Root cause: `ReexplainContext` (built from the worked example / explainer points, `src/lib/child/reexplain.ts`) never carries an answer-attempt state because there isn't one on the Explainer, but the caller still has to pick `true` or `false` for the required `wasCorrect` field, and picked `true`, which is the wrong choice for this call site (arguably `false` would be equally wrong, implying the child got something wrong).
  - Fix: make `wasCorrect` genuinely optional end-to-end (it's already typed `wasCorrect?: boolean` in `TutorRequest`) and add a third `framing` branch in `generateExplanation` for `req.wasCorrect === undefined`, e.g. "This is the child's first time seeing this concept; introduce it fresh and clearly, with no reference to a right or wrong answer." Then stop passing `wasCorrect: true` from `explain-another-way.tsx:59`; omit the field entirely.
  - Risk: Low, a small prompt-branch addition plus removing one hardcoded field; the Checker still gates the output either way, so there's no new safety surface. Live-verify by re-tapping "Show me another way" on a fresh (never-answered) Explainer and confirming the AI response no longer opens with correctness-praise language.

- [ ] **B4 · Low/Medium · English `letter_tiles` derived visual fires on any prompt with exactly one quoted single word, even when spelling it out has no pedagogical relevance to the question: confirmed showing "stabbed" broken into individual letters for a tone/connotation-analysis question** — `src/lib/child/english-visual.ts:121-141` (the `quotedWordPattern` fallback: "requires the prompt to contain EXACTLY ONE such quoted word" is the only gate; no check on what the question is actually asking)
  - Repro / evidence: as Ivy, `eng_analysis` practice question 2: "A writer's choice of a harsh word like 'stabbed' affects the: [tone / page count / font / title]" (correct answer: tone). The rendered figure read `alt="The word "stabbed" shown as individual letter tiles."` and visually showed s-t-a-b-b-e-d as separate tiles: a spelling-shape visual for a question that is entirely about connotation/effect (a real GCSE English AO2 skill, "explain the effect of language choices"), not spelling. The tiles convey nothing relevant to answering the question. This is the sixth instance of the recurring "shallow-regex/keyword-match-on-raw-prompt-text" derived-visual class tracked in `automation/backlog.md` EPIC 1 (five prior instances already fixed across `math-visual.ts`, `teaching-animations.ts`, `english-visual.ts` twice, and `science-visual.ts`); this is a new (sixth) instance, not a repeat of an already-fixed one.
  - Root cause: `deriveEnglishVisual`'s `letter_tiles` fallback (the deriver's own doc-comment says it's for "a plain word the question is about", originally designed for prompts like "the opposite of 'begin'") only checks that the prompt contains exactly one quoted single word; it has no signal for WHY the word is quoted, so it fires identically whether the question is about spelling/word-identity (where showing the letters helps) or about the word's effect/connotation/tone (where showing the letters is irrelevant noise, at best decorative, at worst distracting).
  - Fix: narrow the gate to only fire when the prompt itself is asking about the word's identity/spelling/meaning (e.g. require a co-occurring cue phrase such as "opposite of", "means", "synonym for", "word for", "spelled/spelling" alongside the quoted word), and explicitly exclude prompts containing effect/analysis language ("affects", "effect", "suggests", "creates", "tone", "connotation", "mood"), mirroring the existing `IRREGULAR_PLURALS`-style "when in doubt, return `null` and fall back to the decorative AI image" pattern already used elsewhere in this exact file.
  - Risk: Low, a pure-function heuristic tightening in one already-unit-tested file (`english-visual.ts`); add the "stabbed"/tone-question prompt above as a new negative test case (asserting `null`) alongside a positive "opposite of 'begin'"-style case, so this doesn't regress silently again (see **F7** below for making this a standing practice across all the visual derivers).

## Features & upgrades (High value → Low)

- [ ] **F1 · curriculum (headline, north-star lane) · Deepen the four thinnest GCSE topics: one new tier-1 `maths_mensuration` question authored in full, plus the other three scoped for a future run** (size: S, ties to `automation/backlog.md` EPIC 1/2/3)
  - The win: a fresh authoritative audit this run (a temporary, non-committed script that imports the seed arrays directly, never touching the live DB, counting questions per `topic_tag`, correcting for the tuple-format arrays that plain string-grep misses, per the 2026-08-23/28/29 precedent) found the bank's four thinnest GCSE topics are `maths_mensuration` (3 questions, tiers 2-3 only), `maths_inequalities` (3 questions, tiers 3-4 only), `maths_transformations` (3 questions, tiers 2-3 only) and `maths_simultaneous` (3 questions, tiers 3-5). All three's text was re-derived and confirmed correct (no correctness bug); this is purely a coverage/tier-range gap: none of the four has an entry-level tier-1 item, and none spans the full 1-5 tier range a mature topic should. **Also confirmed a stale premise in `automation/backlog.md`'s own EPIC 1 note**: `sci_atoms` was flagged there as "thin (4 items)"; the same script shows it actually has **10** questions (4 mastery), the same tuple-format under-count class the note itself warns about. Recommend the backlog line be corrected to drop `sci_atoms` from the thin-topics watchlist (already done in today's backlog refresh).
  - New question (verbatim `SeedQuestion` shape, to be added to `curriculum.seed.ts`'s `maths_mensuration` tuple array): re-derived by hand, exactly one defensible correct option, Edexcel 1MA1 G16 (perimeter and area of rectangles), the same spec point the topic's existing tier-2 area item cites:
    ```
    {
      topic_tag: "maths_mensuration",
      subject: "mathematics",
      tier: 1,
      key_stage: 4,
      kind: "practice",
      prompt: "Work out the perimeter of a rectangle 6 cm long and 4 cm wide.",
      options: ["10 cm", "20 cm", "24 cm", "48 cm"],
      correct_index: 1,
      explanation: "Perimeter is the total distance around the shape: add all four sides. 6 + 4 + 6 + 4 = 20 cm, or use 2 × (length + width) = 2 × 10 = 20 cm.",
      hints: ["Perimeter means the distance all the way around the outside of the shape, add up all four sides.", "A rectangle has two lengths and two widths: 6 + 6 + 4 + 4, or 2 × (6 + 4)."],
      misconceptions: ["This adds the length and width once (6 + 4) but forgets a rectangle has FOUR sides, not two.", "", "This multiplies length times width (6 × 4), which finds the AREA, not the perimeter, a classic mix-up.", "This doubles the answer twice (2 × 6 × 4) instead of doubling the SUM of length and width."],
    }
    ```
  - Not authored today (scoped for a future run, per the "don't over-author in one run" pattern): `maths_inequalities` needs a tier-1/2 entry item and a tier-5 stretch item; `maths_transformations` needs a tier-4/5 item (e.g. combined translation plus reflection); `maths_simultaneous` needs a tier-1/2 "read the answer off a table of values" style entry item. Flagging the gap now (with the exact tier/count numbers above) is the valuable part; a future run can author against these specifics without re-deriving the pool sizes from scratch.
  - Risk: none at authoring stage for the one question above, unambiguous by construction, exactly one defensible correct option, distractors map to real GCSE misconceptions (perimeter/area confusion, forgetting the fourth side, double-doubling). Owner approval gate via `DECISION:` still applies before seeding.

- [ ] **F2 · delight (standing owner priority) · Give Eddie a presence during the breathing/calm-break moment, today it's the only reactive moment in the whole lesson with no mascot at all** (size: S)
  - The win: every other reaction moment in the lesson now has `EddieAvatar` (correct: `warm-nod`, reteach: `encouraging`, celebration: `celebrating`, per `automation/backlog.md` EPIC 9, "present at every scoped call site") except the calm breathing break (`BreathBreak`, `practice-player.tsx:126-168`), which today shows only a generic pulsing `Wind` icon in a circle with no mascot anywhere. This is exactly the moment a child most needs a warm, reassuring presence, triggered either by 2+ consecutive struggled questions or the immediate "attention"-category miss found live today (a wrong answer classified as a likely not-reading-carefully slip, which triggers the break on the very first miss of a question, confirmed via `shouldOfferBreak()` in `src/lib/child/eddie-copy.ts:52-64`), and it's the one place Eddie is silently absent.
  - Change: add `<EddieAvatar mood="encouraging" accent={accent} reduced={reduced} />` (or a new calm-specific mood, e.g. `"resting"`, if the owner wants a visually distinct breathing-specific pose) beside the breathing circle in `BreathBreak` (`practice-player.tsx:135-167`), matching the same warm layout pattern already used at the reteach screen (`:1525-1530`).
  - Risk: Low, presentational only, reuses the existing `EddieAvatar` component and its established reduced-motion handling; verify the breathing circle's own animation and Eddie's don't visually compete for attention on a small 390px screen.

- [ ] **F3 · accessibility / modern-upgrade · Adopt `react-aria`'s focus-management primitives for the practice-player's phase-transition CTAs (directly closes B1 systemically, not as a one-off patch)** (size: S/M)
  - The win: B1 above is a symptom of a broader pattern: `practice-player.tsx` swaps entire interactive regions (Check-answer to Keep-going, chip trays, tap_reveal cards) via conditional rendering with no consistent focus-restoration discipline, and it's exactly the kind of "SEND-grade custom control" surface the owner has already pre-approved `react-aria` for. Rather than scattering ad hoc `ref.focus()` calls at each transition point (workable for B1's narrow fix, but a whack-a-mole pattern long-term), `react-aria`'s `FocusScope` (with `restoreFocus`/`autoFocus`) gives a single, tested primitive for "when this region's content changes, move focus to the sensible next control", reusable across the Check-answer/Keep-going swap, the tap_reveal card-select flow, and any future custom control.
  - Change: add `react-aria` (or the narrower `@react-aria/focus` package) as a new dependency, wrap the practice-player's action region in a `FocusScope` with `autoFocus` on the primary CTA's mount, and migrate B1's fix onto this primitive rather than a bespoke `useEffect`.
  - Risk: Low-Medium, a new dependency (vet its bundle-size cost per the standing delight-feature rule before shipping; `@react-aria/focus` alone is small, well under the "sane bundle budget" bar) and it touches a core, heavily-tested child component, so the existing keyboard/`prefers-reduced-motion`/focus-ring tests should be re-run after integrating, not just the new focus behaviour.

- [ ] **F4 · SEND / accessibility (great feature) · Sync the reading ruler to the narration's current word position when read-aloud is playing, instead of requiring the child to manually drag their finger down the screen while listening** (size: S/M)
  - The win: `ReadingRuler` (`src/components/child/reading-ruler.tsx`) is pointer/touch-follow only; a dyslexic child (Ivy has the flag) who enables both "Read questions to me" and "Reading ruler" today has to manually track their own finger down the screen in sync with the audio, which defeats much of the point of a reading ruler (it's supposed to remove a manual tracking burden, not add a second one). The narration system already computes word-level timings and a live playback position for karaoke captions (`src/lib/child/use-narration.ts:61-94`, confirmed via code read: "Playback position in seconds, poll from rAF for karaoke highlight"); the exact data needed to drive the ruler already exists, just isn't wired to it.
  - Change: when narration is actively playing and the reading ruler is enabled and the child hasn't moved their finger/pointer in the last ~1s, compute the currently-highlighted caption word's on-screen Y position (already rendered for karaoke) and feed it into `ReadingRuler`'s `y` state instead of (or blended with) the pointer position; fall back to pure pointer-follow the instant the child touches the screen, so it never fights a deliberate re-read.
  - Risk: Low-Medium, must not regress `prefers-reduced-motion` (the ruler already correctly avoids animating opacity) and must stay `pointer-events-none`; the main design risk is choosing sensible "who wins, the pointer or the audio" arbitration so it never feels like it's fighting the child.

- [ ] **F5 · curriculum (EPIC 3 slice, scoped not authored) · Science "required practicals" recall is a genuinely unscoped GCSE content gap: name it precisely for a future authoring run** (size: S to scope, M to author)
  - The win: `automation/backlog.md` EPIC 3 already flags "Science required-practical recall (e.g. magnification, titration, rate-of-reaction methodology)... remain unscoped thin areas"; AQA Combined Science Trilogy 8464 examines required-practical methodology directly (apparatus, variables, sources of error), a command-word-heavy, exam-realistic question shape ("describe how you would...", "explain why...", "evaluate this method...") that the current bank doesn't appear to target explicitly anywhere (`sci_cells`, `sci_reactions`, `sci_forces`, `sci_energy`, `sci_electricity` etc. all test conceptual recall, not practical-method recall).
  - Change: flagging, not authoring, this run; the exact current AQA 8464 required-practical list and numbering needs a human/owner check against the live spec sheet before any question gets written (per the hard authoring rule: cite the exact spec reference for every authored question, and this run isn't confident enough in the precise current numbering to cite it without web access). A future run should pull the current spec PDF, confirm the practical list, and author 2-3 methodology-recall questions per science subject area as a new `sci_practicals` topic (or folded into existing topics, whichever the curriculum owner prefers).
  - Risk: None at the scoping stage; per the hard authoring rule, do not author specific required-practical questions until the exact spec references are confirmed; a wrong practical name/step would be actively miseducating.

- [ ] **F6 · latest-stack (today's Sunday deep-dive) · Research spike: evaluate migrating from `framer-motion` v11 to the current `motion` package / v12-13 line** (size: S research, M if a migration follows)
  - The win: `npm outdated` shows `framer-motion` pinned at `^11.15.0` (installed 11.18.2, "Wanted" 11.18.2, already at the top of its own declared range) while the package's own latest published version is `13.1.1`, a two-major-version gap. Framer Motion rebranded starting with v11/v12: the maintainers' primary package is now published as `motion` (with `framer-motion` kept as a thin backward-compatible re-export), and the newer major versions carry API refinements and (per the vendor's own messaging) performance/bundle-size improvements relevant to the owner's stated direction of richly using framer-motion for delight. This is exactly the kind of "latest APIs they unlock" check the north-star brief asks for, and animation is a headline lane here (EPIC 6).
  - Change: a research-only spike first: read the `motion`/`framer-motion` v12→v13 changelog for breaking changes against this codebase's actual usage patterns (the shared `LazyMotion`/`ReducedMotionProvider` split, `AnimatePresence`, `whileTap`/`whileInView`, the `motion.div`/`motion.button`/`motion.path` primitives used throughout `practice-player.tsx`/`interaction.tsx`), then decide whether to (a) bump within the current `framer-motion` package name if the maintainers still publish compatible majors, or (b) migrate the import to `motion/react` per the vendor's own recommended path. Do not blind-bump a 2-major-version animation dependency across the entire child-facing delight surface without this research first; too much surface area (every celebration, hint entrance, phase-bar, See-it reveal) for a "just bump it" treatment.
  - Risk: Medium, this is the single riskiest dependency in the app for a big-jump bump (it touches nearly every child-facing animation), so treat this as research-and-report, not an automatic build item, even though it's filed as an F# for tracking; a full migration should be its own dedicated, thoroughly-tested pass with the complete `npm test`/`type-check`/`build` gate plus a live Playwright animation smoke across several lesson states, not folded into a routine dependency-bump commit.

- [ ] **F7 · a11y / dev-QA (closes the recurring EPIC 1 risk class properly, not just this instance) · Add negative-case unit tests to every `deriveXVisual` heuristic so a coincidental keyword/quote match can't silently ship an irrelevant or wrong figure again** (size: S/M)
  - The win: B4 above is the sixth distinct instance of the same root shape tracked in `automation/backlog.md` EPIC 1 ("shallow-regex/keyword-match-on-raw-prompt-text" derivers firing confidently on prompt text they shouldn't) across four different files. Each instance so far has been fixed reactively, one Scout-run discovery at a time. The derivers (`math-visual.ts`, `science-visual.ts`, `english-visual.ts`) are already pure and already unit-tested for their positive cases (does the right visual appear for the right prompt); none of them appear to carry a curated set of negative cases (prompts that superficially resemble a match but shouldn't produce one), which is exactly the gap that let five, then six, instances of this same bug class ship.
  - Change: for each deriver, add a `describe("does NOT derive a visual for...")` block with 3-5 real or realistic prompts that share surface features with a positive trigger (a single quoted word in a non-spelling context; a number-pattern in a non-arithmetic context; a science keyword used metaphorically) and assert `null`/no-match, seeded initially from this run's "stabbed"/tone-question case and the five prior fixed instances (grep `automation/memory.md` for the exact prompts that broke each time; they're all recorded).
  - Risk: Low, pure test-only addition, zero runtime risk; the value is entirely in catching the seventh instance before it ships rather than after a live Playwright pass finds it.

- [ ] **F8 · stack hygiene (routine, trivial) · Bump `tsx` 4.23.12 to 4.23.13** (size: S)
  - The win: `npm outdated` shows this is the one genuinely safe, zero-risk patch bump available this run (everything else outdated, `next`, `eslint`, `eslint-config-next`, `@next/bundle-analyzer`, `@types/node`, `typescript`, is a major-version jump deliberately out of scope per `automation/backlog.md` EPIC 7's staged Next 16 migration plan, or for `typescript`, a claimed "7.0" jump too far ahead of this project's actual toolchain to treat as routine). `npm audit` is at **0 vulnerabilities** both prod-only and full-tree this run (re-confirmed), so this is pure freshness, not a CVE fix.
  - Change: `npm install tsx@4.23.13` (or `npm update tsx`), then the standard gate (`npm run type-check && npm test && npm run lint && npm run build`).
  - Risk: None, `tsx` is a dev-only script runner (used for `npm run seed`/`smoke:setup` etc.), never part of the production bundle.

---

## Summary
- Bugs: 4 (0 Critical, 1 Medium/High, 2 Medium, 1 Low/Medium) · Features: 8
- Top 3 to ship: **B1** (a real, live-reproduced keyboard-accessibility gap that compounds on every question in every lesson: focus silently resets to the document's very top instead of the next primary action), **B3** (a live-reproduced, checker-passed AI response that opens "That's absolutely correct!" on content the child hasn't answered anything about yet, confusing framing traced to one hardcoded `wasCorrect: true`), **F1** (a fully-specified, hand-derived new GCSE question closing a real tier-range gap in the thinnest topic in the bank, plus a correction to a stale backlog premise about `sci_atoms`).

This run drove the child learning loop at maximum depth as **Ivy** (KS4-band, 29/30 topics certified going in; this test family is now deep into cross-band GCSE content). Took `eng_analysis` ("Language Analysis") end-to-end as a genuine first-time certification (not yet mastered going in): Explainer, then "Show me another way" (this is where B3 was found), then Practice 2/2 (wrong-then-right on Q1 with a calm no-red nudge plus "Why isn't that right?"; Q2 triggered a genuine breathing-break on the FIRST wrong answer via the "attention"-category immediate-break path in `shouldOfferBreak()`, confirmed not a bug), then "Start mastery", then Mastery 3/3 (**deliberately refreshed mid Mastery-check-2 to re-verify 2026-08-29's B1 fix, PASS, landed back on "Mastery check 1: 2 of 3" with score intact, not the Explainer**), then certified, then the certificate rendered correctly. All four interaction types were driven with both a correct and a wrong answer this run: `mcq` (full keyboard-only path including a confirmed visible focus ring and native roving-radiogroup arrow-key behaviour, this is where B1 was found), `drag_drop` (tested the raw pointer-drag gesture via synthetic `DragEvent`+`DataTransfer` dispatch, not just tap-to-place, closing the honest coverage gap flagged in 2026-08-29's report; full pass, correct settle with green checks on all three slots), `fill_blank` (wrong-then-right on the `2x+3=11` item, calm no-red nudge, a second wrong triggered the breathing break too), `tap_reveal` (`eng_devices`'s simile-spotting card question, reveal-then-select gesture, wrong-then-right). A rapid double-click on "Check answer" did not double-score. Parent oversight ran end-to-end: dashboard, child profile, generated a real portfolio for Ivy (29/30 certified, SHA-256 verified, "In progress" evidence status correctly since not all mocks are done), opened the public `/verify-portfolio?hash=...` page (confirmed hash match), schedule/tutoring/settings all clean. Admin (overview/finance/escalations) and tutor (empty "No sessions assigned yet" queue) both confirmed read-only and clean, data-silo holding. Auth surfaces inspected clean (`/signup` fields empty with clear copy, `/forgot-password` clean, `/.well-known/security.txt` and the CSP `report-uri` from 2026-08-29's F3/F4 both confirmed live via `curl`). Zero console errors and zero failed network requests across the entire session, both viewports. Static checks all green: `type-check`, `lint`, `npm test` (915/915 across 94 files), `npm audit` (0 vulnerabilities, prod-only and full-tree). Security headers (CSP with `report-uri`, HSTS 2yr plus preload, X-Frame-Options DENY, Permissions-Policy) all confirmed via `curl -IL` against `www.edway.uk` (bare `edway.uk` 308-redirects, same apex to www gotcha noted in past runs). Quick Web-Vitals spot-check on `/` (not today's deep-dive lane): LCP 600ms, CLS 0, TTFB 250ms, excellent, no finding. Vercel MCP was not in the tool list this run (consistent with many prior runs); `curl`-based `/api/health` (200) substituted throughout.
