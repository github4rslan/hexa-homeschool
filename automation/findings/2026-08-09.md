# Scout findings — 2026-08-09

DECISION: all

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           → build EVERY item below (default)
    B1, F2, F5    → build ONLY these IDs
    skip          → build nothing tonight
  Leave it as `all` and do nothing to run fully autonomous.
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright (MCP)  ·  Base URL: https://edway.uk (served from https://www.edway.uk)
Day-focus (UTC Sunday): latest-stack deep-dive. Standing lanes also run: full child
lesson pass/fail, curriculum/GCSE audit + authoring, delight/wrong-answer within the
calm-wrong law, and any critical/high bug regardless of the day.

## Coverage this run
- Marketing (/, /pricing, /how-it-works, /safety, /for-parents) + /login: desktop
  (innerWidth 1280, scrollWidth 1270, no overflow) AND mobile (innerWidth 390,
  scrollWidth 380, no overflow). Zero console errors. One h1 per page.
- /api/health = 200 `{ok, db:up}` (apex 308-redirects to www, expected).
- Parent (SMOKE): dashboard clean, no overflow, zero console errors.
- Child (Ivy, PIN): took Science "Forces & Motion" (sci_ks3_forces) end-to-end —
  explainer (worked-example StepReveal + glossary chips + narration) → practice
  3/3 (one deliberate WRONG) → mastery 3/3 → certified. PASS. See child pass/fail
  below. Zero console errors across the whole lesson.
- Admin (ADMIN): /admin overview + /admin/finance, READ-ONLY. Zero console errors.
- Tutor (TUTOR): /tutor, READ-ONLY. Empty queue ("No sessions assigned yet") —
  data-silo holds, no cross-family data. Availability toggle advisory-only (as designed).
- Static: `npm run type-check` GREEN, `npm run lint` GREEN, `npm audit` = 0 vulnerabilities.
- Security headers (curl): CSP locked (default-src 'self', frame-ancestors 'none',
  object-src 'none', base-uri/form-action 'self', upgrade-insecure-requests),
  HSTS 2yr+preload, X-Frame DENY, nosniff, Referrer-Policy, Permissions-Policy.
  Permissions-Policy is correctly route-scoped: microphone=(self) only under /learn,
  camera+mic (self + meet.jit.si) only under /tutor, denied elsewhere. No security bug.
- Teardown: logged out of all three accounts (POST /logout → 200 each) + browser_close.

## Child lesson pass/fail (Ivy — Science "Forces & Motion")
- Explainer (StepReveal worked example, glossary chip "Friction", narration button): PASS
- Practice mcq, deliberate WRONG (chose "friction"): PASS — calm law held. Copy
  "Ivy, not quite yet — have another go. You're closer than you think!", "Why isn't
  that right?" control, escalating hint, "2 tries left". Verified the wrong option's
  computed colour is oklab(0.606 0.085 -0.202) — a VIOLET tint (negative b = blue/
  violet), NOT red. No shake, no buzzer, no cross. PASS.
- Practice correct (gravity): PASS — star burst "Brilliant! 🎉", accent settle.
- Practice 2/3 + 3/3 correct: PASS ("You nailed it! ⭐", "Amazing work! 🚀").
- Mastery check 3/3 → certified: PASS ("Topic mastered! You got 3 out of 3 right").
- Band-promotion celebration fired (child-safe wording, no key-stage shown):
  "You unlocked a whole new set of Science adventures!" (F2, 2026-08-08 — verified live).
- Certificate + "See it on my journey" links present. Progress saved.

---

## Bugs (Critical → Low)

- [ ] **B1 · Low (a11y / Children's Code) · Parent dashboard + admin shell have no `<main>` landmark** — `src/app/(dashboard)/dashboard/page.tsx`, `src/app/(admin)/admin/layout.tsx`
  - Repro / evidence: on `/dashboard` (parent) and `/admin` (staff), `browser_evaluate`
    of `document.querySelector('main')` returns `null`; landmarks present were only NAV
    + HEADER. Every OTHER dashboard page (`/schedule`, `/settings`, `/tutoring`,
    `/dashboard/children/[childId]`) DOES render its own `<main>` (grep-confirmed), and
    the tutor + marketing shells do too — so the two busiest authed landing pages are the
    inconsistent ones.
  - Root cause: the dashboard group layout wraps children in `<div id="main-content" …>`
    (not `<main>`), and `/dashboard/page.tsx` renders straight into it with top-level
    `<div className="flex-1 p-6 …">` (lines ~136 and ~304) instead of `<main>`. The admin
    group (`(admin)/admin/layout.tsx`) has no `<main>` AND no `<SkipLink>` anywhere.
  - Fix: (a) change the two top-level `<div>` wrappers in `/dashboard/page.tsx` to
    `<main>` (do NOT flip the layout `#main-content` div to `<main>` — the child pages
    under it already render their own `<main>`, which would nest). (b) In the admin
    layout add a `<main>` wrapper around `{children}` and a `<SkipLink />` (reuse
    `components/ui/skip-link.tsx`, target `#main-content`), mirroring the marketing/
    dashboard shells. Screen-reader landmark navigation + the skip-link then work on the
    most-visited authed surfaces.
  - Risk: pure structural/semantic; no layout change (main is display:block like div).
    Verify the skip-link still lands (target id present) and no double-main on child pages.

- [ ] **B2 · Low (a11y) · Marketing homepage has a colour-contrast violation flagged by the shipped axe pass** — marketing `/`
  - Repro / evidence: the `@axe-core/playwright` pass added on 2026-08-08
    (`npm run a11y --project=public`, `e2e/a11y-audit.ts`) reported ONE pre-existing
    SERIOUS `color-contrast` violation on the marketing homepage (recorded in the
    2026-08-08 mechanic run log). It was surfaced but never fixed — the pass only FAILS
    on `critical`, so a `serious` contrast miss ships.
  - Root cause: a foreground/background token pair on `/` falls below the WCAG AA 4.5:1
    ratio (likely a muted `fog`/`text-*-400` on a dark panel). Exact node not captured
    this run (headless axe run deferred for budget).
  - Fix: Mechanic should run `npm run a11y --project=public` to get the exact selector +
    measured ratio, then raise the offending text token to ≥4.5:1 (or ≥3:1 for large
    text) — a token/utility swap, no structural change. Consider making the a11y pass
    also fail on `serious` for marketing so this can't silently regress.
  - Risk: purely a colour value; re-run the axe pass to confirm 0 serious after the fix.

## Features & upgrades (High value → Low)

- [ ] **F1 · curriculum / GCSE readiness (HEADLINE) · Author 5 exam-style command-word questions for topics that have none** (size: S)
  - The win: EPIC 2 (make questions feel like the real paper). F7 (2026-08-09) added 6
    command-word items but left most KS4 topics on single-step recall MCQs. These 5 add
    real command words ("Solve", "Calculate", "Work out", "Explain") + index-aligned
    misconceptions to five uncovered topics. Each has exactly ONE defensible answer, is
    fully computable, and renders through the existing mcq + misconception path (no engine
    change). Natural key = topic_tag + prompt, all prompts new ⇒ clean insert. Transcribe
    VERBATIM into the `EXAM_STYLE_QUESTIONS` array in
    `src/lib/data/curriculum.seed.ts`, then `npm run seed` (idempotent, curriculum-only).
  - Change: append these `SeedQuestion` objects (all `key_stage: 4`):

    ```ts
    // Edexcel 1MA1 A17 — solve a linear equation ("Solve").
    { topic_tag: "maths_algebra_linear", subject: "mathematics", tier: 4, key_stage: 4, kind: "mastery",
      prompt: "Solve 4x + 5 = 29.",
      options: ["x = 6", "x = 8.5", "x = 24", "x = 34"], correct_index: 0,
      explanation: "Subtract 5 from both sides: 4x = 24. Divide both sides by 4: x = 6.",
      hints: ["Subtract 5 from both sides first.", "Then divide both sides by 4: 24 divided by 4."],
      misconceptions: ["", "Looks like you added 5 instead of subtracting: (29 + 5) ÷ 4. Subtract 5 first, then divide.", "Looks like you subtracted 5 but did not divide by 4. 24 ÷ 4 is 6.", "Looks like you added 5 and did not divide. Subtract 5, then divide by 4."] },

    // Edexcel 1MA1 G20 — Pythagoras ("Calculate").
    { topic_tag: "maths_pythagoras", subject: "mathematics", tier: 5, key_stage: 4, kind: "mastery",
      prompt: "A right-angled triangle has its two shorter sides 6 cm and 8 cm. Calculate the length of the hypotenuse.",
      options: ["10 cm", "14 cm", "48 cm", "100 cm"], correct_index: 0,
      explanation: "By Pythagoras, the hypotenuse squared is 6² + 8² = 36 + 64 = 100. The square root of 100 is 10 cm.",
      hints: ["Square both shorter sides and add them: 6² + 8².", "Then take the square root of the total."],
      misconceptions: ["", "Looks like you added the two sides (6 + 8). Pythagoras squares them first: 6² + 8².", "Looks like you multiplied 6 × 8. Square each side and add, then square root.", "That is 6² + 8² before the square root. Take the square root of 100."] },

    // Edexcel 1MA1 P7 — single-event probability ("Work out").
    { topic_tag: "maths_statistics", subject: "mathematics", tier: 3, key_stage: 4, kind: "mastery",
      prompt: "A bag has 3 red, 5 blue and 2 green counters. One counter is taken at random. Work out the probability that it is blue.",
      options: ["1/2", "1/5", "3/10", "5/3"], correct_index: 0,
      explanation: "There are 3 + 5 + 2 = 10 counters. 5 of them are blue, so the probability is 5/10, which simplifies to 1/2.",
      hints: ["Count the total number of counters first: 3 + 5 + 2.", "Probability is the number of blue over the total, then simplify."],
      misconceptions: ["", "Looks like the green count over the total (2/10). Blue is 5 out of 10.", "That is the probability of red (3 out of 10). Blue is 5 out of 10.", "That compares blue to green, not blue to the whole bag. Use blue over the total of 10."] },

    // AQA 8464 Physics 4.2.1 — V = I × R ("Calculate").
    { topic_tag: "sci_electricity", subject: "science", tier: 3, key_stage: 4, kind: "mastery",
      prompt: "A current of 2 A flows through a resistor of 5 Ω. Calculate the potential difference across the resistor.",
      options: ["10 V", "2.5 V", "7 V", "0.4 V"], correct_index: 0,
      explanation: "Using V = I × R: potential difference = 2 A × 5 Ω = 10 V.",
      hints: ["Use the equation potential difference = current × resistance (V = I × R).", "Multiply 2 A by 5 Ω."],
      misconceptions: ["", "Looks like you divided 5 by 2. Potential difference is current times resistance: 2 × 5.", "Looks like you added 2 and 5. Use V = I × R, so multiply.", "Looks like you divided current by resistance. V = I × R, so 2 × 5."] },

    // AQA 8700 Paper 1 Q3-style — effect of a simile ("Explain").
    { topic_tag: "eng_analysis", subject: "english", tier: 4, key_stage: 4, kind: "mastery",
      prompt: "'The old house sagged, its windows like tired eyes.' Explain the main effect of the simile 'like tired eyes'.",
      options: ["It makes the house feel weary and neglected", "It shows the house is brand new", "It creates a fast, exciting pace", "It just lists factual details about the windows"], correct_index: 0,
      explanation: "The simile compares the windows to 'tired eyes', which — with 'old' and 'sagged' — makes the house feel worn out, weary and neglected.",
      hints: ["'Tired eyes' compares the windows to something weary — what mood does that create?", "Link the words 'old', 'sagged' and 'tired' to how the house feels."],
      misconceptions: ["", "A sagging house with 'tired' eyes suggests age and neglect, not newness.", "A simile about tiredness slows the mood; it does not speed the pace.", "'Like tired eyes' is a comparison for effect, not a factual list."] },
    ```
  - Risk: none to the engine. Answers re-derived (4x=24→x=6; 6²+8²=100→10; 5/10=1/2;
    2×5=10; simile→weary). Every distractor is a distinct value (no equal-value trap).

- [ ] **F2 · curriculum coverage (EPIC 3) · Add the missing GCSE Maths "Inequalities" strand + 3 authored starters** (size: M)
  - The win: Edexcel 1MA1 A22 (solve linear inequalities, represent on a number line) is a
    high-frequency Foundation+Higher topic with NO topic in the bank. Adding it continues
    the F8 mensuration precedent. The mock gate is already count-driven-safe
    (`lib/engine/mock-gate.ts` `mockUnlockCount = min(gcseTopicCount, 10)`), so a 12th
    maths topic will NOT break the mock unlock.
  - Change: add a `maths_inequalities` topic to `BASE_SEED_TOPICS` in
    `curriculum.seed.ts` (`title: "Inequalities"`, `key_stage: 4`,
    `working_grade_band: "Grade 3–5"`, `order: 12`, `prerequisite_tags:
    ["maths_algebra_linear"]`) with a `worked_example` (solve `x + 3 < 7` on a number
    line — Mechanic to author the StepReveal in the WORKED_EXAMPLES map + a short glossary
    for "inequality"/"number line"), plus these starters (transcribe verbatim):

    ```ts
    // maths_inequalities (Edexcel 1MA1 A22)
    [3, "practice", "Solve the inequality x + 3 < 7.", ["x < 4", "x < 10", "x > 4", "x < 21"], 0, "Subtract 3 from both sides: x < 4."],
    [4, "mastery", "Solve 2x ≥ 10.", ["x ≥ 5", "x ≥ 20", "x ≥ 8", "x ≤ 5"], 0, "Divide both sides by 2: x ≥ 5. The sign does not flip when dividing by a positive number."],
    [4, "mastery", "Which integers satisfy −2 < x ≤ 1?", ["−1, 0, 1", "−2, −1, 0, 1", "0, 1", "−2, −1, 0"], 0, "−2 is excluded (strict <) and 1 is included (≤), so the integers are −1, 0 and 1."],
    ```
    A 2-mastery topic certifies at 2/2 (per `selectMasteryAttempt` min(bank,3) + score===total),
    so add a 3rd mastery or keep the practice+2 mastery — either is a clean certify path.
    Then `npm run seed`.
  - Risk: a new topic touches selection/roadmap/hub, but all are band + count driven and
    the mock-gate is already safe. Confirm the topic is reachable/certifiable after seed.

- [ ] **F3 · stack (latest-stack deep-dive) · Refresh the in-range dependency patch/minor drift** (size: S)
  - The win: several deps are behind their in-range latest with security/bugfix content and
    zero API change. Keeping current is the cheapest hygiene and keeps `npm audit` at 0.
  - Change: `npm update` (respecting the `^` ranges) or targeted bumps: next 15.5.22→15.5.23,
    @sentry/nextjs 10.57→10.69, mongodb 7.2→7.5, stripe 22.2→22.4, jose 6.2.3→6.2.8,
    postcss 8.5.23→8.5.26, autoprefixer 10.5.0→10.5.4, @upstash/redis 1.38.0→1.38.2,
    @playwright/test 1.60→1.62, vitest 4.1.8→4.1.10, tsx 4.22→4.23, geist 1.7.1→1.7.2,
    eslint 9.39.4→9.39.5, @types/react 19.2.15→19.2.18, @types/react-dom→19.2.4,
    @types/node (stay on 22.x — do NOT jump to 26 major), eslint-config-next stay 15.5.x
    (16 is a major — see F4). Then type-check + tests + lint + build, confirm audit 0.
  - Risk: all in-range; run the full gate + a homepage console smoke on the deploy.
    posthog-js (1.386→1.414) is a bigger minor with analytics-client shift risk — bump
    only if the gate + a parent-page console check stay clean, else leave (memory has
    deferred it twice; it is parents-only and never in `(child)`).

- [ ] **F4 · stack (latest-stack deep-dive) · Evaluate + stage the Next.js 16 major upgrade** (size: L)
  - The win: Next 15.5.22 → 16.3.0 is now GA (also eslint-config-next 16, eslint 10). Next 16
    brings the stabilised caching model, Turbopack-by-default, faster builds and newer React
    APIs — a real perf + security + longevity win for a push-to-prod app. This is the headline
    latest-stack item, but it is a MAJOR with breaking changes, so it is an evaluate-then-stage
    task, not a blind bump.
  - Change: read the official Next 16 upgrade guide; run `npx @next/codemod@canary upgrade
    latest` on a throwaway branch; check the known 15→16 breaks (async request APIs
    `cookies()/headers()/params` already awaited here?, caching defaults, `next/image`
    changes, middleware matcher, the `images`/`headers()` config in `next.config.ts` where
    the route-scoped CSP + Permissions-Policy live — MUST survive intact). Verify the CSP
    nonce path (see F7), the hand-rolled `public/sw.js`, and the child animation surfaces.
    Gate hard: type-check + all tests + build + a full Playwright child-lesson smoke on the
    Vercel preview BEFORE it hits main. If anything in the security headers or child flow
    regresses, hold.
  - Risk: HIGH (major). Do it isolated, gate on the deployed preview, and keep the
    route-scoped security headers + child calm-law animations verified before merging.

- [ ] **F5 · stack (latest-stack deep-dive) · Evaluate the remaining major-version bumps (motion, lucide-react, tailwind-merge, eslint 10)** (size: M)
  - The win: framer-motion 11 → the renamed `motion` package (v12/13), lucide-react
    0.469 → 1.31, tailwind-merge 2.6 → 3.6, eslint 9 → 10. Staying on the maintained lines
    unlocks smaller bundles + newer APIs and avoids a big-bang later.
  - Change: `framer-motion`→`motion` is mostly a rename (`import { motion } from
    "motion/react"`); it is used HEAVILY across the child calm-law animations
    (interaction settle, star burst, phase bar, See-it), so migrate carefully and re-verify
    a full lesson + `prefers-reduced-motion` neutralisation before shipping. tailwind-merge
    3 + lucide-react 1 are lower-risk swaps (check the `cn()` helper + any renamed icons).
    Prefer to land these AFTER/with F4 so the stack moves once.
  - Risk: MEDIUM — the motion rename touches every animated child surface; gate on a live
    child-lesson pass (correct settle, calm-wrong stays violet-not-red, reduced-motion off).

- [ ] **F6 · delight / wrong-answer (calm-wrong law) · Warm the hint entrance + a calm "See it" beckon on a miss** (size: S)
  - The win: standing owner priority — supportive wrong-answer motion. Today a miss shows
    the calm dim + hint text + (after the 2nd miss) the "See it" unlock, but the hint and
    the See-it control just appear. Give the miss a gentle "here's a hand" feel: slide the
    hint card in from a small offset with a soft fade, and give the newly-unlocked "See it"
    button a ONE-TIME calm beckon (a slow opacity/scale breathe, ~1s, then rest) so the
    child's eye is drawn to the method, not to the mistake.
  - Change: in `src/components/child/practice-player.tsx` (and the hint render in
    `interaction.tsx`), wrap the hint card in a `framer-motion` entrance
    (`initial={{opacity:0, y:6}} animate={{opacity:1, y:0}}`) and add a bounded beckon on
    the See-it button when it first mounts. Reuse the existing `useReducedMotion` guard so
    both fully neutralise. No red/shake/buzzer, no input delay (motion is non-blocking and
    the button is clickable immediately), no tracking.
  - Risk: LOW — presentational; verify reduced-motion collapses it to static and the beckon
    never blocks or delays the tap. Stays strictly inside the calm-wrong law.

- [ ] **F7 · security hardening · Move to a nonce-based CSP and drop `script-src 'unsafe-inline'`** (size: M)
  - The win: the CSP is otherwise excellent, but `script-src` still allows `'unsafe-inline'`,
    which is the one meaningful XSS weakening (an injected inline `<script>` would execute).
    A per-request nonce lets Next's inline bootstrap scripts run while blocking any
    un-nonced inline script — the strongest single CSP hardening available for a children's
    platform.
  - Change: generate a per-request nonce in `middleware.ts`, thread it into the CSP header
    (`script-src 'self' 'nonce-<n>' https://meet.jit.si`) and into Next via the documented
    nonce integration so first-party inline scripts carry it. Keep `style-src
    'unsafe-inline'` for now (Tailwind/inline styles need it; tightening styles is a separate,
    harder step). Verify no console CSP violations on marketing, dashboard, AND the child
    lesson (Jitsi + Cloudinary + Sentry + PostHog connect-src must still pass). Best paired
    with F4 (Next 16 has first-class nonce support).
  - Risk: MEDIUM — a missed inline script breaks silently; roll out behind a live
    console-violation smoke on every route group before trusting it.

- [ ] **F8 · retention (EPIC 5) · Surface "topics due for review" to the parent in the weekly digest** (size: S)
  - The win: the child-side spaced-rep loop is solid (warm-up interleaves subjects, interval
    doubles on a correct recall capped at 90 days — verified in `spaced-repetition.ts`), but
    the PARENT has no visibility of review debt. When a child skips warm-ups, certified
    topics quietly go overdue and decay before exam day. A deterministic "N topics are due
    for review this week (M overdue)" line in the weekly digest lets the parent nudge a
    review session — no child profiling, purely from `next_review_at` dates.
  - Change: in `lib/engine/weekly-summary.ts` (+ the `/api/digest/weekly` template), compute
    due/overdue counts from the child's `competence_matrix.next_review_at` (reuse the
    existing `isDueForReview` primitive) and add one warm line + a `/learn/warmup` CTA.
    Deterministic, parent-scoped, ownership-checked; nothing new tracked on the child.
  - Risk: LOW — read-only aggregation over existing schedule dates; keep it qualitative
    (counts + encouragement), never a per-child psychological read.

---

## Summary
- Bugs: 2 (0 critical / 0 high / 0 med / 2 low)  ·  Features: 8
- Top 3 to ship:
  - F1 — 5 exam-style command-word questions (Solve/Calculate/Work out/Explain), full
    SeedQuestion shape, five topics that had none. Biggest GCSE-readiness win, lowest risk.
  - F4 — evaluate + stage the Next.js 16 upgrade (headline latest-stack; gate on preview).
  - B1 — add the missing `<main>` landmark to the parent dashboard + admin shell (+ admin
    skip-link); a11y/Children's-Code gap on the two busiest authed pages.
- Note: the seed bank re-audited CLEAN this run (re-derived every quantitative answer in
  curriculum.seed.extra.ts + the F7 exam-style items + F8 mensuration; the B3 water-
  absorption reword is correct). No correctness bugs to file — the curriculum value now is
  coverage + exam-condition fidelity, per EPIC 1's finding.
