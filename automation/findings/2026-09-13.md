DECISION: all

# Scout findings, 2026-09-13

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           -> build EVERY item below (default)
    B1, F2, F5    -> build ONLY these IDs
    skip          -> build nothing tonight
  Leave it as `all` and do nothing to run fully autonomous.
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright (MCP tools only). Base URL: https://edway.uk

Day-of-week focus: **Sunday, latest-stack + growth/discoverability deep-dive**,
plus the standing max-depth child pass and every-day lanes (bugs, curriculum,
journeys) regardless of focus.

Note on date: the harness's local-time system reminder said 2026-09-14, but
`date -u` (checked three ways: bash `date -u`, bash `date`, and PowerShell
`[DateTime]::UtcNow`) all agree the real UTC date is **2026-09-13** (a Sunday).
Filing under the correct UTC date per instructions, since the local clock is a
different timezone (PST) and had drifted a day ahead.

Desktop pass confirmed at `innerWidth` **1280** (`browser_resize` 1280x800).
Mobile pass confirmed at `innerWidth` **390**, `scrollWidth` **380** (no
overflow) on `/`, `/pricing`. No horizontal overflow found on any page checked
at either width.

Static checks: `npm run type-check` GREEN, `npm test` GREEN (107 files, 1089
tests), `npm run lint` GREEN, `npm audit` **0 vulnerabilities**. `npm outdated`
shows a small in-range batch (`autoprefixer` 10.5.6 to 10.6.0, `posthog-js`
1.430.2 to 1.430.3, `tailwind-merge` 3.6.0 to 3.7.0) plus the deliberate major
holds already tracked in `automation/backlog.md` EPIC 7 (`next`/`eslint`/
`eslint-config-next`/`@next/bundle-analyzer` all blocked together on the
Next 15 to 16 migration). `typescript` (5.9 to 7.0) and `vitest` (4 to 5) are
new major jumps since the last check, filed below as F6 alongside the small
patch batch.

---

## Bugs (Critical to Low)

- [x] **B1 · High (re-opens EPIC 20, homepage-only) · the homepage throws a React hydration-mismatch error (minified React #418) on every fresh load, desktop and mobile** : no single file, whole-tree hydration
  - Repro / evidence: fresh `browser_navigate('https://edway.uk/')` (never a client-side route transition, always a hard `goto`) reproduced the exact same console error four separate times this run: twice at 1280px desktop, once after a hard reload, and once at 390px mobile. Each time: `Minified React error #418; visit https://react.dev/errors/418?args[]=HTML&args[]=` with the same stack trace rooted in the same `4bd1b696-...js` chunk. `browser_network_requests` confirms Sentry actually captures this error 4 times per pageview (four `POST .../envelope/` calls fired), so this is a real, already-monitored production error, not a one-off. No other marketing page (`/pricing`, `/how-it-works`, `/safety`, `/for-parents`, `/roadmap`, `/contact`, `/about`, `/resources`, `/compliance`, `/childrens-code`, `/local-authorities`, `/demo`, `/gallery`, `/agents`) threw it, so this is homepage-scoped, not sitewide. Two independent fresh `curl` fetches of `/` returned byte-identical HTML, ruling out server-side non-determinism as the direct cause. Note: `StatsStrip`'s `CountUp` counters were separately confirmed to work correctly once actually scrolled into view; an initial reading of "stuck at 0" was a false alarm from checking before the section entered the viewport, recorded here so a future run does not re-chase it as a bug.
  - Root cause: not fully pinned down this run. EPIC 20 was previously retired 2026-09-03 after four consecutive clean checks (last clean re-check 2026-09-08), so this is a fresh regression, not a continuation of the old unresolved case. A code read of every homepage-only component (`Hero`, `TrustBar`, `StatsStrip`, `Problem`, `Solution`, `JourneyPreview`, `AgentsPreview`, `FeatureGrid`, `SafetyPreview`, `Testimonials`, `CompliancePreview`, `CTA`, plus `ReducedMotionProvider`/`LazyMotionProvider`) found no `Math.random`/`Date.now`/`typeof window`-gated render branch that would explain a server/client text mismatch. The most likely remaining candidate, not yet confirmed, is a build-id/version skew between the homepage's edge-cached HTML (`X-Vercel-Cache: HIT`, served with a non-zero `Age`) and the currently-deployed client JS bundle: the homepage is the single most heavily cached, highest-traffic route, which fits a caching-skew theory better than a code-level mismatch a static read failed to find.
  - Fix: (1) reproduce with React's non-minified dev build locally (`next dev` is off-limits per `CLAUDE.md`, so use the full unminified error text from `https://react.dev/errors/418` cross-referenced against a local `next build && next start` instance) to get the real component name in the mismatch; (2) in parallel, check whether purging or forcing a fresh edge render of `/` (a new deploy, or an explicit revalidation) makes it disappear, which would confirm the caching-skew theory without needing the exact component; (3) once found, apply the standard fix (move the non-deterministic value into a `useEffect` plus state, or add `suppressHydrationWarning` narrowly if the mismatch is provably cosmetic).
  - Risk: Low to investigate (read-only plus one local build); the eventual fix's risk depends entirely on what's found, likely low given the homepage otherwise renders and functions correctly (this is a hydration warning turned error, not a visible breakage).

- [x] **B2 · Medium (growth/credibility, sitewide) · `twitter:image` points at a real, live 404 on every single page, while `og:image` correctly resolves, so sharing an Edway link on X/Twitter specifically shows a broken preview image** : `src/app/layout.tsx:50-57` (`openGraph.images`) and `:59-65` (`twitter.images`)
  - Repro / evidence: `curl -sI https://www.edway.uk/og-image.png` returns a genuine `404 Not Found` (confirmed twice, plus a filesystem check: no file named anything like `og-image` exists anywhere under `public/`). Yet the live homepage's actual `<meta property="og:image">` tag (checked via `curl -s https://www.edway.uk/ | grep og:image`) correctly reads `https://edway.uk/opengraph-image?748f57ea6c6daeb2`, Next.js's file-convention dynamic image route (`src/app/opengraph-image.tsx`, a fully-built, on-brand `next/og` `ImageResponse` with the real Edway wordmark, gradient, and tagline), because Next's special file-based `opengraph-image` route wins over the sitewide `metadata.openGraph.images` array declared in `layout.tsx` once both exist. But `<meta name="twitter:image">` still reads the broken `https://edway.uk/og-image.png` directly (also confirmed via `curl`), because there is no `twitter-image.tsx` file-convention equivalent to override the explicit `metadata.twitter.images` array, so that one field is taken literally and 404s. Net effect: Facebook, LinkedIn, Slack, WhatsApp and iMessage unfurls (which read `og:image`) show the good branded image; X/Twitter cards (which read `twitter:image` specifically) show a broken or blank image on every single page, since no page overrides this sitewide default.
  - Root cause: `layout.tsx`'s `metadata.openGraph.images` and `metadata.twitter.images` both still literally reference `/og-image.png`, a static asset that was apparently planned but never actually created. The `openGraph` field is silently rescued by the newer `opengraph-image.tsx` file convention (built separately, likely after the static metadata was first written, and never cross-checked against it), but the `twitter` field has no equivalent rescue.
  - Fix: delete the `images` line from the `twitter` object in `layout.tsx`'s `metadata` (Next.js's Twitter Card generation falls back to the Open Graph image when `twitter.images` is absent, the same mechanism already rescuing `og:image`); while there, also delete the now-dead `openGraph.images` array pointing at the same nonexistent file, since it is already superseded live and just risks confusing a future reader into thinking it is the active path. Re-verify with a fresh `curl` of `/` afterwards: both `og:image` and `twitter:image` should resolve to the same working `/opengraph-image` route.
  - Risk: Very low. Pure metadata deletion; the working file-convention route is untouched and unaffected by removing the dead override.

- [x] **B3 · Medium (compliance-document correctness) · a child's predicted grade renders as a literal duplicated word, "Grade Grade 4-5", on the printable Local Authority registration pre-fill and, by the same root cause, on the parent-facing exam-decision card, whenever the source evaluation came from a mock exam rather than a diagnostic** : `src/app/(dashboard)/compliance/cnis/page.tsx:49` (confirmed live) and `src/lib/engine/exam-decision.ts:120` (confirmed by code, same data shape)
  - Repro / evidence: as the SMOKE parent, `/compliance/cnis` for Ivy (whose latest Maths evaluation is a mock, "Mock score 100%") renders: "Core subjects & current standing: Mathematics (working Grade Grade 4-5), English, Science", the word "Grade" appearing twice back to back, on a document explicitly meant to be printed and submitted to a real Local Authority officer under the Children's Wellbeing and Schools Act 2026. Root-caused via `src/lib/data/diagnostic.ts:34-43`'s `tierToGrade()` (used by the mock-exam evaluation path, `src/lib/engine/mock-exam.ts:62,101`), which returns a full string already containing the word, for example `"Grade 4-5"`, whereas the plain-diagnostic path (`onboarding/diagnostic/actions.ts:145-149`'s `gradeFromBand()`) returns a bare number like `"5"`. Both `cnis/page.tsx:49` (`` `(working Grade ${s.grade})` ``) and `exam-decision.ts:120` (`` `Working at ${focus.grade ? `Grade ${focus.grade}` : ...}` ``) unconditionally prepend the literal word "Grade" to `s.grade`/`focus.grade`, assuming the bare-number shape, so either call site doubles the word whenever the underlying evaluation happens to be mock-derived. Confirmed live for the `cnis` page (exact repro above); the `exam-decision.ts` site was not independently live-driven this run because the one 13-plus test child with an active exam-decision card (Sam Smoke) happens to have a diagnostic-sourced (bare-number) Maths grade today, not a mock-sourced one. The code path is identical and the data shape (`model_predicted_grade` from a mock) is the same one already proven live on Ivy, so this is filed with high confidence even without a fresh screenshot of that second card.
  - Root cause: `model_predicted_grade` is inconsistently formatted at the two places that write it (diagnostic path writes a bare number; mock path writes a full "Grade X-Y" string), so any downstream reader that assumes one shape breaks on the other.
  - Fix: normalize at the source rather than patching each call site (there may be more not yet found). Change `tierToGrade()` (or wherever the mock path consumes it before writing `model_predicted_grade`) to store the same bare-band shape the diagnostic path already uses (for example `"4-5"` without the word "Grade"), and have each display site decide its own prefix wording ("Grade", "working Grade", etc.) consistently. Add a small unit test asserting `model_predicted_grade` never contains the substring "Grade" regardless of source, so a future reader can safely prepend it.
  - Risk: Low to medium. Touches a shared field read in several places (dashboard card, child-profile "Current standing", the exam-decision card, the cnis pre-fill), so grep every consumer of `model_predicted_grade`/`.grade` before shipping to confirm none of them expect the old "Grade X-Y" shape verbatim (for example a check like `grade.startsWith("Grade")`).

- [x] **B4 · Low (record-correction, EPIC 7 / EPIC 20 lineage) · the still-open "Fraunces preload" investigation (B4, 2026-09-12) was chasing the wrong font: the persistent "preloaded but unused" warning on `/login` and `/signup` is GeistSans/GeistMono, not Fraunces** : `src/app/layout.tsx` (declares `GeistSans`/`GeistMono`), `automation/backlog.md` EPIC 7
  - Repro / evidence: `curl -sI https://www.edway.uk/login` (zero browser state, no prior navigation) confirms the server itself sends `Link: rel=preload` headers for two font files, `0b78ff376f6b9734-s.p.woff2` and `723e11e5093b8e80.p.woff2`, the same two hashes 2026-09-08's original B4/B5 finding attributed to Fraunces. Fetching the actual CSS chunk that declares these files this run (`curl` the referenced `_next/static/css/531c1fcb1e69d47b.css`) shows they are declared as `@font-face{font-family:GeistSans...}` and `@font-face{font-family:GeistMono...}`, the site's own primary body/heading fonts, declared in the root layout and used on every single page including `/login` itself, not the marketing-only Fraunces serif. `preload: false` on the `(marketing)/layout.tsx` Fraunces call (shipped 2026-09-12) is confirmed working correctly: `/dashboard` and `/settings` show zero preload warnings via clean `curl` checks (no `Link:` header at all), and `/resources` (a real marketing page that legitimately uses Fraunces) is the only page seen with a genuinely Fraunces-related warning. The residual GeistSans/GeistMono warning on `/login`/`/signup` specifically (not `/dashboard`, not `/settings`, not the homepage) is very likely a benign, well-known Chrome heuristic false-positive on `next/font`'s variable-weight (`100 900`) font preloading, since GeistSans is unambiguously the real rendered body font on those pages.
  - Fix: none needed with urgency; this is a record-correction so a future run stops re-investigating "Fraunces" on `/login`/`/signup`. If the warning is worth chasing at all, it is a GeistSans/GeistMono variable-font preload heuristic, a completely different, much lower-stakes investigation than the one EPIC 7/EPIC 20 have been carrying. Recommend closing that thread rather than reopening it, since the actual font in question (the site's universal body font) is provably used on every page it preloads on.
  - Risk: None (documentation-only finding).

- [ ] **B5 · Low (content correctness, already-seeded) · the brand-new GCSE-tier stretch question for `maths_quadratics` (2026-09-12's F3) renders literal ASCII math notation ("^2", "+/-", "sqrt(...)") instead of the unicode ("squared", "plus-minus", "root") every other question in the bank uses** : `src/lib/data/curriculum.seed.ts:1991` (prompt), `:1995` (explanation), `:1997-1998` (hints)
  - Repro / evidence: live-drove the full `maths_quadratics` mastery-to-certificate-to-brain-stretch flow as Ivy this run; the "Fancy a brain stretch?" bonus question rendered verbatim as authored: "Use the quadratic formula to solve x^2 + 2x - 2 = 0, where x = (-b +/- sqrt(b^2 - 4ac)) / 2a." Every other quadratics question in the same file renders with real unicode math symbols instead of ASCII approximations, so this one question, seeded the same day the bank's very first GCSE-tier stretch item shipped, reads a generation behind the rest of the file it sits in, purely a formatting/authoring miss rather than a correctness one (the maths itself was independently re-derived and confirmed correct this run: 0.73 squared plus 2 times 0.73 minus 2 is approximately 0.0006, approximately 0, checks out).
  - Fix: text-only edit of the three seed-file lines above: `x^2` to the proper superscript, `+/-` to the proper plus-minus symbol, `sqrt(b^2 - 4ac)` to the proper root notation (and the matching occurrences in `hints[0]`/`hints[1]`), matching the exact unicode characters already used elsewhere in the same file. No `correct_index`, `options`, or logic changes.
  - Risk: Very low. Pure text formatting on an already-seeded, already-live document; no re-derivation needed since the underlying maths is untouched.

## Features & upgrades (High value to Low)

- [ ] **F1 · curriculum (north-star, EPIC 23 slice) · author a GCSE-tier stretch question for `sci_atoms`: relative atomic mass from isotopic abundance** (size: S)
  - The win: EPIC 23 (opened 2026-09-12) tracks that GCSE-tier "stretch" content is almost entirely missing from the bank; the backlog explicitly named `sci_atoms` (isotope-abundance calculation) as a good candidate for a future run. Spec-verified this run directly against AQA's own specification page (`aqa.org.uk/subjects/science/gcse/science-8464/specification/chemistry-subject-content`, section 5.1.1.6 "Relative atomic mass": "The relative atomic mass of an element is an average value that takes account of the abundance of the isotopes of the element. Students should be able to calculate the relative atomic mass of an element given the percentage abundance of its isotopes.") This is exactly that skill, using the classic, textbook-standard chlorine example (re-derived clean: 0.75 times 35 plus 0.25 times 37 equals 26.25 plus 9.25 equals 35.5, matching chlorine's real relative atomic mass).
  - Change: seed this `SeedQuestion` into `sci_atoms` in `curriculum.seed.ts`:
    ```ts
    {
      topic_tag: "sci_atoms",
      subject: "science",
      tier: 5,
      key_stage: 4,
      kind: "stretch",
      prompt:
        "Chlorine has two naturally occurring isotopes: 75% of chlorine atoms are chlorine-35 and 25% are chlorine-37. Calculate the relative atomic mass of chlorine using this abundance data.",
      options: ["35.5", "36", "35", "72"],
      correct_index: 0,
      explanation:
        "Relative atomic mass is a weighted average based on isotope abundance: (75% x 35) + (25% x 37) = 26.25 + 9.25 = 35.5. This matches chlorine's real relative atomic mass.",
      hints: [
        "Multiply each isotope's mass number by its percentage abundance (as a decimal), then add the results together.",
        "0.75 x 35 = 26.25, and 0.25 x 37 = 9.25. Add these two amounts to get the relative atomic mass.",
      ],
      misconceptions: [
        "",
        "This is a simple average of 35 and 37 ((35 + 37) / 2), which ignores that chlorine-35 is three times more abundant than chlorine-37.",
        "This uses only the mass number of the more common isotope (chlorine-35) and ignores chlorine-37 completely.",
        "This adds the two mass numbers together (35 + 37) instead of taking a weighted average based on abundance.",
      ],
    },
    ```
  - Risk: None at authoring stage. Standard, unambiguous GCSE Higher-tier content, spec-cited live this run, re-derived clean. Owner approval gate via `DECISION:` still applies before seeding.

- [ ] **F2 · curriculum (north-star, EPIC 23 slice) · author a GCSE-tier stretch question for `maths_pythagoras`: 3D Pythagoras (cuboid diagonal)** (size: S)
  - The win: the backlog also named `maths_pythagoras` (a 3D Pythagoras/trig extension) as an EPIC 23 candidate. Confirmed by re-reading the topic's full 5-question pool this run (`curriculum.seed.ts:711-749` plus `curriculum.seed.extra.ts:68-72`): every existing question is 2D only (hypotenuse, tan ratio, cos ratio, a 6-8-10 right-angle check), zero "stretch" items, zero 3D content. Spec-verified live this run: Edexcel 1MA1 G20 is Higher-tier-explicit that Pythagoras applies "in two and three dimensional figures" (confirmed against the Pearson specification PDF and a savemyexams Higher-tier revision note using the same 3D-Pythagoras formula, distance squared equals x squared plus y squared plus z squared, whose own worked example, 4, 6, 3 giving the square root of 61 which is about 7.81, independently cross-checks the numbers used below).
  - Change: seed this `SeedQuestion` into `maths_pythagoras`:
    ```ts
    {
      topic_tag: "maths_pythagoras",
      subject: "mathematics",
      tier: 5,
      key_stage: 4,
      kind: "stretch",
      prompt:
        "A cuboid has length 6 cm, width 4 cm and height 3 cm. Calculate the length of the diagonal running from one corner of the cuboid to the opposite corner, to 1 decimal place.",
      options: ["7.8 cm", "7.2 cm", "5.0 cm", "13 cm"],
      correct_index: 0,
      explanation:
        "The diagonal through a cuboid uses all three dimensions: d^2 = 6^2 + 4^2 + 3^2 = 36 + 16 + 9 = 61, so d = square root of 61, approximately 7.8 cm (1 d.p.).",
      hints: [
        "This is a 3D shape, so the diagonal depends on all three dimensions, not just two of them.",
        "Square all three dimensions, add them together, then take the square root of the total.",
      ],
      misconceptions: [
        "",
        "This only uses the length and width (square root of 6^2 + 4^2, about 7.2), which finds the diagonal of the base rectangle, not the full 3D diagonal through the cuboid.",
        "This only uses the width and height (square root of 4^2 + 3^2, which is 5), missing the length dimension entirely.",
        "This adds the three side lengths directly (6 + 4 + 3 = 13) instead of squaring them, adding, and taking the square root.",
      ],
    },
    ```
    Re-derived clean: 6 squared plus 4 squared plus 3 squared equals 36 plus 16 plus 9 equals 61; the square root of 61 is approximately 7.8102, which rounds to 7.8 cm (1 d.p.). All three distractors are genuinely distinct, plausible sub-skill errors (a 2D face diagonal using the wrong two dimensions, or a naive linear sum), not near-duplicates of each other.
  - Risk: None at authoring stage. Higher-tier-only content, spec-cited live this run, re-derived clean, and gives the topic its first "stretch" item alongside its first genuine 3D question.

- [ ] **F3 · curriculum (EPIC 2 slice) · `eng_poetry`'s second command-word item: enjambment** (size: S)
  - The win: `automation/backlog.md` EPIC 2 names `eng_poetry` as one of the remaining topics still on a single command-word item (its existing one, from 2026-08-20, covers caesura with an "Explain" command word). Re-reading the topic's pool this run confirmed it: 4 factual-recall questions plus the one caesura command-word item, no second one yet. Enjambment is the natural pairing (the opposite concept: continuation with no pause, versus caesura's mid-line pause), a genuinely distinct AQA 8700 AO2 skill (analysing the effect of structural choices), not a near-duplicate of the existing item.
  - Change: seed this `SeedQuestion` into `eng_poetry`:
    ```ts
    {
      topic_tag: "eng_poetry",
      subject: "english",
      tier: 5,
      key_stage: 4,
      kind: "mastery",
      prompt:
        "In the line 'The waves crashed on / the empty shore', the sentence runs on from the first line to the second with no punctuation at the line break. Identify this technique and its typical effect.",
      options: [
        "Enjambment, it creates a flowing, continuous movement that mirrors the sense of the words running on.",
        "Caesura, it creates a sudden pause in the middle of a line.",
        "Alliteration, it repeats the same consonant sound at the start of nearby words.",
        "Personification, it gives human qualities to a non-human thing.",
      ],
      correct_index: 0,
      explanation:
        "Enjambment is when a sentence or phrase continues past the end of a line with no punctuation to stop it. This creates a flowing, continuous movement, often mirroring the sense of movement or continuation in the words themselves.",
      hints: [
        "Look at whether there is any punctuation at the end of the first line. If there isn't, and the sentence keeps going, that has its own specific name.",
        "This is the opposite of a caesura (a pause mid-line): here there is no pause at all where you might expect one, at the end of the line.",
      ],
      misconceptions: [
        "",
        "A caesura is a pause, usually created by punctuation in the middle of a line, not the technique of running a sentence over a line break with no punctuation.",
        "Alliteration is about repeated consonant sounds at the start of words, not about how lines and sentences relate to each other.",
        "Personification gives human qualities to something non-human; this example is about line structure, not description.",
      ],
    },
    ```
  - Risk: None at authoring stage. Standard, unambiguous GCSE English literary terminology, no quantitative derivation needed, distractors are genuinely different named techniques rather than shades of the same one.

- [ ] **F4 · growth (conversion) · add a concrete cost-comparison to `/pricing`, the exact pattern several real competitors already use to make their price look obviously worth it** (size: S)
  - The win: this run's competitor scan (`mcp__exa__web_search_exa`, per the Sunday growth lane) found that comparable UK GCSE/homeschool platforms (ReviseBites, Cognito) lead their pricing pages with an explicit "private tutor cost versus us" comparison (for example "25 to 50 pounds an hour private tutor" versus "AI learning engine from 9.99 pounds a month", with a testimonial citing a real family's monthly tutor spend). Edway's own homepage already states the real differentiator ("Marketplace tutor fallback: verified human tutors dispatched within 15 minutes"), and `/pricing` already lists tutor sessions as a feature line, but never frames the price against what a family would otherwise pay for equivalent private tutoring across 3 subjects, so a visitor has to do that maths themselves. A one-line addition making the value obvious in 5 seconds (the exact "credibility and conversion" bar this lane is meant to check) is cheap and concrete.
  - Change: `src/app/(marketing)/pricing/page.tsx`, near the 49 pounds a month "Edway Complete" card: add one short line, for example "Less than one hour of private tutoring, for all three core subjects, every month" (a defensible, non-specific claim, unlike a fabricated named testimonial with a specific outcome, which would repeat the exact "Honesty over hype" failure mode 2026-09-12's B3 already flagged and fixed for the newsletter count).
  - Risk: Low. Copy-only; keep the comparison qualitative and directional rather than a specific number that could go stale or be second-guessed (do not hardcode a specific market tutoring rate, since Edway has no authority over that figure).

- [ ] **F5 · growth (builds directly on B2) · once `twitter:image` is fixed, consider giving high-traffic secondary pages their own dynamic OG image via the same `next/og` mechanism, not just the sitewide default** (size: M)
  - The win: `src/app/opengraph-image.tsx` proves the pattern already works well (a real, on-brand, code-generated 1200x630 image, no external asset needed). Right now every page (`/pricing`, `/for-parents`, `/local-authorities`, `/safety`) shares the exact same generic "GCSE-ready at 14" image when shared. A parent sharing the `/pricing` page in a homeschooling Facebook group, or a Local Authority officer forwarding `/local-authorities`, would get a noticeably more relevant, more credible-looking preview if the shared image reflected that specific page (for example `/pricing` shows the price, `/local-authorities` shows the compliance framing), a low-cost, high-polish differentiator most competitor sites checked this run do not do.
  - Change: add per-route `opengraph-image.tsx` files (Next's file convention supports this per-segment) for the 3 or 4 highest-value marketing pages, reusing the existing `ImageResponse` JSX pattern with a page-specific headline swapped in.
  - Risk: Low to medium. Each additional edge-rendered image route has a small render-time cost on first share or crawl (cached after); keep the count small (3 to 4 pages) rather than one per route.

- [ ] **F6 · stack hygiene (routine) · clear the small `npm outdated` batch and explicitly log the two new major-version jumps as deliberate holds** (size: S)
  - The win: `autoprefixer` 10.5.6 to 10.6.0, `posthog-js` 1.430.2 to 1.430.3, and `tailwind-merge` 3.6.0 to 3.7.0 are all safe, in-range, non-breaking bumps (`npm audit` stays at 0 vulnerabilities). Separately, `typescript` (5.9 to 7.0) and `vitest` (4 to 5) have not been flagged as majors before in `automation/backlog.md` EPIC 7, worth naming explicitly now as deliberate holds (alongside the existing `next`/`eslint` holds) so a future run doesn't waste a cycle re-discovering them, and so any future decision to tackle either major has a clear "since when" marker.
  - Change: `npm install autoprefixer@latest posthog-js@latest tailwind-merge@latest`, then the standard gate (type-check, tests, lint, build). Add one line to EPIC 7 in `automation/backlog.md` naming `typescript@7` and `vitest@5` as new, not-yet-attempted major holds (both are significant migrations, Vitest 5 in particular tends to change config shape, so scope them as their own future slice, not bundled into this routine patch bump).
  - Risk: Very low for the patch bump itself; the majors are explicitly not part of this change.

---

## Summary
- Bugs: 5 (0 Critical, 1 High, 2 Medium, 2 Low). Features: 6.
- Top 3 to ship: **B1** (a real, Sentry-captured, reproducible-4x React hydration error on the single highest-traffic page, homepage-only, re-opening the previously-retired EPIC 20 with fresh evidence), **B2** (every single page's `twitter:image` points at a genuine live 404 while a perfectly good dynamic OG image already exists and is one metadata deletion away from being used correctly on X/Twitter too), **B3** (a Local Authority-facing compliance document literally reads "Grade Grade 4-5" for any child whose latest evaluation came from a mock, plus the same root cause live in the parent-facing exam-decision card).

**Curriculum correctness spot-check (standing EPIC 1 ask):** re-derived the full live `maths_quadratics` mastery pool (3 questions: factorising, expanding a binomial square, solving `x^2 = 16`) end-to-end as Ivy, including re-confirming 2026-09-08's B1 (`classifyOptions` false "half right") fix is still correctly firing on both the true-negative (`x^2 - 8x + 16` wrong-answer path) and true-positive (`x = 4` for `x^2 = 16`) cases live; re-confirmed the "Your turn" active-recall settle (F4, 2026-09-09) fires correctly on a correct tap. Also re-derived the new `maths_quadratics` stretch question (quadratic formula, F3 2026-09-12) end-to-end live and confirmed it is mathematically correct (only its notation is flagged, B5 above). No new instance of the shallow-regex/derived-visual correctness bug class (EPIC 1) found this run.

**Standing max-depth child pass (Ivy):** completed a full `maths_quadratics` mastery-to-certificate-to-brain-stretch run (3 out of 3 correct after one deliberate wrong answer, hint escalation, the See-it walkthrough stepped through to its "Your turn" active-recall task, "Explain it my way" checker-gated explanation, "Topic mastered!" celebration, and the brain-stretch bonus flow through to its own distinct "Wow, Ivy, you nailed the stretch!" celebration) and a `sci_energy` mcq practice set (correct and wrong on both questions, calm generic wrong copy, no red, shake, or buzzer observed in any wrong-answer state this run). Re-verified the interleaved warm-up (fractions addition, correct-answer explanation). Confirmed the `Celebration` fx component (used for every regular correct answer and, at `big=true`, mastery) and the separate `ConfettiBurst` (canvas-confetti, fires once on first reaching `mastered`) are each already well-scoped, reduced-motion-safe, and non-tracking; no gap found here worth a new finding.

**Parent/tutor/admin journeys (all run this session):** generated a genuinely fresh portfolio for Ivy (Q3 2026, hash starting `3fca8c13`), confirmed its "20/34" and "8/14 Maths" figures agree exactly with the dashboard and child-profile GCSE-only figures (EPIC 22's fix holding consistently across all three surfaces plus, newly checked this run, the mock-unlock gate itself: Ivy's `/learn/mock` correctly reads "8/10" Maths certified, matching the portfolio, not the old all-band blend); opened the public `/verify-portfolio` hash-check page. Tutor (read-only): sessions list correctly empty, availability toggle present. Admin (read-only): Overview, Finance, Escalations all clean, zero console errors.

**Growth/SEO (Sunday deep-dive):** `robots.txt`/`sitemap.xml` both clean (19 URLs, all real marketing pages present, `Disallow` list correctly covers every private route group); `/pricing` already carries `Product`/`AggregateOffer`/`BreadcrumbList` JSON-LD; a `site:edway.uk` search confirms the homepage is indexed and its content is being surfaced correctly. A competitor scan (ReviseBites, Cognito, a GCSEPod/Medly AI comparison article, Trellis, Dolly) found the concrete, borrowable pattern behind F4 above (an explicit cost-versus-private-tutoring comparison on the pricing page), and confirmed Edway's existing "Gallery coming soon" honest-empty-state and dynamic OG image are already ahead of some of these competitors' own social-share presentation, once B2 is fixed.

Teardown: signed out of every role (`fetch('/logout', {method:'POST'})`, confirmed 200 each time) between switches and at the end; closed the Playwright page.
