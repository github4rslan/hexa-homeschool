# Scout findings — 2026-08-03

DECISION: all

<!--
  all           → build EVERY item below (default)
  B1, F2, F5    → build ONLY these IDs
  skip          → build nothing tonight
-->

Runtime: playwright  ·  Base URL: https://edway.uk  ·  Desktop innerWidth 1280, Mobile 390 (both confirmed)  ·  Pass run 2026-08-04 ~14:35 UTC (health `time` + dashboard "TUESDAY 4 AUGUST"; filename kept at the owner-named 2026-08-03).

## Coverage (all clean unless noted)
- **Marketing** (/, /pricing, /how-it-works, /safety, /for-parents) — single `<h1>`, ZERO console errors, NO x-overflow in EITHER viewport (desktop scrollW 1270 ≤ 1280; mobile 380 ≤ 390). `/api/health` → 200 `{ok, db:"up"}`.
- **Parent** (dashboard, child profile, /schedule, /portfolio, /settings) — clean, zero console errors, no overflow (desktop + mobile). 3 children (Sam Smoke 12, Ivy Test 10 [SEND: dyslexia], Sam Test 12), active = Ivy. `WEEK OF 3 AUGUST 2026` formats correctly.
- **F5 re-verify (shipped since last run):** child profile → **Work evidence** section present with the topic dropdown (General / Living Things / Algebraic Expressions / Negative Numbers & Powers / Fractions & Measures / Arithmetic) + "Add work", "No work uploaded yet." Sits beside the F1 "A note for Ivy" textarea and the 5 mastery certificates. All render, no overflow at 390. (Did not upload a real photo — no junk media.)
- **Child mode — English "Reading & Spelling" (`eng_ks2_reading`) end-to-end as Ivy, PASS:** mood check-in tapped "Tough day" → **F4** warm ack "Thanks for sharing. 💛 Let's keep it light today — one quest is plenty." Explainer StepReveal (be-cause worked example) + narration control; practice wrong#1 = calm "Take a slow breath. Ivy, not quite yet — have another go." + **F2** "Why isn't that right?" + nudge hint + "2 tries left", **no red/buzzer**; correct = star burst "Brilliant! 🎉"; "See it" unlocked; practice 2/2 → mastery 3/3 (Learn→Practise→Mastery phase bar lit per stage) → "Topic mastered! 3 of 3 · certified" + Save certificate + See-it-on-journey. **F6** "Start here · today's plan" pill on English. Zero console errors throughout.
- **Child extras:** warm-up (spaced-rep review) played "6 × 9" → "Spot on! Six groups of 9 make 54."; mock exam honestly gated ("English mock locked · you have 1/10" — count ticked up live after the new cert). No overflow.
- **Admin** (overview, finance, escalations) — READ-ONLY, clean, zero console errors, no overflow. 12 parents / 13 children / 2 lessons this week / 0 open escalations; MRR £148. Recent-activity titles humanised ("Reading & Spelling · 100%"). ("Feature flags" nav → `/admin/settings`, correct — a direct `/admin/feature-flags` guess 404s, not a real link.)
- **Tutor** — READ-ONLY, F9 availability toggle present, empty queue ("No sessions assigned yet") → data-silo holds.
- **Teardown:** logged out of parent, admin and tutor (`/logout` POST → 200 each); browser closed.

Static: **type-check GREEN, lint GREEN.** `npm audit` = **2 high, both dev/build-only** (`brace-expansion` via eslint/glob/minimatch; `fast-uri` via `@sentry/webpack-plugin → webpack → ajv` — a *build* dep, not the prod runtime). No prod-runtime CVE. Stack current (Next 15.5.x / React 19 / Tailwind 4.3).

---

## Bugs (Critical → Low)

- [x] **B1 · Low-Med · "Current standing" says "Not yet assessed — run the diagnostic" for a child who is clearly progressing** — `src/app/(dashboard)/dashboard/children/[childId]/page.tsx:135-170`
  - Repro / evidence: on Ivy's profile the **Current standing** card shows *Mathematics / English / Science: "Not yet assessed — run the diagnostic."* for all three subjects, while the SAME card footer reads "**5 topics certified so far**" and the **Working level** card below reads "Mathematics — **Working at GCSE level**". A parent sees a direct contradiction: three "not assessed" chips next to five certificates and a GCSE working level. Screenshot state captured in-session (desktop 1280).
  - Root cause: `standings` come only from `latestEvaluationsBySubject` (diagnostic/mock `evaluation_records`). A child who progressed via **lessons** (certified topics + a computed working band via `childCurrentBands`, both already fetched on this page as `certified` and `bands`) has no per-subject evaluation row, so every subject falls to the "Not yet assessed" branch.
  - Fix: in the per-subject standing cell, when there's no evaluation but the subject has a working band / certified topics, show that instead of the diagnostic prompt — e.g. "Working at {band} level · {n} certified" (data is already in scope: `bands`, `countCertified`). Keep "Not yet assessed — run the diagnostic" only when the subject truly has zero evaluation AND zero competence progress.
  - Risk: presentation-only; don't change the exam-decision inputs (still evaluation-driven). Keep copy within the calm/honest bar.

## Features & upgrades (High value → Low)

- [x] **F1 · feature · English derived question figures (extend the visual chain into English)** (size: M) — *serves Ivy's today's-plan subject; most build-ready (proven recipe)*
  - The win: maths (`deriveMathVisual`) and science (`deriveScienceVisual`, shipped) render precise, honestly-described SVG figures; **English questions render an empty `<figure>`** (confirmed live on "plural of box" — `figure` node with no child). Add deterministic English diagrams for the common KS2 shapes: base-word + suffix tiles ("happy + ness → happiness"), plural-rule chips (box → box**es**), and syllable/phoneme split ("be · cause"). Consistent, accessible (real `alt`), cached-free.
  - Change: new pure `src/lib/child/english-visual.ts` (`deriveEnglishVisual(topicTag, prompt)`, keyword/topic gated, returns `null` when unsure) + SVG renderer `src/components/child/english-visual.tsx`, wired in `practice-player.tsx` alongside `mathVisual`/`scienceVisual` (English branch before the AI/decorative fallback). Unit-test the deriver like the maths/science ones.
  - Risk: English prompts vary — only render when keyword-confident, else fall through (never a wrong diagram). Mirror `science-visual.ts` structure exactly.

- [x] **F2 · feature/accessibility · SEND-aware reading-support defaults** (size: S/M) — *mission-core; Ivy is flagged dyslexic yet starts with every aid OFF*
  - The win: `ChildDoc.send_indicators` stores "dyslexia" for Ivy, but the reading-support suite (`reading_font`, `text_scale`, `reading_ruler` — `lib/child/reading-supports.ts`) defaults **all OFF** with **no link to the SEND flag**. A dyslexic child gets the plain font until someone manually flips it in My-stuff. Auto-applying a sensible default (dyslexia-friendly font on, optionally the ruler) for a child whose `send_indicators` includes "dyslexia" is a real, low-effort accessibility win that makes the SEND promise visible.
  - Change: when the stored `reading_font` is *undefined* (never set) AND `send_indicators` includes a dyslexia marker, treat the effective default as font-on in `readingSupportsFromChild` (or resolve at the child-doc read in `repo.ts`); child/parent can still toggle it off in My-stuff (an explicit `false` always wins over the SEND default). Keep it child-controlled thereafter (Children's Code) — this only changes the *initial* default, never locks it.
  - Risk: don't override an explicit child choice; distinguish "never set" from "set to false". Purely a default; no tracking/profiling.

- [x] **F3 · feature · Parent audio encouragement note (voice note) on the child hub** (size: M/L) — *extends the shipped F1 text note; especially strong for a dyslexic learner*
  - The win: the F1 text "note for Ivy" is lovely but a *dyslexic* child reads it with effort. Let the parent record a short (≤20s) spoken note that plays as a warm card at the top of the hub ("Hi Ivy, so proud of you — one quest at a time 💛"). Hearing a parent's voice is a high-payoff, calm, human moment and is more accessible than text for pre-readers/SEND.
  - Change: on the child profile add a MediaRecorder capture (parent side only) → upload via the existing `/api/media/sign` + `/api/media` (ownership re-checked, folder derived server-side) → store the audio URL on the child doc (`audio_note_url`, mirror `setChildNote`). Render a play control in `components/child/parent-note-card.tsx` (reuse the existing audio element pattern from narration). No AI, no Checker (human voice), no `(child)` analytics.
  - Risk: recording is a *parent*-dashboard action, never in `(child)`; cap duration/size; let the parent delete/replace; degrade cleanly if Cloudinary unset (hide the recorder, keep the text note).

- [x] **F4 · feature/stack · Web Push milestone notifications for parents (reuse the hand-rolled SW)** (size: L) — *new capability; PWA infra already exists*
  - The win: milestone signals (mastery, inactivity reminder, remediation handoff) go out today as email + optional SMS only. The installable PWA already ships a hand-rolled `public/sw.js`. Adding Web Push means a parent who installed Edway gets an instant, free, on-device "Ivy just mastered Reading & Spelling 🎉" — the most immediate channel, no per-message cost.
  - Change: add a `push` + `notificationclick` handler to `public/sw.js`; a VAPID keypair (env-gated, `WEB_PUSH_*` — unset = feature fully off, per the graceful-degradation pattern); a `PushSubscription` store on the parent doc; a subscribe control on the dashboard (consent-gated, parents-only); emit from `lib/notify/parent-event.ts` alongside the existing email/SMS, respecting `event_notifications_opt_out`. **Never** register push in `(child)`.
  - Risk: browser-permission UX; keep it strictly parents-only and opt-in; VAPID unset must be a silent no-op; CSP already allows `worker-src 'self'`.

- [ ] **F5 · feature · Child "Today I learned" one-tap reflection → parent feed** (size: M)
  - The win: after finishing today's quests, offer the child a calm, optional one-tap reflection ("What felt good today?" → a few friendly chips: "I got faster", "It was tricky but I did it", "I liked the pictures"). It closes the loop warmly for the child and surfaces a human line on the parent's activity feed — richer than a bare "completed a lesson".
  - Change: chip-selection only (no free text ⇒ no distress-gate surface needed) on the "all done" state in `src/app/(child)/learn/page.tsx`; store a small `daily_reflection` (deterministic enum, date-keyed, one per day) via a repo function (ownership-checked); render on the dashboard activity feed (`listActivityFeed`). No AI, no profiling, out of `(child)` analytics.
  - Risk: keep it optional and skippable (never a gate to finishing); chips only, human-authored copy; one write per child per day.

- [ ] **F6 · security/stack · Clear the 2 dev/build-only audit highs + refresh Next/React patch line** (size: S) — *low priority, honest*
  - The win: `npm audit` shows 2 highs — `brace-expansion` (eslint/glob/minimatch toolchain) and `fast-uri` (`@sentry/webpack-plugin → webpack → ajv`, a *build* dep). Neither is in the prod runtime, but both now report "fix available via `npm audit fix`" (non-force). Worth a careful pass to zero the audit + take the latest Next 15.5 / React 19 patch.
  - Change: try `npm audit fix` (NON-force — never `--force`, which downgrades next→9); if `fast-uri`/`brace-expansion` resolve without breaking eslint, keep it; bump Next/React to the latest patch and re-run the full gate (type-check + tests + lint + build). Keep the existing prod-CVE `overrides` intact.
  - Risk: `audit fix` can touch lockfile transitively — verify lint + build stay green; don't pull eslint@10 (breaking major).

---

## Summary
- Bugs: **1** (0 critical / 0 high / 0 med / 1 low-med). Across marketing + parent + child + admin + tutor, desktop AND mobile, the live pass found **zero console errors, zero overflow, and every child flow PASS** — an exceptionally clean, mature build.
- Features: **6.**
- Top 3 to ship: **F1** English derived question figures (proven pattern, serves the active subject, fills the last empty-`<figure>` subject); **F2** SEND-aware reading defaults (turns Ivy's dyslexia flag into a real accommodation); **F3** parent audio encouragement note (human warmth + accessibility, extends the shipped text note).
