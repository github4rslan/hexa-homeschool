DECISION: all

# Scout findings, 2026-09-17

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           -> build EVERY item below (default)
    B1, F2, F5    -> build ONLY these IDs
    skip          -> build nothing tonight
  Leave it as `all` and do nothing to run fully autonomous.
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright (MCP tools only). Base URL: https://edway.uk

Day-of-week focus: **Thursday, end-to-end journeys deep-dive** (B-journeys,
extra depth and edge cases), plus the standing max-depth child pass and
every-day lanes (bugs, curriculum, journeys for every persona) regardless of
focus.

Confirmed UTC date via `date -u`: 2026-09-17 (matches the system reminder this
run, no drift).

Desktop pass confirmed at `innerWidth` **1280** (`browser_resize` 1280x800).
Mobile pass confirmed at `innerWidth` **390**, `scrollWidth` **380** (no
overflow) on `/` and `/pricing`, scrolled to the bottom of both via
`window.scrollTo` (the documented `document.scrollingElement.scrollTop`
no-op quirk from a prior run's memory note was re-confirmed: it silently
no-ops on this site, `window.scrollTo`/`window.scrollY` is the reliable
check).

Static checks: `npm run type-check` GREEN, `npm run lint` GREEN, `npm audit`
0 vulnerabilities. `npm outdated` shows a small in-range batch
(`@sentry/nextjs` 10.74.0 to 10.75.0, `@types/node` 22.20.2 to 22.20.3,
`@upstash/ratelimit` 2.0.8 to 2.1.0, `autoprefixer` 10.6.0 to 10.6.1,
`lucide-react` 1.45.0 to 1.47.0, `motion` 13.2.0 to 13.4.0, `posthog-js`
1.430.3 to 1.433.7) plus the existing deliberate major holds tracked in
`automation/backlog.md` EPIC 7 (`next`/`eslint`/`@next/bundle-analyzer`,
`typescript`, `vitest`).

---

## Bugs (Critical to Low)

- [ ] **B1 · High (re-opens EPIC 24, the fix did not hold) · the homepage still throws the same React hydration-mismatch error (minified React #418) on every fresh load, AFTER the 2026-09-13/14 fix shipped and deployed, and the fix's own root-cause theory is now disproven** : `src/components/fx/reduced-motion-provider.tsx`, `src/components/fx/use-hydration-safe-reduced-motion.ts`
  - Repro / evidence: fresh `browser_navigate('https://edway.uk/')` reproduced `Minified React error #418` on the FIRST console read this run, then again on an immediate second fresh navigate (2/2). Confirmed via `browser_network_requests` that Sentry still captures it (4 `POST .../envelope/` calls per pageview, same pattern as the 2026-09-13 report). Confirmed the shipped fix (`bffcfee`, "Fix: B1 live regression, hydration mismatch persisted sitewide via MotionConfig") is genuinely on `main` and deployed (`git merge-base --is-ancestor bffcfee origin/main` succeeds; `/pricing`, `/how-it-works`, `/safety`, `/for-parents` all still clean, confirming the error stays homepage-scoped, not a general regression). Critically: **`window.matchMedia('(prefers-reduced-motion: reduce)').matches` returns `false`** in the exact same browser session that reproduces the error, both on this run's default Playwright profile. The shipped fix's entire theory (`ReducedMotionProvider` resolving a real `prefers-reduced-motion: reduce` preference differently between server and the hydrating client render) requires that media query to actually be `true` for a real visitor to trigger the mismatch — it is not true here, yet the identical error still fires. This is strong evidence the shipped fix, while a real and worthwhile hardening (it removes framer-motion's own render-phase detection either way), was not the actual root cause of the error a plain, no-special-preference visitor hits on the homepage.
  - Root cause: not re-established this run. Ruled OUT: the reduced-motion hydration theory from the 2026-09-13/14 fix (see evidence above — the exact trigger condition it targets is false in a session that still reproduces the bug). Not yet checked this run: a genuine non-minified local repro (`next build && next start`, off the record since `npm run dev`/`next start` are against `CLAUDE.md`'s "never run locally" rule for iterative work, but a ONE-OFF non-minified local build purely to read the real hydration diff, per the original B1 finding's own step 1, still appears not to have been done in either the 2026-09-13 or this run — the fix was shipped on a plausible theory, not a confirmed one).
  - Fix: do the local non-minified repro this time before a third guess-and-ship attempt (`next build && next start` once, read the real component-level hydration diff React prints in dev mode, kill the server immediately after — this is a one-off diagnostic action, not "running the app locally" as a workflow). Cross-reference against every homepage-only component again (`Hero`, `TrustBar`, `StatsStrip`, `Problem`, `Solution`, `JourneyPreview`, `AgentsPreview`, `FeatureGrid`, `SafetyPreview`, `Testimonials`, `CompliancePreview`, `CTA`) for anything that reads `window`/`navigator`/a cookie/locale-dependent formatting during the render that hydrates, not just inside an effect. `Testimonials`' `/api/testimonials` fetch-then-`setState` pattern was re-read this run and looks hydration-safe (empty array on both server and the render that hydrates), but is worth a second look with the real error text in hand. `footer.tsx`'s `{new Date().getFullYear()}` is sitewide (not homepage-scoped) so it's an unlikely match but also worth ruling out explicitly since it's the one clearly non-deterministic value found homepage-adjacent.
  - Risk: Low to investigate (one local build, read-only). The eventual fix's risk depends on what's found; treat the previous "removed framer-motion's render-phase detection" change as still worth keeping (it is a real hardening independent of whether it was the root cause), so the next fix should be additive, not a revert.

- [x] **B2 · Medium (compliance/parent-facing copy correctness, 6th site of a known class) · the "Grade Grade" duplicated-word bug (previously fixed at 5 display sites, 2026-09-13) has a 6th, un-fixed call site: the parent-facing "Why" reasoning text on `/schedule`** : `src/lib/db/repo.ts:3867-3884` (`scheduleItemReason`), read from `src/lib/db/repo.ts:3954` (`gradeBySubject`, sourced from `latestEvaluationsBySubject`'s raw `doc?.model_predicted_grade`)
  - DONE (2026-09-19, Mechanic). Note: the finding's own suggested diff (keep the hardcoded lowercase "grade " word in the template AND wrap the value in `formatWorkingGrade`) would NOT actually have fixed this: `formatWorkingGrade` always returns the full "Grade X" string (word included), so that combination still reads "predicted grade Grade 4-5". The correct fix removes the redundant hardcoded "grade" word from the sentence and lets `formatWorkingGrade` supply the whole "Grade X" phrase, matching the pattern already used at the other 5 sites (e.g. `exam-decision.ts`'s "Working at ${formatWorkingGrade(...)}"). Also grepped the whole codebase for other unformatted grade-interpolation sites per the finding's request; the only other candidates found (`dashboard/children/[childId]/page.tsx`'s "working level"/"boundary estimate" lines, `portfolio-generator.tsx`'s "Grade evidence" metric, `assessment-narrative.ts`'s `gradeSentence`) were traced to their source types and confirmed safe (either no hardcoded "grade" word adjacent, or the underlying value is always a bare number, e.g. mock boundary grades are written as bare digits like "5" per `mock-paper.ts`). Exported `scheduleItemReason` (was module-private) to unit test it directly; added 3 tests to `tests/grade-band.test.ts`.
  - Repro / evidence: live-approved Ivy's week this run (`/schedule?child=...`, "Approve this week") and the Monday/Thursday Maths items both render: **"Why: The diagnostic predicted grade Grade 4–5 in Maths; Statistics & Probability is the next topic on the GCSE path."** — "grade Grade 4–5", the exact duplicated-word shape the 2026-09-13 B3 fix addressed at `cnis/page.tsx`, `exam-decision.ts`, `mock/page.tsx`, `mock-result-view.tsx` and `portfolio-generator.tsx`, via a new `formatWorkingGrade()` helper in `lib/data/diagnostic.ts` that strips any pre-existing "Grade " prefix. `scheduleItemReason()` was never updated to use it: `grep` confirms zero references to `formatWorkingGrade` anywhere in `repo.ts`, and the function still directly interpolates the raw string into a template that already hardcodes the word "grade": `` `The diagnostic predicted grade ${input.predictedGrade} in ${subject}; ...` ``. `input.predictedGrade` traces to `gradeBySubject.get(subject)`, itself `standings.map((s) => [s.subject, s.grade])` where `s.grade` is `doc?.model_predicted_grade ?? null` — the same raw, un-formatted field, read directly with no `formatWorkingGrade()` pass, unlike the 5 already-fixed sites. Confirmed the correctly-formatted portfolio table on the SAME page load (`/portfolio`, generated fresh this run for Ivy) shows the bare "Grade 4–5" with no duplication, proving the fix genuinely works elsewhere and this really is a missed 6th site, not a regression of the original fix.
  - Root cause: `scheduleItemReason()` in `repo.ts` reads `model_predicted_grade` directly rather than through `formatWorkingGrade()`, so any mock-sourced grade (which stores the full "Grade X–Y" string, per `tierToGrade()` in `lib/data/diagnostic.ts`) doubles the word when the function's own hardcoded "grade" prefix is added.
  - Fix: import `formatWorkingGrade` from `@/lib/data/diagnostic` into `repo.ts` and wrap the interpolation: `` `The diagnostic predicted grade ${formatWorkingGrade(input.predictedGrade) ?? input.predictedGrade} in ${subject}; ...` ``. Add one unit test asserting `scheduleItemReason()` never produces the substring "Grade Grade" for a mock-sourced grade input, mirroring the existing `formatWorkingGrade` test coverage.
  - Risk: Very low. Single function, already has an established, tested fix pattern to copy from 5 other sites; no data change, display-only.

- [x] **B3 · High (security/abuse, missing rate limit) · the "Email portfolio to your Local Authority" server action has NO rate limiting at all, unlike every other outbound-effect route in the app** : `src/app/(dashboard)/portfolio/actions.ts:17-52` (`emailPortfolio`)
  - DONE (2026-09-19, Mechanic). Added `rateLimit(\`portfolio-email:${parentId}\`, 5, 60_000)` right after the session check, returning the exact 429-shaped `{ ok: false, reason: "Too many requests. Please wait a moment and try again." }` used by the sibling `api/portfolio/route.ts`. No new pure logic (the shared `lib/rate-limit.ts` already has its own Vitest coverage), so no new test added, just the wiring.
  - Repro / evidence: read `emailPortfolio()` in full. It requires a session (`currentParentId()`) and validates the email is syntactically well-formed, then calls `sendEmail()` (`lib/email/send.ts`) directly with no rate-limit check anywhere in between. Contrast with the sibling portfolio-generation route, `src/app/api/portfolio/route.ts:146`, which explicitly rate-limits at `rateLimit(\`portfolio:${authParentId}\`, 20, 60_000)` with a comment explaining exactly this class of risk ("An unauthenticated caller could... burn CPU... A per-parent rate limit bounds the compute"). `lib/email/send.ts`'s own comment confirms Brevo (the live provider) "sends to ANY recipient once a sender is verified" — there is no allow-list restricting the destination to a plausible `.gov.uk`/council address, and no cap on how many times one authenticated parent can trigger a send. Any signed-in parent (including the lowest, free "Diagnostic" trial tier — confirmed live via `/admin/users` this run: 12 of 14 real accounts are on "Diagnostic/Trialing") could script repeated calls to send unlimited Edway-branded transactional email, from Edway's own verified sender, to any third-party address of their choosing — a spam/phishing-adjacent abuse vector against Edway's Brevo sender reputation and deliverability, not just a compute-cost issue like the sibling route's own stated concern.
  - Root cause: the rate-limiting sweep that covers `/api/tutor`, `/api/tts`, `/api/stt` (per `CLAUDE.md`'s own "Gotchas" section) and `/api/portfolio`'s generation POST was never extended to this Server Action, likely because it isn't under `/api/` and so wasn't caught by whatever review pass added the others.
  - Fix: add a `rateLimit()` call at the top of `emailPortfolio()`, mirroring the existing pattern, e.g. `rateLimit(\`portfolio-email:${parentId}\`, 5, 60_000)` (5/minute is generous for genuine one-at-a-time LA sharing, tight enough to kill scripted abuse), returning `{ ok: false, reason: "Too many requests. Please wait a moment and try again." }` on the limited branch, exactly like `api/portfolio/route.ts`'s existing 429 shape.
  - Risk: Very low. Additive-only change using an already-proven helper (`lib/rate-limit.ts`, already imported elsewhere), no behaviour change for a genuine parent sharing one portfolio with one LA officer.

## Features & upgrades (High value to Low)

- [x] **F1 · curriculum (north-star, EPIC 23 slice) · author a GCSE-tier stretch question for `sci_electricity`: combining series-circuit resistance with Ohm's law** (size: S)
  - DONE (2026-09-19, Mechanic). Transcribed verbatim into `curriculum.seed.ts`, added 3 unit tests to `tests/curriculum-batch2.test.ts` (well-formedness, no near-duplicate stretch item, and the arithmetic itself). Ran `npm run seed` after the gate to upsert it into the live DB.
  - The win: `automation/backlog.md` EPIC 23 tracks that GCSE-tier "stretch" content is almost entirely missing from the bank and asks for 1 to 2 more topics per run, across different subjects (Maths currently has 3 of the bank's 4 real stretch items). `sci_electricity` has two existing "mastery" command-word items (`V = I × R`, `P = V × I`, both single-step) but zero stretch items and zero coverage of combining resistors, even though it's an explicit Higher-tier spec point. Spec-verified live this run via AQA's own specification page (`aqa.org.uk/subjects/science/gcse/science-8464/specification/physics-subject-content`, section 6.2.2 "Series and parallel circuits": "the total resistance of two components is the sum of the resistance of each component... $R_{total} = R_1 + R_2$... Students should be able to: calculate the currents, potential differences and resistances in dc series circuits.")
  - Change: seed this `SeedQuestion` into `curriculum.seed.ts` (or `.extra.ts`) under `sci_electricity`:
    ```ts
    {
      topic_tag: "sci_electricity",
      subject: "science",
      tier: 5,
      key_stage: 4,
      kind: "stretch",
      prompt:
        "Two resistors of 4 Ω and 6 Ω are connected in series to a 20 V battery. Calculate the current flowing through the circuit.",
      options: ["2 A", "5 A", "3.3 A", "200 A"],
      correct_index: 0,
      explanation:
        "For resistors in series, total resistance adds: R_total = 4 Ω + 6 Ω = 10 Ω. Then current = potential difference ÷ resistance: I = V ÷ R = 20 V ÷ 10 Ω = 2 A.",
      hints: [
        "First find the total resistance of the circuit: for resistors in series, add the resistances together.",
        "Once you have the total resistance, use I = V ÷ R with the total resistance, not just one resistor.",
      ],
      misconceptions: [
        "",
        "This uses only the 4 Ω resistor (20 ÷ 4 = 5), ignoring that the second resistor is also in the circuit and adds to the total resistance.",
        "This uses only the 6 Ω resistor (20 ÷ 6 ≈ 3.3), ignoring that the first resistor is also in the circuit and adds to the total resistance.",
        "This multiplies the voltage by the total resistance (20 × 10 = 200) instead of dividing; current is voltage divided by resistance, not voltage times resistance.",
      ],
    },
    ```
    Re-derived clean: 4 + 6 = 10 Ω; 20 ÷ 10 = 2 A. All three distractors are genuinely distinct, plausible sub-skill errors (using only one resistor, or the wrong operation), not near-duplicates.
  - Risk: None at authoring stage. Standard, unambiguous Higher-tier content, spec-cited live this run. Owner approval gate via `DECISION:` still applies before seeding.

- [ ] **F2 · curriculum (north-star, EPIC 23 slice) · author a GCSE-tier stretch question for `eng_punctuation`: correct use of a semicolon to join two independent clauses** (size: S)
  - The win: English currently has ZERO stretch-kind questions anywhere in the bank (all 4 existing stretch items are Maths x3, Science x1), even though top-band (grade 8-9) SPaG accuracy — the exact skill "a semicolon needs an independent clause on both sides" tests — is precisely the kind of stretch content EPIC 23 is asking for. `eng_punctuation`'s own topic summary already names "colons" among its scope; semicolons are the direct sibling skill and the topic has zero coverage of either currently. This is a standard, textbook-unambiguous GCSE English Language AO6 (accurate range of punctuation) skill with exactly one defensible correct answer, not a subjective literary-analysis judgement call, so it stays safely inside the hard authoring rule about ambiguity risk.
  - Change: seed this `SeedQuestion` into `eng_punctuation`:
    ```ts
    {
      topic_tag: "eng_punctuation",
      subject: "english",
      tier: 5,
      key_stage: 4,
      kind: "stretch",
      prompt:
        "Identify the sentence that correctly uses a semicolon to join two independent clauses.",
      options: [
        "I love writing; it lets me explore new worlds.",
        "I love writing; because it lets me explore new worlds.",
        "I love writing, it lets me explore new worlds.",
        "I love; writing lets me explore new worlds.",
      ],
      correct_index: 0,
      explanation:
        "A semicolon can join two independent clauses (each a complete sentence on its own) without a conjunction. 'I love writing' and 'it lets me explore new worlds' are both independent clauses, so the first sentence is correct.",
      hints: [
        "A semicolon needs a COMPLETE sentence (an independent clause) on BOTH sides of it.",
        "Check each side of the semicolon: can it stand alone as its own sentence? If not, the semicolon is used incorrectly.",
      ],
      misconceptions: [
        "",
        "'Because it lets me explore new worlds' cannot stand alone as a sentence (it's a dependent clause), so a semicolon is the wrong punctuation here.",
        "This uses a comma to join two independent clauses, which is a comma splice; a semicolon (or a full stop, or a conjunction) is needed instead.",
        "This places the semicolon between 'I love' and 'writing lets me explore new worlds', but 'I love' is not a complete sentence on its own, so the semicolon is misplaced.",
      ],
    },
    ```
  - Risk: None at authoring stage. Standard GCSE SPaG rule, no ambiguity, all three distractors are genuinely distinct, real error types.

- [ ] **F3 · curriculum (EPIC 2 slice) · `eng_spelling`'s second command-word item: the "should of" / "should have" error** (size: S)
  - The win: `automation/backlog.md` EPIC 2 names `eng_spelling` as one of the last remaining single-command-word-coverage topics (its existing item uses "Identify" for their/there/they're). "Should of" instead of "should have" is one of the most commonly cited GCSE English writing errors and is completely unambiguous (a modal verb like "should" can never be followed by the preposition "of"). Using "Explain" as the command word (rather than another "Identify") also adds genuine command-word variety to the topic, which is EPIC 2's stated goal.
  - Change: seed this `SeedQuestion` into `eng_spelling`:
    ```ts
    {
      topic_tag: "eng_spelling",
      subject: "english",
      tier: 2,
      key_stage: 4,
      kind: "mastery",
      prompt:
        "'I should of studied harder for the exam.' Explain what is wrong with this sentence.",
      options: [
        "'Of' should be 'have' — 'should have' is the correct form; 'of' is a preposition, not part of the verb.",
        "The sentence is correct as it stands.",
        "'Of' should be 'off' to show the exam is finished.",
        "'Studied' should be spelled 'studyed'.",
      ],
      correct_index: 0,
      explanation:
        "'Should of' is a common mistake caused by how 'should have' sounds when spoken quickly. The correct form is 'should have' (or the contraction 'should've') — 'of' is a preposition and can never follow a modal verb like 'should', 'could' or 'would'.",
      hints: [
        "Say the sentence aloud — 'should of' sounds like 'should have' when spoken quickly, but they are not interchangeable in writing.",
        "'Of' is a preposition (e.g. 'a cup of tea'); it can never follow 'should', 'could' or 'would' in standard written English.",
      ],
      misconceptions: [
        "",
        "This sentence contains the classic 'should of' error and needs correcting, not leaving as is.",
        "'Off' means to separate or complete something; it doesn't fix the grammar issue here at all.",
        "'Studied' is already spelled correctly (past tense of 'study': change the y to i, add -ed); the actual error is 'should of'.",
      ],
    },
    ```
  - Risk: None at authoring stage. Unambiguous, well-known GCSE-relevant SPaG error, distractors are genuinely distinct wrong turns.

- [ ] **F4 · tooling/hygiene (prevents a repeat of today's own near-miss) · `scripts/seed-test-accounts.ts` should detect and warn about extra children beyond its own 2-child fixture** (size: S)
  - The win: this run's dashboard showed THREE children on the SMOKE parent account ("Sam Smoke", "Ivy Test", "Sam Test") where the seed script (`scripts/seed-test-accounts.ts:118-146`) only ever creates TWO ("Sam Test", "Ivy Test"). "Sam Smoke" is a genuine stray — confirmed via `/admin/users` that the account really does have 3 children live — almost certainly created by a prior session exercising the "Add child" flow without cleanup. This run nearly mis-filed it as a fresh finding before tracing it back to the seed script; a future Scout or Mechanic run doing the same live child-count assertions (e.g. "the fixture has exactly 2 children") could silently get confused by it, and it inflates the admin's live "Children: 15" / "Parents: 14" counts with test noise the owner may not expect.
  - Change: at the end of `scripts/seed-test-accounts.ts`, after the idempotent upsert of "Sam Test"/"Ivy Test", add a read-only check: count `children` docs for `parent_id: parentId` and, if the count is more than 2, print a clear warning listing the unexpected names/ids (never delete automatically — leave any cleanup decision to the owner). This makes the drift visible at the next `npm run seed:test-accounts` run instead of silently accumulating.
  - Risk: Very low. Additive, read-only console warning; no behavioural change to the app itself.

- [ ] **F5 · delight (Wednesday lane, standing owner priority) · a warm tap-response animation on the child-mode mood check-in ("How are you feeling today?")** (size: S)
  - The win: live-drove this exact moment as Ivy this run (`/learn`): tapping a mood face currently swaps straight to static text ("Thanks for checking in! 🌟") with no motion on the tapped face itself, one of the few primary child-facing taps that currently has zero micro-interaction (every interaction-type's answer tap already has a settle animation per `automation/memory.md` EPIC 6, but this pre-lesson mood check-in was never in that sweep). A small bounce/scale-up on the tapped emoji, in the same calm, self-contained style as the existing settle-pulse primitives, would give this genuinely warm, low-stakes moment (it's the very first tap of the session) the same polish as everything after it.
  - Change: `src/app/(child)/learn/page.tsx` (or wherever the mood-face buttons live) — reuse the existing settle-pulse `motion`/`m` primitive already used elsewhere (see `components/fx/`) on the tapped button: a brief scale (e.g. 1 → 1.15 → 1) plus the existing "Thanks for checking in!" text swap. Must honour `prefers-reduced-motion` (already the sitewide convention via `ReducedMotionProvider`) and stay purely visual — no new tracking, no timing that blocks the next action.
  - Risk: Very low. Self-contained, reuses an already-proven primitive; keep the bundle cost at zero by reusing rather than adding a new dependency.

- [ ] **F6 · delight (Wednesday lane) · a landing flourish when a drag_drop chip is placed (tap-to-place path), not just the pick-up lift** (size: S)
  - The win: drove the `maths_geometry` drag_drop question end-to-end this run (tap-to-place AND keyboard placement, both PASS functionally). `automation/memory.md` EPIC 6 records a "drag_drop chip pick-up lift" shipping 2026-08-19, but the PLACEMENT moment itself (the chip settling into "Triangle: 3. Activate to remove.") currently just swaps text instantly with no landing motion, an asymmetry against the pick-up side which does animate. A small settle-into-place flourish (e.g. a brief scale/glow matching the eventual correct/wrong colour once checked) on drop/tap-placement would complete the pair and make the whole drag interaction feel considered end-to-end, including the keyboard-activation path which currently has no motion feedback at all beyond the text change.
  - Change: the drag_drop interaction component (`src/components/child/interaction.tsx` or its drag_drop sub-renderer) — on successful placement (both pointer-drop and the tap/keyboard `Enter` fallback), fire a brief scale-in flourish on the now-filled slot, reusing the same primitive as F5 above for consistency. Must fire identically for pointer, tap, and keyboard activation (the keyboard path must not be a second-class citizen — confirmed this run that keyboard placement already works functionally, just with no motion).
  - Risk: Very low. Visual-only, must stay `prefers-reduced-motion`-safe and non-blocking to input, per the standing hard rules.

- [ ] **F7 · feature (north-star, exam-technique literacy) · a command-word glossary tooltip the first time a child meets each GCSE command word in a question** (size: M)
  - The win: real exam papers lose students marks not on subject knowledge but on misreading the command word ("calculate" vs "explain" vs "evaluate" vs "describe" vs "compare" each require a genuinely different kind of answer, and examiner reports consistently cite command-word confusion as a real, common cause of lost marks). Edway's own question bank already uses these command words extensively (confirmed via this run's live drive: "Calculate the relative atomic mass...", "Explain the main effect...", multiple "Identify..." items) but never teaches what they each actually demand. A small, first-encounter-only info affordance (e.g. a subtle underline + tap/hover tooltip on the bolded command word within a question prompt, shown once per command word per child, dismissible) would directly build the exact literacy skill the real exam rewards, at very low build cost since the content (a short definition per command word) is static and human-authored, fitting the "AI never authors curriculum" rule perfectly since this is pure static UI copy, not a generated explanation.
  - Change: a small static `COMMAND_WORD_DEFINITIONS` map (calculate, explain, evaluate, describe, compare, identify, analyse, justify — 8-10 entries, one line each, e.g. "Calculate — work out a numerical answer, usually showing your method.") in `src/lib/child/` alongside the existing curriculum constants; a lightweight client component that scans the current question's `prompt` for the first matching command word, renders it with a subtle affordance, and tracks "seen" command words in the child's own local state (not analytics — Children's Code, no tracking) so the tooltip only surfaces the first 1-2 times per word, not on every question forever.
  - Risk: Low-medium. Needs care that the tooltip never blocks or delays answering (purely additive, dismissible, keyboard-reachable, never auto-opens over the answer options) and stays out of the checker/AI pipeline entirely (static copy only).

- [ ] **F8 · feature (north-star, parent-facing GCSE readiness) · a "distance to target grade" widget on the parent dashboard, synthesising data that already exists but is never shown together** (size: M)
  - The win: the dashboard, child-profile and portfolio pages each separately show pieces of GCSE readiness (predicted grade, topics certified, mock status) but never answer the single question a parent actually wants answered at a glance: "how far is my child from where they need to be?" `lib/engine/trajectory.ts` and `lib/engine/exam-decision.ts` already compute readiness percentage and predicted grade deterministically from real certification/mock data (confirmed via this run's code read — no new data collection needed, explicitly non-profiling per the existing "progress evidence, not a predicted grade" framing already in `trajectory-chart.tsx`). A small widget combining "predicted grade X" + "N topics still to certify to reach the next grade band" + "target exam window" into one honest, encouraging line would be a genuinely new synthesis, not a data-collection change, directly serving the north star (comfort/confidence via visible progress toward the real goal).
  - Change: a new small card on `src/app/(dashboard)/dashboard/page.tsx` (per-child), reusing `lib/engine/trajectory.ts`'s existing exports plus the already-computed GCSE-only certified/total counts (post-EPIC-22 fix) to phrase something like "Grade 5 today, on track for Grade 6 once 3 more Maths topics are certified" — deterministic, no new AI call, no new data collected, consistent with the existing "progress evidence, not a prediction of a child" framing already used elsewhere in the codebase.
  - Risk: Low-medium. Purely a read/display feature over existing deterministic data; the main risk is copy tone (must stay warm and non-pressuring, never "behind" framing, matching the existing `STAGE_PHRASE`/reason-copy conventions already in `repo.ts`).

- [ ] **F9 · stack hygiene (routine) · clear the small `npm outdated` in-range batch** (size: S)
  - The win: `@sentry/nextjs`, `@types/node`, `@upstash/ratelimit`, `autoprefixer`, `lucide-react`, `motion`, `posthog-js` are all safe, in-range, non-breaking patch/minor bumps (`npm audit` stays at 0 vulnerabilities throughout). Keeps the steady drip of small version bumps EPIC 7 has maintained every run, so nothing accumulates into a larger, riskier jump later.
  - Change: `npm install @sentry/nextjs@latest @types/node@latest @upstash/ratelimit@latest autoprefixer@latest lucide-react@latest motion@latest posthog-js@latest`, then the standard gate (type-check, tests, lint, build).
  - Risk: Very low. All in-range bumps, no major-version jumps; the deliberate `next`/`eslint`/`typescript`/`vitest` major holds are explicitly not part of this change.

---

## Summary
- Bugs: 3 (0 Critical, 2 High, 1 Medium, 0 Low). Features: 9.
- Top 3 to ship: **B1** (the homepage hydration error, previously "fixed" 2026-09-13/14, still reproduces live this run — and critically, the shipped fix's own root-cause theory is now disproven since the reproducing session has `prefers-reduced-motion` genuinely off — so this needs the real non-minified local repro before a third guess), **B3** (the Local-Authority portfolio email share has no rate limit at all, a genuine spam/abuse vector against Edway's own email sender reputation, one `rateLimit()` call away from matching every sibling route), **B2** (a 6th, previously-missed site of the already-diagnosed "Grade Grade" duplicated-word bug, this time on the parent-facing `/schedule` "Why" reasoning card).

**Standing max-depth child pass (Ivy, all 4 interaction types + resilience, this run):** `maths_pythagoras` full explainer-to-practice-to-mastery-to-certificate-to-brain-stretch run (mcq, deliberately wrong twice including the calm-break "stand up and stretch" prompt firing correctly, the See-it walkthrough's "Your turn" active-recall settle confirmed still working, correct on the final try, "Topic mastered!" then the F2-from-2026-09-13 3D-cuboid-diagonal stretch question live and correct). `eng_devices` tap_reveal (wrong-then-right, calm no-red feedback, full mastery run to certification). `maths_algebra_linear` fill_blank (wrong-then-right in practice; in mastery, deliberately failed 2 of 3 questions including exhausting all 3 tries down to the auto-opened See-it walkthrough, reaching the warm "Let's look at this another way" reteach screen with Eddie present, 1/3 score, non-punitive copy throughout — confirmed the reteach loop end to end for the first time in several runs' worth of reports). `maths_geometry` drag_drop (tap-to-place AND pure-keyboard-Tab-then-Enter placement both confirmed working, correct settle with checkmarks). Warm-up interleaving (ratio-and-proportion question, correct-answer explanation). Mock-exam gate correctly reads 9/10 Maths (up from 8/10 pre-session) after today's fresh certification. No new instance of the EPIC 1 shallow-regex/derived-visual correctness bug class found this run (the AI-generated per-question misconception explanations, e.g. correctly diagnosing "you added 5 instead of subtracting" from a specific wrong answer, were notably accurate and well-targeted every time this run).

**Parent/tutor/admin journeys (all run this session, real writes on the safe test family):** approved a fresh weekly plan for Ivy end to end (`/schedule` → "Approve this week" → confirmed "Approved" badge); generated a genuinely fresh portfolio for Ivy (21/34 topics, 9/14 Maths, matching the dashboard/mock-gate figures exactly, EPIC 22's fix still holding); confirmed the `twitter:image` fix from 2026-09-13 (B2 that day) is live and correct via a fresh `curl` (`twitter:image` now resolves to the same working dynamic OG route as `og:image`, no more dead `/og-image.png` reference); opened `/verify-portfolio` (clean); re-confirmed `/compliance/cnis` shows the CORRECT "working Grade 4–5" (no duplication) for Ivy, proving the original 5-site fix is genuinely solid and today's B2 above really is a 6th, separate site. Tutor (read-only): sessions list correctly empty with a genuine empty state (no sessions to open a handoff from — noted, not a bug). Admin (read-only): Overview, Users, Finance, Escalations all clean, zero console errors; incidentally traced a THIRD child ("Sam Smoke") on the SMOKE parent account back to a stray beyond `scripts/seed-test-accounts.ts`'s own 2-child fixture (see F4).

**Marketing sweep (desktop + mobile):** `/`, `/pricing`, `/how-it-works`, `/safety`, `/for-parents`, `/about`, `/roadmap`, `/resources`, `/compliance`, `/contact`, `/login` all checked for console errors this run; every page clean except the homepage's own hydration error (B1). No horizontal overflow found on `/` or `/pricing` at 390px wide (`scrollWidth` 380 both times). Security headers (`curl -sI /`) re-confirmed healthy: HSTS+preload, X-Frame-Options DENY, Permissions-Policy, nosniff, and the full CSP all present; `robots.txt`/`sitemap.xml` both clean.

Teardown: signed out of parent, tutor and admin sessions between each switch (`Sign out` clicked and confirmed landing back on `/` or `/login` each time); closed the Playwright session at the end.
