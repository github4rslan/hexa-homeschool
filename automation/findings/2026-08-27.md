# Scout findings — 2026-08-27

DECISION: all

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           → build EVERY item below (default)
    B1, F2, F5    → build ONLY these IDs
    skip          → build nothing tonight
  Leave it as `all` and do nothing to run fully autonomous.
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright (MCP tools only) · Base URL: https://edway.uk

Day-of-week focus: Thursday → end-to-end journeys (B-journeys) deep-dive with extra depth/edge cases, plus the standing max-depth child pass (all 4 interaction types + resilience), every-day lanes run regardless of focus. No owner priority idea was supplied this run — ranking below is Scout's own judgement of value.

Desktop pass confirmed at `innerWidth` **1280** (`browser_resize` 1280×800). Mobile pass confirmed at `innerWidth` **390**, `scrollWidth` **380** (no horizontal overflow) on `/`, `/pricing`, `/dashboard`, `/learn` (hub, a resumed `fill_blank` lesson, and a `drag_drop` question). Whole-page scroll confirmed on the homepage (`scrollHeight` 16236 → `scrollY` reached 15392, the true bottom).

Credential handling: all test-account credentials (`SMOKE_EMAIL/PASSWORD`, `TUTOR_EMAIL/PASSWORD`, `ADMIN_EMAIL/PASSWORD`) were read from `.env.local` via Bash directly into this session's context only, and typed straight into `browser_type` parameters — never written to any file in the working tree. Screenshots taken mid-session were reviewed live and then deleted (the Playwright screenshot tool in this runtime can only write inside the repo — either `.playwright-mcp/`, gitignored, or the repo root if no subpath is given — never the real OS temp dir), so none were left in the working tree; confirmed clean via `git status --porcelain` before finishing.

---

## Bugs (Critical → Low)

- [x] **B1 · High · A science question figure derived from the wrong deriver branch shows a "Solid / Liquid / Gas" particle diagram on questions that have nothing to do with states of matter — live-reproduced on a real Human Body Systems question, and confirmed by source to also hit Chemical Reactions and Ecology** — `src/lib/child/science-visual.ts:188-211` (`deriveScienceVisual`'s `states_of_matter` branch)
  - Repro / evidence: as Ivy, opened the **Human Body Systems** (`sci_body`) practice set. Question 1, *"Where does gas exchange happen in the lungs?"* (options Trachea/Alveoli/Bronchi/Diaphragm) rendered a figure captioned *"Particle diagrams for the three states of matter: a solid with particles packed in a fixed grid, a liquid with particles close but able to move, and a gas with particles spread far apart."* with three little "Solid / Liquid / Gas" panels — a completely unrelated KS3 chemistry diagram sitting next to a GCSE biology respiration question. Screenshot reviewed live (viewport capture of the practice screen) before cleanup.
  - Root cause: `deriveScienceVisual`'s `states_of_matter` branch (`science-visual.ts:188-211`) matches on bare keyword substrings including `" gas"` / `"gas "` with **no topic-tag gate at all** — unlike the `cell` branch just above it (which additionally requires `topicTag.includes("cell")`) and the `plant_parts` branch below it (requires `topicTag.includes("living")`). Any prompt anywhere in the bank that contains the word "gas" in ANY sense (not just phase-of-matter) trips this branch, because it runs before the un-gated `material_property` branch and there is no biology/chemistry branch ahead of it to intercept human-body or reaction prompts. Confirmed via `grep` across every seed file that this is not a one-off: the same collision fires on **at least three more real, currently-live questions**, all correctly authored curriculum text, all innocently containing the word "gas" in a non-phase-of-matter sense:
    - `sci_body` (`curriculum.seed.ts:336`): *"Where does gas exchange happen in the lungs?"* (live-reproduced above)
    - `sci_body` mastery distractor text (`curriculum.seed.ts:949,962`): *"To slow the blood down for **gas exchange** with cells"* — this is itself the CORRECT option text for an "Explain why arteries have thick walls" mastery question I answered live this run; the figure shown alongside it is the same wrong particle diagram
    - `sci_reactions` (`curriculum.seed.ts:353`): *"What gas is produced when an acid reacts with a metal?"*
    - `sci_ecology` (`curriculum.seed.ts:386`): *"Which gas do plants remove from the air during photosynthesis?"*
    This is the exact class of bug `automation/backlog.md` EPIC 1 flagged as an *"undetermined risk, no colliding prompt found... yet"* candidate on 2026-08-23 — this run supplies the concrete collision.
  - Fix: the safest, smallest change is to drop the two bare `" gas"` / `"gas "` needles from the `states_of_matter` list (every OTHER needle — `solid`, `liquid`, `ice`, `freeze`, `frozen`, `melt`, `steam`, `state(s) of matter`, `evaporat`, `condens` — is genuinely phase-of-matter-specific and, per the same `grep`, never collides with a non-states topic in the current bank). Optionally also require the branch to fire only when the prompt contains at least one of `solid`/`liquid` alongside `gas` (so a genuine "gas → liquid" phase-change question, which does exist in `sci_states`/`sci_ks2_materials`, still matches), which is a slightly more defensive version of the same fix. Add a unit test asserting `deriveScienceVisual("sci_body", "Where does gas exchange happen in the lungs?")` returns `null` (falls through to the decorative AI figure, exactly like a subject with no deriver at all) and that the existing `sci_states`/`sci_ks2_materials` phase-change prompts still return a `states_of_matter` spec.
  - Risk: low — purely a needle-list edit in one pure function with existing unit test coverage (`tests/` already covers `science-visual.ts` derivers per the codebase's testing convention); re-run the full derive-visual test suite and spot-check the four listed prompts return `null` while genuine states-of-matter prompts (e.g. "When water freezes it becomes:") still return the figure.

- [x] **B2 · High · A topic's certification date silently jumps to "today" every time it is re-mastered, even though the topic was already certified weeks earlier — undermining both the LA compliance portfolio's "Awarded {date}" evidence and the parent-facing "topics certified this week" counts** — `src/lib/db/repo.ts:1534-1564` (`upsertCompetence`)
  - Repro / evidence: not live-reproduced (deliberately — doing so would corrupt the SMOKE test family's real certification history in production with no clean rollback, which Scout's discovery-only remit doesn't permit). Confirmed by direct code read instead, which is unambiguous:
    ```ts
    const set: Partial<CompetenceDoc> = {
      state,
      certified_at: state === "certified" ? new Date() : null,
      updated_at: new Date(),
    };
    if (state === "certified") {
      const existing = await col.findOne({ child_id: childId, topic_tag: topicTag });
      if (existing?.state !== "certified") {
        // ... only sets next_review_at / review_interval_days on a FRESH certification
      }
    }
    ```
    The function's own comment says *"Schedule the first spaced-repetition review only on a FRESH certification... Re-running a certified topic must not reset its review schedule"* — and the code correctly guards `next_review_at`/`review_interval_days` against exactly that. But `certified_at` itself has **no equivalent guard**: it is unconditionally set to `new Date()` on every write with `state === "certified"`, including a re-run of an ALREADY-certified topic. This run's own "Practice more" pass on two already-certified topics (`maths_algebra_linear`, `eng_devices`) proved this path is genuinely reachable by an ordinary child — a child can freely re-enter Practise→Mastery on any already-certified topic from the "Practice more" link and, on a perfect 3/3, `logLessonCompletion` (`src/app/(dashboard)/lesson/actions.ts:68-121`) calls `upsertCompetence(..., "certified")` again unconditionally (no existing-state check before the call).
  - Blast radius: `certified_at` is the single source of truth for — the LA compliance portfolio's per-certificate "Awarded {date}" line (`lib/compliance/portfolio.ts`), the parent dashboard's "X topics certified this week" stat and "Ivy's week in review" card, the readiness-trajectory chart's certification-over-time series, and the child map's badge-shelf date. All four would silently show today's date for a topic the child mastered a month ago, just because they revisited it for extra practice — a real evidence-integrity problem for a platform whose portfolio explicitly brands itself "tamper-evident." (The mastery-celebration NOTIFICATION itself is correctly deduped and does NOT re-fire — `emitMasteryEvent`'s `dedupeKey` is `mastery:{childId}:{topicTag}`, not date-scoped, per `lib/notify/parent-event.ts:52` — so this is specifically a silent data/evidence bug, not a spammy duplicate-email bug.)
  - Fix: mirror the exact guard already used for the review schedule two lines below: fetch `existing` once at the top of the function (or reorder the existing fetch above the `set` object), then compute `certified_at: state === "certified" ? (existing?.state === "certified" ? existing.certified_at : new Date()) : null`. Small, contained, single-function change.
  - Risk: low — purely a data-write guard in one already-small function; add a unit test (or a targeted integration check against a test DB) asserting a second `upsertCompetence(..., "certified")` call for the same child+topic leaves `certified_at` unchanged from the first call. Live-verify with a read-only DB query (compare a topic's `certified_at` before and after a deliberate re-take), not a fresh live re-take that would itself trigger the bug against real data.

- [x] **B3 · Medium · The child profile page tells a parent a subject "has not been assessed yet" in one card while the card directly above it says the same subject is "Working at GCSE level" with topics certified — a real, live-reproduced contradiction on the same page** — `src/lib/engine/assessment-narrative.ts:86-94` + `src/app/(dashboard)/dashboard/children/[childId]/page.tsx:249-277`
  - Repro / evidence: on Ivy's child profile (`/dashboard/children/{id}`), the **"Current standing"** card (fixed by a 2026-08-05 bug fix, B1 that run) correctly reads *"English — Working at GCSE level · 6 topics certified"* and *"Science — Working at GCSE level · 7 topics certified"* (both subjects have no diagnostic/mock evaluation yet, so it falls back to lesson-based certification — exactly the accepted fix). Scrolling down to the **"Understanding these results"** card on the SAME page, the English and Science rows instead read *"English has not been assessed yet — the diagnostic takes about five minutes..."* and the identical sentence for Science — flatly contradicting the card two sections above it.
  - Root cause: `buildAssessmentNarrative` (`assessment-narrative.ts:86-94`) is handed only `standings` (diagnostic/mock `readiness`/`grade` — via `latestEvaluationsBySubject`), with **no** `certifiedCounts`/`bandBySubject` input, so any subject with `readiness === null && !grade` always gets the "has not been assessed yet" sentence, regardless of real lesson-based progress. This is the identical root-cause SHAPE the 2026-08-05 fix solved for "Current standing" (`page.tsx:139-142` comment: *"the standalone 'Working level' block was merged in here so the same band isn't shown twice"*) — but that fix only touched the "Current standing" card; `buildAssessmentNarrative` and its caller (`page.tsx:101`) were never updated, so the SAME page still contradicts itself lower down. The section is gated on `assessed = standings.some(s => s.readiness !== null || s.grade !== null)` (`page.tsx:102`) — true here because Maths has a mock — so the whole card renders, including the individually-wrong English/Science lines.
  - Fix: extend `buildAssessmentNarrative`'s signature to also accept `certifiedCounts: Record<Subject, number>` (already computed once per page load at `page.tsx:64`, just not threaded through), and give the `readiness === null && !grade` branch (`assessment-narrative.ts:88-93`) a second branch mirroring `page.tsx`'s own "Current standing" fallback: when `certifiedCounts[s.subject] > 0`, return something like `"${label} hasn't had a formal diagnostic yet, but ${n} topic${n===1?"":"s"} ${n===1?"is":"are"} already certified through lessons."` instead of the flat "not assessed" line. Add a unit test covering both the zero-certified and some-certified cases.
  - Risk: low — an additive parameter + one new conditional branch in a pure, already-unit-tested function; no change to the diagnostic/mock logic itself. Re-verify live on Ivy's profile that English/Science no longer contradict "Current standing."

- [x] **B4 · Low · Child profile "Current standing" shows a doubled word — "Mock score · Grade Grade 3" — for any subject with a mock result** — `src/app/(dashboard)/dashboard/children/[childId]/page.tsx:174`
  - Repro / evidence: Ivy's Maths card (has a mock result) rendered, verbatim in the accessibility tree: `Mock score · Grade Grade 3`.
  - Root cause: `tierToGrade()` (`src/lib/data/diagnostic.ts:34-43`) already returns grade strings WITH the word "Grade" baked in (`"Grade 3"`, `"Grade 1–2"`, `"Grade 4–5"`, etc. — this is `model_predicted_grade`, stored verbatim). The template at `page.tsx:174` then adds a second, redundant `"Grade "` prefix: `` {s.grade ? ` · Grade ${s.grade}` : ""} `` → `"· Grade " + "Grade 3"` → `"· Grade Grade 3"`. (The neighbouring `mockBoundaryGrade` line just below, `"Exam-style grade {s.mockBoundaryGrade}"`, is correctly NOT doubled — `mock_boundary_grade` is stored as a bare number/letter per its own doc comment, so that one template is right; only the `s.grade` one is wrong.)
  - Fix: drop the literal `"Grade "` from the template at line 174 (the value already contains it): `` {s.grade ? ` · ${s.grade}` : ""} ``.
  - Risk: none — a one-word string-literal deletion in a display template; live-verify Ivy's Maths card reads "Mock score · Grade 3".

## Features & upgrades (High value → Low)

- [x] **F1 · curriculum (spec gap, EPIC 3) · A new `maths_transformations` topic — Edexcel 1MA1 spec point G7 (translations, reflections, rotations, enlargements) currently has ZERO coverage anywhere in the question bank** (size: M)
  - The win: `grep -in "simultaneous\|transformation" src/lib/data/curriculum.seed*.ts` returns nothing at all — not a thin topic, a total absence. Transformations is one of the most consistently-examined topics on both Foundation and Higher Edexcel 1MA1 papers (describe/construct translations by a vector, reflect in a line, rotate about a point, enlarge by a scale factor). This closes a real spec gap rather than deepening an already-covered one, directly serving the north star. Follows the exact precedent already accepted for `maths_inequalities` and `maths_mensuration` (both added the same way: a new topic entry + a worked example + 3 checked starter questions).
  - Topic entry (for `curriculum.seed.ts`'s `SEED_TOPICS` array, after `maths_inequalities`):
    ```
    { subject: "mathematics", topic_tag: "maths_transformations", title: "Transformations", summary: "Translations, reflections, rotations and enlargements.", key_stage: 4, working_grade_band: "Grade 3–5", order: 13, prerequisite_tags: ["maths_geometry"] },
    ```
  - Worked example (for `WORKED_EXAMPLES_BY_TOPIC`):
    ```
    maths_transformations: {
      title: "Enlarge a shape by scale factor 2",
      scenario: "A rectangle has sides 3 cm and 5 cm. It is enlarged by scale factor 2.",
      steps: [
        { line: "Multiply every side length by the scale factor.", visual: { label: "Scale factor", value: "× 2" } },
        { line: "3 cm becomes 3 × 2 = 6 cm.", visual: { label: "Side 1", value: "6 cm" } },
        { line: "5 cm becomes 5 × 2 = 10 cm.", visual: { label: "Side 2", value: "10 cm" } },
      ],
      yourTurn: "Try enlarging a 4 cm side by scale factor 3.",
    },
    ```
  - Starter questions (verbatim `SeedQuestion` shape):
    ```
    {
      topic_tag: "maths_transformations",
      subject: "mathematics",
      tier: 2,
      key_stage: 4,
      kind: "practice",
      prompt: "A shape is enlarged by scale factor 3. Its original side length is 4 cm. Calculate the new side length.",
      options: ["12 cm", "7 cm", "1⅓ cm", "8 cm"],
      correct_index: 0,
      explanation: "To enlarge a length, multiply it by the scale factor: 4 cm × 3 = 12 cm.",
      hints: ["Multiply the original length by the scale factor — don't add it.", "4 × 3 = ?"],
      misconceptions: ["", "This adds the scale factor to the length (4 + 3) instead of multiplying.", "This divides by the scale factor (4 ÷ 3) instead of multiplying.", "This doubles the length as if the scale factor were 2, not 3."],
    },
    {
      topic_tag: "maths_transformations",
      subject: "mathematics",
      tier: 2,
      key_stage: 4,
      kind: "practice",
      prompt: "Triangle A is translated by the vector (3, −2). A vertex of A is at (1, 4). Find the coordinates of that vertex after the translation.",
      options: ["(4, 2)", "(−2, 6)", "(4, 6)", "(1, 4)"],
      correct_index: 0,
      explanation: "Add the vector to the point: (1 + 3, 4 + (−2)) = (4, 2).",
      hints: ["Add the x-values together, then add the y-values together.", "1 + 3 = 4, and 4 + (−2) = 2."],
      misconceptions: ["", "This subtracts from x and adds to y (1 − 3, 4 + 2) — the signs are swapped.", "This adds 3 and 2 as if both were positive, ignoring the negative sign on the y-value.", "This is the original point — the translation was never applied."],
    },
    {
      topic_tag: "maths_transformations",
      subject: "mathematics",
      tier: 3,
      key_stage: 4,
      kind: "mastery",
      prompt: "Which single transformation maps a shape onto an identical mirror image of itself, as if reflected in a mirror line?",
      options: ["Reflection", "Rotation", "Translation", "Enlargement"],
      correct_index: 0,
      explanation: "A reflection flips a shape over a line, producing a mirror image that is the same size but the opposite way round.",
      hints: ["Think about what happens when you hold a shape up to a mirror.", "Rotation turns a shape, translation slides it, and enlargement resizes it — none of those flip it."],
      misconceptions: ["", "A rotation turns the shape around a fixed point — it doesn't create a mirror-image flip.", "A translation slides the shape without turning or flipping it.", "An enlargement changes the size of the shape; it doesn't create a mirror image."],
    }
    ```
  - Risk: none at authoring stage — read-and-flag/propose only, hand-re-derived: 4×3=12 ✓; (1+3, 4−2)=(4,2) ✓; reflection is the only one of the four transformations that reverses orientation ✓. Each has exactly one defensible correct option. Owner approval gate via `DECISION:` still applies before seeding; Mechanic should also add `maths_transformations` to any hard-coded subject-topic-count lists (mock-gate floor, roadmap ordering) the same way `maths_inequalities` was previously wired in.

- [x] **F2 · curriculum / exam-readiness (new capability) · A tap-to-define "command word" chip on exam-style questions, reusing the already-shipped glossary pattern, so a child learns what "Calculate" / "Explain" / "Describe" / "Evaluate" actually ask them to DO** (size: M)
  - The win: the brief's north star explicitly calls out "knowing the exact exam style" as core to readiness, and the mock/practice bank already has real command words threaded through prompts (confirmed live this run: *"Explain why arteries have thick, muscular and elastic walls,"* *"Calculate the exterior angle of a regular pentagon,"* *"Calculate the potential difference..."*), but nothing today teaches a child what each command word is asking for as a SKILL, separate from the specific question. A child who doesn't yet distinguish "Explain" (give a reason/mechanism) from "Describe" (state what happens, no reason needed) will lose marks on the real paper regardless of subject knowledge. This is genuinely new capability, not a re-skin of an existing feature, but it deliberately reuses a fully-proven pattern (`lib/child/glossary.ts` + `<GlossaryText>`, shipped 2026-07-27, extended multiple times since) rather than inventing new UI.
  - Change: a small new pure module `lib/child/command-words.ts` exporting a human-authored dictionary (`Calculate: "work out a numerical answer, usually showing your method"`, `Explain: "give a reason or mechanism, not just what happens"`, `Describe: "say what happens, in order, without needing to say why"`, `Evaluate: "weigh up strengths and weaknesses to reach a judgement"`, `Compare: "say what is the same and what is different between two things"`, `Estimate: "give an approximate answer using rounding"`, `Show that: "prove a given result using working, not just state it"`) plus a `detectCommandWord(prompt: string): string | null` pure function matching the first word/phrase of a prompt (case-insensitive, whole-word) against the dictionary keys. Thread it through `practice-player.tsx`/`mastery` question rendering the same way the English glossary already renders inline: a small "CALCULATE" badge near the question heading, tap-to-reveal the definition + "Read aloud" (reuses the exact `<GlossaryText>`/TTS wiring already in place). No AI, no new schema field — command words are inferred deterministically from the prompt text already in the database.
  - Risk: low — purely additive UI + one new pure/unit-tested module; must NOT fire on non-exam-style prompts that happen to start with a matched word in a different sense (spot-check the dictionary against the existing bank the same way the glossary/visual deriver work always has been, before shipping) and must degrade to nothing (no badge) when no command word matches — never a wrong or misleading tag.

- [x] **F3 · curriculum (EPIC 2 depth) · Second command-word `maths_pythagoras` mastery question — trigonometry "Calculate the angle"** (size: S)
  - The win: `maths_pythagoras` has exactly one command-word item today (`curriculum.seed.ts:677`, "Calculate the length of the hypotenuse" via Pythagoras' theorem itself). This adds a distinct trigonometry "Calculate" item using `tan`, giving the topic command-word depth across BOTH halves of its spec (Pythagoras AND basic trig ratios), not just one repeated phrasing.
  - Question (verbatim `SeedQuestion` shape):
    ```
    {
      topic_tag: "maths_pythagoras",
      subject: "mathematics",
      tier: 5,
      key_stage: 4,
      kind: "mastery",
      prompt: "A right-angled triangle has an opposite side of 6 cm and an adjacent side of 8 cm. Calculate the size of angle θ, to 1 decimal place.",
      options: ["36.9°", "53.1°", "75°", "48.6°"],
      correct_index: 0,
      explanation: "tan θ = opposite ÷ adjacent = 6 ÷ 8 = 0.75. Taking the inverse tan (tan⁻¹) of 0.75 gives θ ≈ 36.9° (1 d.p.).",
      hints: ["tan θ = opposite ÷ adjacent. Work out that ratio first.", "Use the inverse tan (tan⁻¹) button on your calculator to turn the ratio into an angle."],
      misconceptions: ["", "This finds the OTHER angle in the triangle by dividing adjacent by opposite instead of opposite by adjacent (8 ÷ 6, not 6 ÷ 8).", "This treats the ratio 0.75 as if it were the angle in degrees — tan θ = 0.75 is not the same as θ = 75°. You still need to take the inverse tan.", "This uses sin instead of tan, and treats the adjacent side (8 cm) as if it were the hypotenuse."],
    }
    ```
  - Risk: none — hand-derived (arctan(6/8) = arctan(0.75) ≈ 36.87° → 36.9°; the swapped-ratio distractor arctan(8/6) ≈ 53.13° → 53.1°, and 36.9° + 53.1° = 90.0°, the correct complementary-angle sanity check for this triangle). Owner approval gate applies.

- [x] **F4 · curriculum (EPIC 2 depth) · Second command-word `sci_electricity` mastery question — "Calculate the power" (P = VI)** (size: S)
  - The win: `sci_electricity` has exactly one command-word item today (`curriculum.seed.ts:726`, "Calculate the potential difference" via V = IR). This adds the other core AQA 8464 electrical-quantities equation (P = VI), giving the topic depth across two required equations instead of one repeated formula.
  - Question (verbatim `SeedQuestion` shape):
    ```
    {
      topic_tag: "sci_electricity",
      subject: "science",
      tier: 3,
      key_stage: 4,
      kind: "mastery",
      prompt: "A 12 V battery drives a current of 3 A through a lamp. Calculate the power delivered to the lamp.",
      options: ["36 W", "4 W", "15 W", "0.25 W"],
      correct_index: 0,
      explanation: "Using P = V × I: power = 12 V × 3 A = 36 W.",
      hints: ["Power = voltage × current (P = V × I).", "Multiply 12 V by 3 A."],
      misconceptions: ["", "This divides voltage by current (12 ÷ 3) instead of multiplying them.", "This adds voltage and current (12 + 3) instead of multiplying them.", "This divides current by voltage (3 ÷ 12) instead of multiplying voltage by current."],
    }
    ```
  - Risk: none — hand-derived (12 × 3 = 36). Owner approval gate applies.

- [ ] **F5 · curriculum / delight (ties directly to B1) · A derived Human Body Systems visual, so `sci_body` questions get an honest figure instead of falling to a wrong or generic one** (size: M)
  - The win: `deriveScienceVisual` already extends cleanly into `cell`/`plant_parts`/`food_chain`/`life_cycle` — `sci_body` (Human Body Systems, live-driven to full certification this run) is the one major KS4 science topic with NO dedicated branch at all, so today it either falls through to the WRONG states-of-matter figure (B1) or the generic decorative AI image. A simple labelled-diagram branch (e.g. a lungs/alveoli sketch for gas-exchange prompts, a heart/blood-vessel sketch for circulation prompts — mirroring the existing `cell` diagram's level of simplicity) would give this topic the same free, honest, always-present figure quality every other major topic already has, and doubles as the natural companion fix to B1 (once the wrong figure stops appearing, `sci_body` should get its OWN correct one, not just silence).
  - Change: add a `human_body` (or two: `respiratory`/`circulatory`) kind to `ScienceVisualSpec` in `science-visual.ts`, gated on `topicTag === "sci_body"` (mirroring the existing `cell`/`plant_parts` gating pattern) plus a keyword check (`lungs`, `alveoli`, `breath`, `heart`, `blood`, `artery`, `vein`, `capillary`), with a simple SVG renderer alongside the existing `states_of_matter`/`cell` renderers in `question-visual.tsx`. Keep it deliberately generic (never label the specific answer option, exactly like the existing `cell` diagram omits the cell wall so it can't give away the plant-vs-animal-cell answer).
  - Risk: low — additive branch at the tail of an existing, well-tested deriver chain; verify the new branch does NOT fire on `sci_reactions`/`sci_ecology` prompts that also mention "gas" (the same collision class as B1) by keeping it strictly `topicTag === "sci_body"`-gated.

- [ ] **F6 · delight (small, standing lane) · Give the "Thanks for sharing!" reflection confirmation a warm entrance instead of an instant pop-in** (size: S)
  - The win: `TodayReflection` (`components/child/today-reflection.tsx`) already animates the chip tap itself (`whileTap`) and the mood check-in (`emoji-checkin.tsx`) already has a settle-bounce on the picked face AND a `animate-child-pop` entrance on its "Thanks for checking in!" confirmation — but `TodayReflection`'s own confirmation line (`{picked && <p>Thanks for sharing! 💛</p>}`, line 62-66) has no entrance at all, just an instant conditional render. Small, easy consistency win across two visually-similar "you just told me something" moments on the same hub.
  - Change: wrap the confirmation `<p>` in `AnimatePresence` + a `motion.p` with a gentle `initial={{ opacity: 0, y: 6 }}` → `animate={{ opacity: 1, y: 0 }}` fade/slide, matching the existing "warm hint-card entrance" pattern used elsewhere in the child experience. `useReducedMotion`-gated to an instant appearance, as everywhere else.
  - Risk: none — presentational only, one small component, no logic change.

- [ ] **F7 · security / stack hygiene · Bump the in-range ("Wanted") dependency versions, several of which touch auth, money and the database driver** (size: S)
  - The win: `npm outdated` shows several packages with a newer version already satisfying the existing `package.json` semver range (so `npm update` picks them up with zero `package.json` edits): `mongodb` 7.5.0 → 7.6.0 (the official driver for the single system of record), `stripe` 22.4.0 → 22.6.0 (the money-path SDK), `jose` 6.2.8 → 6.2.10 (the JWT session library), `@sentry/nextjs` 10.69.0 → 10.71.0, `@upstash/redis` 1.38.2 → 1.38.3 (distributed rate-limit backend), `@axe-core/playwright` 4.12.1 → 4.13.0, `@types/react-dom`, `tsx`, `vitest` — all patch/minor, all within the already-declared range, so this is routine hygiene rather than a version-policy change. (Next.js/eslint/framer-motion majors stay OUT of scope here — those are correctly tracked in `automation/backlog.md` EPIC 7 as their own dedicated migration passes, not routine bumps.)
  - Change: `npm update` (or bump each individually), then `npm run type-check && npm test && npm run lint && npm run build` as the standard gate.
  - Risk: low — all in-range patch/minor bumps with no direct-dependent API changes expected; the standard green-gate + a live health-check covers it. Re-run `npm audit` after to confirm 0 vulnerabilities still holds.

- [ ] **F8 · feature (small, uses data already in the schema) · Show each topic's GCSE grade band inline on the parent-facing curriculum roadmap** (size: S)
  - The win: every `CurriculumTopicDoc` already carries a `working_grade_band` (e.g. "Grade 3–5", confirmed live via the admin curriculum CMS, which is the ONLY place this field is currently rendered — `grep -rln working_grade_band src` returns just the admin page + the seed/type/repo files). The parent-facing roadmap (`RoadmapCard`, shown on every child profile) fetches the full `CurriculumTopicDoc` in `childSubjectRoadmap` (`repo.ts:2397-2429`) but only extracts `topic_tag`/`title` into `RoadmapTopic` before rendering — the grade band is read from the DB and then silently discarded. Surfacing it (a small badge next to each topic, e.g. "Geometry & Angles · Grade 3–5") gives a parent instant grade-context for what's coming next without needing a new field, a migration, or any curriculum authoring — pure plumbing of data that already exists, directly serving GCSE-readiness confidence for the PARENT half of the north star.
  - Change: add `workingGradeBand: string | null` to the `RoadmapTopic` interface (`lib/engine/roadmap.ts:19-23`) and `RoadmapTopicInput`, thread it through `buildRoadmapTopics` (currently maps `{ topic_tag, title }` only) and the `childSubjectRoadmap` caller in `repo.ts:2425-2427`, then render a small muted badge in `RoadmapCard` (`components/dashboard/roadmap-card.tsx`) next to each topic row.
  - Risk: none — additive field on an existing, already-unit-tested pure function (`buildRoadmapTopics`); presentation-only on the parent side, never shown to the child (matches the existing "band language stays parent-only" convention already documented in the component's own file header).

---

## Summary
- Bugs: 4 (2 High / 1 Medium / 1 Low) · Features: 8
- Top 3 to ship: **B1** (a wrong "states of matter" particle diagram renders on real Human Body Systems / Chemical Reactions / Ecology questions — live-reproduced with a screenshot, confirmed by source to hit 4 separate live questions, and the exact undetermined-risk class `automation/backlog.md` flagged on 2026-08-23), **B2** (a topic's certification date silently jumps to "today" on every re-mastery, corrupting the LA portfolio's "Awarded {date}" evidence and the parent's weekly certified-topic counts — confirmed by code, deliberately not live-reproduced to avoid corrupting real test-family data), **F1** (a total, zero-coverage gap in the Edexcel 1MA1 spec — Transformations — closed with a topic entry, worked example and 3 checked starter questions, following the exact precedent already accepted for `maths_inequalities`/`maths_mensuration`).

This run drove the FULL child learning loop end-to-end at maximum depth per the standing brief: two topics taken from Learn → Practise (mixed right/wrong) → Mastery → certificate (`eng_ks3_reading` fresh; `sci_body` deliberately failed mastery once to confirm the reteach/Eddie loop, then re-passed to certify) — every hint rung, the calm-break prompt, "Why isn't that right?", See-it walkthroughs, and both celebration types all fired correctly with zero red/shake/buzzer anywhere. All four interaction types were completed this run (`mcq`, `fill_blank` including its wrong-then-right path, `drag_drop` via both tap-to-place AND full keyboard placement, `tap_reveal` including the reveal/select gesture split). Resilience checks all PASS: a genuine browser refresh mid-lesson resumed at the exact step with score intact ("Welcome back, Ivy"); a rapid triple-click on "Check answer" did not double-score or double-render the result panel; full keyboard-only navigation (Tab/Arrow/Enter) completed a question end-to-end with a visible focus ring throughout. Every B-journeys flow ran start-to-finish and PASSED: parent oversight (dashboard → child profile → portfolio generation → email-share confirmation → CNIS registration pre-fill), plan/schedule (approved a fresh week for Sam Smoke live, dashboard immediately reflected it — re-confirming EPIC 11's fix holds under a second real approve-then-reload), tutor (empty queue, silo holds, read-only), and admin (overview → finance → escalations, all read-only, zero destructive clicks). Mock exam correctly stayed honestly locked ("You've done your Maths mock for this week... next one unlocks on 31 August") rather than re-offering it. Zero console errors and zero failed network requests across the entire session, in both viewports. Static checks all clean: `type-check`, `lint`, and `npm audit` (0 vulnerabilities) all green.
