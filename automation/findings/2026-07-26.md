# Scout findings — 2026-07-26

DECISION: all

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           → build EVERY item below (default)
    B1, F2, F5    → build ONLY these IDs
    skip          → build nothing tonight
  IDs: B# = bug fix, F# = feature / upgrade.
-->

Runtime: playwright  ·  Base URL: https://edway.uk  ·  Desktop innerWidth **1280** · Mobile innerWidth **390**

Coverage: marketing (`/`, pricing, how-it-works, safety, for-parents), auth (login/signup inspected — no junk submits), `/api/health` (200 `{ok, db:up}`), parent dashboard + schedule + portfolio + tutoring + settings, **child mode taken end-to-end** (Maths lesson Learn→Practise→Mastery→certified, plus English + Science + mock all confirmed playable), **admin** (read-only, 4 sub-pages), **tutor** console (read-only). Both viewports on every surface. Logged out of all three accounts, browser closed.

## Live pass/fail — child mode (taken as a child, not eyeballed)

Drove the Maths quest (`maths_ks2_fractions`) fully as Ivy (KS2), desktop + mobile:
- **Explainer** (worked example "Find one quarter of 40", 3 steps + step narration + "Your turn") — **PASS**.
- **Practice Q1 "What is ½ of 16?"** — deliberately wrong ×2: wrong#1 = calm name-nudge ("Ivy, not quite yet…"), one hint, "2 tries left", **no red/buzzer**; wrong#2 = "See it" unlocks, hint ladder escalates (2 hints), "1 try left". "See it" opens Eddie's choreographed walkthrough. Correct (8) = star burst + "Brilliant! 🎉" + "Keep going". **PASS**.
- **Practice Q2/Q3** correct → "Amazing work! 🚀" → "Start mastery". **PASS**.
- **Mastery** 3/3 correct → **"Topic mastered! … certified 🎉"** trophy screen (Maths now 2/10 certified). **PASS**.
- **Mock exam** honestly gated ("You have 2/10", calm lock — not a dead-end). **PASS**.
- English + Science quests both resolve to real playable KS2 lessons (no dead-walls). **PASS**.
- **SEND/reading help (Ivy):** the "My stuff" page now carries a full **Easier-to-read** suite — easy-read font, text size (Normal/Large/Larger), reading ruler, colour themes, read-aloud toggle. **This is the carried-over F4 (dyslexia reading mode) — already SHIPPED, so it is dropped from the menu.**

**Zero console errors on every surface** (marketing / dashboard / child lesson flow / admin / tutor). No horizontal overflow at 390px anywhere. Data-silo holds: the tutor account saw only an empty "No sessions assigned yet" queue — no cross-family data. Prior fixes verified live: B1/B2/B3 from 2026-07-25 all shipped (dashboard avg-time now "4m · below the 45–60 min target"; single name greeting; "WEEK OF 20 JULY 2026"), settings password fields carry correct `autocomplete`, admin finance no longer says "1 accounts".

---

## Bugs (Critical → Low)

- [x] **B1 · Low · "week in review" card says "1 lessons · 1 certified" (never singularised)** — `src/components/dashboard/week-in-review.tsx:58`
  - Repro / evidence: parent dashboard *and* the child hub both render **"1 lessons · 1 certified · 20 July – 26 July"** in Ivy's week-in-review card. "1 lessons" / "1 certified" is ungrammatical.
  - Root cause: template literal interpolates raw counts: `` `${review.lessonsCompleted} lessons · ${review.topicsCertified.length} certified · …` `` — no plural handling.
  - Fix: singularise both nouns. The codebase already has the exact helper pattern (`plural(n, one, many)` in `lib/engine/weekly-summary.ts` + `daily-summary.ts`, and inline `lesson${n===1?"":"s"}` in `lib/email/templates.ts`) — reuse it: `${n} lesson${n===1?"":"s"} · ${c} topic${c===1?"":"s"} certified`.
  - Risk: pure copy on one shared card; renders in both `parent` and child variants — check both.

- [ ] **B2 · Low · Login page hard-codes "parent account" but is the sign-in for tutors and admins too** — `src/app/(auth)/login/page.tsx`
  - Repro / evidence: `/login` subtitle reads **"Sign in to your Edway parent account."** The same form is the only sign-in for the tutor (`/tutor`) and admin (`/admin`) roles (both authenticated here this run). A tutor/admin reads a screen telling them it's a parent account.
  - Root cause: role-blind copy on a shared auth surface.
  - Fix: neutralise to "Sign in to your Edway account." (one word) — no logic change, no role detection needed.
  - Risk: trivial presentational copy.

## Features & upgrades (High value → Low)

- [ ] **F2 · security · Clear the 5 high npm-audit CVEs (next / postcss / sharp / fast-uri)** (size: S)
  - The win: `npm audit` = **5 high** — `next` (8 advisories incl. SSRF in rewrites, DoS via Server Actions, image-optimiser SVG DoS, unauthenticated Server-Function disclosure), nested `postcss` (XSS via `</style>`, source-map path traversal), `sharp <0.35` (4 libvips CVEs), and `fast-uri` (host-confusion). type-check + lint + build are all green, so this is pure hygiene with no code churn.
  - Change: run **`npm audit fix` (NOT `--force`)** — the audit now reports a non-breaking fix is available for every one (patched `next` within `^15.5`, `postcss`, `sharp`, `fast-uri`). Re-run type-check + build to confirm the deploy stays green.
  - Risk: **never `npm audit fix --force`** — memory confirms it downgrades `next` to 9. Verify `next ^15.5.x` resolves to a patched 15.5, and the sharp-backed icon-generation script still renders.

- [ ] **F6 · billing · Annual billing toggle on `/pricing` (the advertised 17% is unreachable)** (size: M)
  - The win: `/pricing` still states the annual saving **twice** but there is no monthly/annual toggle and both CTAs are monthly (`/api/billing/checkout?tier=standard|family`, confirmed live). A visitor is promised a discount they cannot select — a broken promise on the page built to convert.
  - Change: add a monthly/annual segmented toggle that swaps the displayed prices and appends `&interval=annual`; map annual Stripe price ids (`STRIPE_PRICE_STANDARD_ANNUAL` / `_FAMILY_ANNUAL`) in `lib/billing/stripe.ts`; the webhook already derives tier from the price id, so no webhook change.
  - Risk: needs the two annual price ids provisioned in Stripe + Vercel; gate the toggle behind their presence so it degrades cleanly when unset (pilot mode). Confirm the 14-day trial still applies to the annual price.

- [ ] **F5 · feature · Downloadable mastery certificate on topic certification** (size: M)
  - The win: verified live — the "Topic mastered! … certified 🎉" trophy screen offers only "See it on my journey" / "Back to subjects"; the achievement evaporates. A one-tap "Save my certificate" that renders a dated, child-named certificate (topic, date, SHA-256 verification hash) gives families a tangible, printable reward and doubles as portfolio evidence — reusing the existing dossier-hash + Cloudinary infrastructure.
  - Change: a deterministic certificate builder (pure `lib/compliance/*` or a small route) → PNG/PDF via the existing media path; button on the `complete` branch of `practice-player.tsx`; surface it to the parent in `/portfolio`.
  - Risk: keep the child-facing surface free of PII beyond first name; generation is best-effort with a graceful 503 when media is unset (like other media paths).

- [ ] **F3 · feature · Deterministic math figures: multiplication-array / number-line + "fraction of an amount"** (size: M)
  - The win: extends the owner's accepted pattern (F1 fraction-bars, F2 percent-grid in `deriveMathVisual`). Two live gaps found this run: (1) simple facts like "7 × 8" fall through to a generic AI PNG; (2) **"What is ½ of 16?"** — a fraction-**of-an-amount** question — opens a *generic* "Think it through" strategy walkthrough in "See it", with no fraction/grouping visual. A derived dot-array (`a×b`), number line (`a±b`), and a "16 split into 2 groups of 8" grouping visual are clearer, free, always present, and get real descriptive alt text. Directly strengthens KS2 arithmetic — Ivy's band.
  - Change: add `array` / `number_line` / `groups` specs to `deriveMathVisual` (`src/lib/child/math-visual.ts`), parsing `a × b`, `a ± b`, and `½/⅓/¼ of N`; render in `MathVisual` (`src/components/child/math-visual.tsx`) with accent theming + honest alt; unit-test each deriver like the fraction path.
  - Risk: keep it a pure fallback in the existing chain (AI PNG still used when non-derivable); cap the rendered dot count (≤144) so big products don't explode the SVG.

- [ ] **F8 · feature · Spaced-repetition "Daily review" quest on the child hub** (size: M/L, ambitious)
  - The win: the retention loop is half-built and invisible to the child. `CompetenceDoc` already stores `next_review_at` / `review_interval_days`, and the progress map already tells the parent "review comes up in N days" — but a certified topic that's *due* never resurfaces as something to actually do. A calm **"Review" quest** on `/learn` that surfaces 1–3 due, already-certified topics (a short 3-question retrieval set, no re-teach) turns Edway from "march forward" into genuine mastery-with-retention — the single most evidence-backed lever for GCSE readiness, and fully deterministic (no AI, human-authored questions, no child profiling).
  - Change: a pure selector in `lib/engine/*` (`dueReviewTopics(competence, now)` → ordered by overdueness, capped) unit-tested; a repo wrapper (ownership-checked) that reads due topics for the active child; a "Review" card in `quest-cards.tsx` that launches a lightweight review flow (reuse `PracticePlayer` in a "review" mode that doesn't alter certification, only nudges `next_review_at` forward on success via the existing spaced-repetition schedule).
  - Risk: keep it additive to `todayCard` (don't crowd out new-topic quests); a passed review lengthens the interval, a missed one shortens it — reuse the existing interval math, don't invent a new schedule. Never demote a certified topic.

- [ ] **F7 · ui · Replace "Loading…" text with skeleton states (dashboard + lesson)** (size: S)
  - The win: on first paint both the dashboard and a child lesson flash a bare centred "Loading…" string before the RSC content streams (observed live on both). A generation-old affordance on the two primary surfaces. Skeleton cards (matching the StatCard / children-grid / lesson shapes) feel modern and cut perceived latency.
  - Change: add `loading.tsx` (or a Suspense fallback) under `(dashboard)/dashboard` and the `(child)/learn/lesson` route rendering shimmer skeletons in the real layout grid; reuse one small `<Skeleton>` primitive.
  - Risk: purely presentational; disable the shimmer under `prefers-reduced-motion`.

- [ ] **F9 · feature · In-lesson tap-to-define glossary for tricky words (SEND / accessibility)** (size: M, ambitious)
  - The win: builds on the just-shipped reading-help suite (easy-read font, ruler, read-aloud) with the next inclusion lever — comprehension, not just decoding. A child who can read "quarter" or "denominator" but doesn't *know* it stalls silently. A tap on a highlighted key term shows a short, human-authored, child-voice definition (optionally read aloud via the existing cached `/api/tts`). Genuinely differentiating for SEND + EAL learners and squarely on Edway's mission.
  - Change: add an optional `glossary?: {term,definition}[]` to the question/topic schema (human-authored data, never AI — same rule as questions); a small `<GlossaryTerm>` child component that wraps matched terms in the prompt/explainer with an accessible popover (keyboard + touch, `.child-touch` target), reusing `use-narration.ts` for read-aloud.
  - Risk: definitions are human-authored curriculum data (Checker/AI never author them — Children's Code); keep the popover keyboard-reachable and WCAG-contrast clean; degrade to plain text when a topic has no glossary.

---

## Summary
- Bugs: **2** (0 critical / 0 high / 0 med / 2 low)  ·  Features: **7**
- Static: type-check **PASS** · lint **PASS** · `/api/health` **200** `{ok, db:up}` · npm audit **5 high** (next / postcss / sharp / fast-uri — non-force fix available). Security headers all strong (CSP, HSTS 2yr, X-Frame DENY, nosniff, Permissions-Policy locked). Stack current: Next 15.5.18, React 19, Tailwind 4.3.3 (off beta).
- Site health: mature and well-hardened — zero console errors on any surface, no mobile overflow, data-silo intact, child lesson flow taken end-to-end with **no regression**. Carried-over **F4 (dyslexia reading mode) already shipped** and is dropped.
- Top 3 to ship: **F2** clear the 5 high CVEs (non-force `npm audit fix`, build-ready) · **F6** annual billing toggle (the advertised 17% is currently unreachable) · **F5** downloadable mastery certificate (reuses dossier-hash + media infra, delightful moment).
