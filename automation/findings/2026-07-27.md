# Scout findings — 2026-07-27

DECISION: all

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           → build EVERY item below (default)
    B1, F3, F5    → build ONLY these IDs
    skip          → build nothing tonight
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright  ·  Base URL: https://edway.uk  ·  Desktop innerWidth **1280** · Mobile innerWidth **390**

Coverage: marketing (`/`, pricing, how-it-works, safety, for-parents — all with a real `<h1>`, no overflow), auth (login/signup inspected — no junk submits), `/api/health` (**200** `{ok, db:up}`), parent dashboard + schedule + portfolio + tutoring, **child mode taken end-to-end** (Maths `maths_ks3_negatives` Learn→Practise→Mastery→**certified**, English + Science confirmed playable, journey map), **admin** (read-only: overview + finance), **tutor** console (read-only). Both viewports on every surface. Logged out of parent, admin AND tutor; browser closed.

## Live pass/fail — child mode (taken as a child, not eyeballed)

Drove the **Maths quest `maths_ks3_negatives` (Negative Numbers & Powers)** fully as **Ivy** — note she has cross-band progressed: age 10 but her Maths band is now KS3 (Year 7 topic), having certified all KS2 Maths. Desktop + mobile:
- **Explainer** ("Work out −3 + 7", number-line narrative, 3 steps + per-step narration + "Your turn: −5 + 8") — **PASS**.
- **Practice Q1 "What is −3 + 7?"** — deliberately wrong ×2: wrong#1 = calm name-nudge ("Ivy, not quite yet — have another go…"), one hint, "2 tries left", **no red/buzzer**; wrong#2 = **"See it" unlocks**, hint ladder escalates, "1 try left". Correct (4) = star burst + "Brilliant! 🎉" + "Keep going". **PASS**.
- **"See it"** opened Eddie's choreographed "Think it through" (Read → underline → test) walkthrough. Works, calm — **PASS** (but see F3: it's the *generic* strategy panel, not a number-line animation).
- **Practice Q2 "5 − 8" / Q3 "4²"** correct → "Amazing work! 🚀" → "Start mastery". **PASS**.
- **Mastery** ("−6 + (−4)", "2³", "−2 × 3") 3/3 correct → **"Topic mastered! … certified 🎉"** trophy (Maths journey now 3/14 mastered). **PASS**.
- English (`eng_ks2_reading` spelling) + Science both resolve to real playable KS2 lessons — no dead-walls. **PASS**.
- Phase bar (Learn → Practise → Mastery) lit correctly at each stage (F4, shipped). Auto-narration ("Unmute reading aloud" pressed), Speak/Listen present.

**Zero console errors on every surface** (marketing / dashboard / schedule / portfolio / tutoring / child lesson flow / journey map / admin overview + finance / tutor). **No horizontal overflow at 390px anywhere** (every surface measured scrollWidth 380 ≤ 390). Data-silo holds: the tutor account saw only "No sessions assigned yet" — no cross-family data. Prior fixes verified live this run: login says "Sign in to your Edway account" (2026-07-26 B2); "WEEK OF 27 JULY 2026" (B3); avg lesson time "4m · below the 45–60 min target" (B1); admin finance honest ("2 active subscribers", correct "1 account / 10 accounts" pluralization); tutor availability toggle + parent-facing notes (F9 tutor workspace). The test family now has **3 children** (Sam Smoke, Ivy Test, Sam Test) with inline child switchers on schedule/portfolio/tutoring.

---

## Bugs (Critical → Low)

Nothing critical/high this run — the codebase stays mature and clean. Three low, admin-facing / hygiene items:

- [x] **B1 · Low · Admin "Recent learning activity" shows raw topic slugs, not human titles** — `src/app/(admin)/admin/page.tsx:99`
  - Repro / evidence: admin overview lists activity as **"Maths Ks3 Negatives · 100%"**, **"Sci Ks3 Cells"**, **"Eng Ks3 Reading"**, **"Maths Number"** — the DB `topic_tag` title-cased, not the authored topic title ("Negative Numbers & Powers", "Living Things", "Reading & Spelling"). Staff read machine ids instead of real names.
  - Root cause: `{l.topic_tag.replace(/_/g, " ")}` (line 99) prints the slug directly with `capitalize`, never looking up `curriculum_topics.title`.
  - Fix: map `topic_tag → title` from the topics collection (repo already reads topics) and render the title, falling back to the humanised slug when a legacy tag has no match.
  - Risk: admin-only, read path; keep the fallback so unknown/legacy tags still render.

- [x] **B2 · Low · Admin metric subtitle says "completed sessions" (banned glossary synonym)** — `src/app/(admin)/admin/page.tsx:61`
  - Repro / evidence: the "Lessons this week" metric card hint reads **"completed sessions"**. `docs/ARCHITECTURE.md` glossary explicitly bans "session" for the learning unit (reserved only for auth/JWT); it was swept from the parent dashboard but survives here in admin product UI.
  - Root cause: `hint="completed sessions"` — stale copy.
  - Fix: change to `hint="completed this week"` (or "completed lessons"). One-word copy change, no logic.
  - Risk: none — presentational.

- [x] **B3 · Low · `/favicon.ico` 404 on raw (non-HTML) responses** — `public/` (no `favicon.ico`)
  - Repro / evidence: loading `/api/health` (a raw JSON response with no HTML `<head>`) makes the browser request `/favicon.ico`, which **404s** (console: "Failed to load resource: 404 … /favicon.ico"). App pages declare `favicon.svg` / `favicon-32.png` so they're fine, but any direct hit or uptime-monitor/bot on `/favicon.ico` gets a 404.
  - Root cause: prior runs generated `favicon-32.png` / `apple-touch-icon.png` etc. but never a root `favicon.ico`.
  - Fix: add `public/favicon.ico` (generate from `favicon.svg` with the already-installed `sharp`, like the other icons) — the classic default path then 200s everywhere.
  - Risk: trivial static asset; no code path.

## Features & upgrades (High value → Low)

- [x] **F3 · feature · Deterministic math figures: number-line (± / negatives), multiplication-array, and "fraction of an amount"** (size: M) — *carried over, re-confirmed with fresh live evidence*
  - The win: extends the owner's accepted pattern (F1 fraction-bars, F2 percent-grid in `deriveMathVisual`). This run's Maths lesson made the gap vivid: the **negatives** topic (`maths_ks3_negatives`) explainer *describes* "start at −3 on a number line and move 7 right" but renders only a "Direction: right = add" text chip — no number line. Practice "5 − 8" fell through to a **generic placeholder image** with the weak alt *"Helpful visual for this mathematics question."* And "See it" is the generic "Think it through" strategy panel, not a number-line animation. A derived number line (`a ± b`), dot-array (`a × b`), and grouping visual (`½/⅓/¼ of N`) are clearer, free, always present, and get honest descriptive alt text. Directly strengthens KS2/KS3 arithmetic — Ivy's live band.
  - Change: add `number_line` / `array` / `groups` specs to `deriveMathVisual` (`src/lib/child/math-visual.ts`, today only derives `fraction` + `percent`), parsing `a ± b` (incl. negatives), `a × b`, and `½/⅓/¼ of N`; render in `MathVisual` (`src/components/child/math-visual.tsx`) with accent theming + real alt; unit-test each deriver like the fraction path.
  - Risk: keep it a pure fallback in the existing chain (AI/placeholder image only when non-derivable); cap rendered marks (dots ≤144, number-line span sane) so big values don't explode the SVG; reduced-motion static.

- [x] **F8 · feature · Spaced-repetition "Daily review" quest on the child hub** (size: M/L, ambitious) — *carried over*
  - The win: the retention loop is half-built and invisible to the child. `CompetenceDoc` already stores `next_review_at` / `review_interval_days`, but a certified topic that's *due* never resurfaces as something to actually do (confirmed this run: the journey map surfaced no "review in N days", and there's no review quest on `/learn`). A calm **"Review" quest** that surfaces 1–3 due, already-certified topics (a short 3-question retrieval set, no re-teach) turns Edway from "march forward" into mastery-with-retention — the single most evidence-backed lever for GCSE readiness, fully deterministic (no AI, human-authored questions, no profiling).
  - Change: a pure selector in `lib/engine/*` (`dueReviewTopics(competence, now)` → ordered by overdueness, capped) unit-tested; an ownership-checked repo wrapper reading due topics for the active child; a "Review" card in the child hub quest list that launches a lightweight review flow (reuse `PracticePlayer` in a "review" mode that doesn't alter certification, only nudges `next_review_at` forward via the existing schedule).
  - Risk: additive to `todayCard` (don't crowd out new-topic quests); passed review lengthens the interval, missed one shortens it — reuse existing interval math, never demote a certified topic.

- [ ] **F5 · feature · Downloadable mastery certificate on topic certification** (size: M) — *carried over, re-confirmed live*
  - The win: verified again this run — the "Topic mastered! … certified 🎉" trophy screen offers only "See it on my journey" / "Back to subjects"; the achievement evaporates. A one-tap "Save my certificate" rendering a dated, child-named certificate (topic, date, SHA-256 verification hash) gives families a tangible, printable reward and doubles as portfolio evidence — reusing the dossier-hash + Cloudinary infrastructure.
  - Change: a deterministic certificate builder (pure `lib/compliance/*` or a small route) → PNG/PDF via the existing media path; button on the `complete` branch of `practice-player.tsx`; surface it to the parent in `/portfolio`.
  - Risk: keep the child-facing surface free of PII beyond first name; generation best-effort with a graceful 503 when media is unset (like other media paths).

- [ ] **F7 · ui · Replace "Loading…" text with skeleton states (dashboard + lesson)** (size: S) — *carried over, re-confirmed live*
  - The win: caught live again — navigating to `/dashboard` flashes a bare centred **"Loading…"** string before the RSC streams; `/admin/finance` flashes **"INITIALISING"**. A generation-old affordance on primary surfaces. Skeleton cards (matching the StatCard / children-grid / lesson shapes) feel modern and cut perceived latency.
  - Change: add `loading.tsx` (or a Suspense fallback) under `(dashboard)/dashboard` and the `(child)/learn/lesson` route rendering shimmer skeletons in the real layout grid; reuse one small `<Skeleton>` primitive.
  - Risk: purely presentational; disable the shimmer under `prefers-reduced-motion`.

- [ ] **F9 · feature · In-lesson tap-to-define glossary for tricky words (SEND / EAL)** (size: M, ambitious) — *carried over*
  - The win: builds on the shipped reading-help suite (easy-read font, ruler, read-aloud) with the next inclusion lever — comprehension, not just decoding. A child who can read "denominator" or "quotient" but doesn't *know* it stalls silently. A tap on a highlighted key term shows a short, human-authored, child-voice definition (optionally read aloud via the cached `/api/tts`). Differentiating for SEND + EAL and squarely on Edway's mission.
  - Change: add an optional `glossary?: {term,definition}[]` to the question/topic schema (human-authored data, never AI — same rule as questions); a small `<GlossaryTerm>` child component wrapping matched terms in the prompt/explainer with an accessible popover (keyboard + touch, `.child-touch` target), reusing `use-narration.ts` for read-aloud.
  - Risk: definitions are human-authored curriculum (Checker/AI never author them — Children's Code); keep the popover keyboard-reachable + WCAG-contrast clean; degrade to plain text when a topic has no glossary.

- [ ] **F10 · feature · Child "achievement shelf" — certified topics become collectible badges on My journey** (size: M, ambitious) — *new*
  - The win: certifications are currently just an incremented count ("3 of 14 mastered") in a plain list. Children respond to *visible, collectible* progress. A warm "shelf" of earned badges (one per certified topic, with the topic's accent + a subtle earned-date) on `/learn/map` turns each certification into something to look back on and collect — a motivational loop that's fully deterministic, no analytics, no profiling (Children's Code safe). Pairs naturally with F5 (each badge → save certificate) and F8 (a reviewed badge glows "kept sharp").
  - Change: a pure `earnedBadges(competence)` selector in `lib/engine/*` (certified topics → {topic, title, accent, certified_at}) unit-tested; a `<AchievementShelf>` component on the journey map rendering earned vs locked slots; reuse the existing accent + Celebration vocabulary. Read-only over `CompetenceDoc` (certification timestamps already stored) — no schema change beyond surfacing.
  - Risk: keep it additive to the existing map (don't replace the topic list); locked slots must never feel punitive (calm "not yet", never red); child-facing copy stays warm.

- [ ] **F11 · feature · Printable / PDF weekly plan for `/schedule`** (size: M) — *new*
  - The win: homeschool parents keep paper records and like a fridge-pinnable week. `/schedule` shows a rich per-day plan (subject, topic, data-grounded "Why") but there's no way to print or export it. A "Print this week" / "Download PDF" that renders the approved plan (child name, week-of date, per-day subject+topic+reason) reuses the portfolio PDF/media infra and doubles as lightweight LA evidence of planned provision.
  - Change: a print-optimised route or a deterministic PDF builder (reuse the portfolio/dossier PDF path in `lib/compliance/*`), fed by the existing `WeeklyScheduleDoc`; a "Print / Download" button on `/schedule`. A pure `@media print` stylesheet is the smallest first cut; PDF export the richer version.
  - Risk: parent-scoped (ownership-checked read of the schedule); keep it best-effort with graceful 503 when media is unset; no child PII beyond first name on the sheet.

---

## Summary
- Bugs: **3** (0 critical / 0 high / 0 med / 3 low)  ·  Features: **7**
- Static: type-check **PASS** · lint **PASS** · `/api/health` **200** `{ok, db:up}` · `npm audit` = **9 high but all dev-only** (eslint toolchain `minimatch`/`brace-expansion` DoS — no fix published; the prod-runtime CVEs next/postcss/sharp/fast-uri were cleared by 2026-07-26 F2 and stay clear — `overrides` `{postcss:$postcss, sharp:^0.35.0}` intact). Security headers strong; stack current (Next 15.5.18, React 19, Tailwind 4.3.3).
- Site health: **mature and clean** — zero console errors on any surface (only latent 404 is `/favicon.ico` on raw JSON responses, B3), no mobile overflow, data-silo intact across parent/admin/tutor, child Maths lesson taken end-to-end with **no regression**. Carried-over F4 (dyslexia mode) & F6 (annual billing) & F2 (CVEs) all remain shipped.
- Top 3 to ship: **F3** number-line/array/fraction-of-amount math figures (live gap re-confirmed on the negatives lesson; extends the accepted deriver pattern) · **F8** spaced-repetition Daily review quest (the retention loop is built in data but invisible to the child) · **F5** downloadable mastery certificate (reuses dossier-hash + media infra; delightful, tangible reward).
