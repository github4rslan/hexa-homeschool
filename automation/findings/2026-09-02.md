# Scout findings — 2026-09-02

DECISION: all

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           → build EVERY item below (default)
    B1, F2, F5    → build ONLY these IDs
    skip          → build nothing tonight
  Leave it as `all` and do nothing to run fully autonomous.
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright (MCP tools only) · Base URL: https://edway.uk

Day-of-week focus: **Wednesday, delight/animation deep-dive**, plus the standing
max-depth child pass and every-day lanes (bugs, curriculum, journeys) regardless
of focus.

**IMPORTANT CARRYOVER NOTE:** the 2026-09-01 findings file (B1 perf waterfall
Critical, B2 homepage hydration Critical/High, B3 negative-array visual Medium,
F1–F4) still has every checkbox unticked and no build commits followed it —
Mechanic appears not to have run against it yet, and Mechanic only reads
*today's* file (falling back to the most recent only if today's is missing), so
once this file exists that report would be silently skipped. Re-verified this
run: B1 (perf) is confirmed **still open**, fresh evidence below as this run's
own B1. B2 (homepage hydration #418) did **NOT reproduce** this run across 3
fresh navigations at both viewports (zero console errors) — noted as
resolved/not-reproducing rather than carried forward; worth one more spot-check
next run before fully retiring EPIC 20. B3 (deriveArray negative-multiplication)
and F1–F4 were not independently re-verified yet this run — see below.

**Owner's signup-email-delivery follow-up (this run's first priority):**
Actually drove a real `/signup` submission end-to-end with a fresh disposable
mailinator.com address, not just eyeballed the form. Result: **PASS, email
delivery is healthy.** Filled the form → submitted → landed cleanly on
`/signup/verify` with zero console errors. Checked the actual mailinator public
inbox: the verification email arrived within ~8 seconds, from `info@edway.uk`,
subject "915333 is your Edway verification code", full correct branded HTML
body with the real 6-digit code (not spam-folder-mangled, not a bounce/rejection
notice). Entered the real code back on `/signup/verify` → verified successfully
→ landed on `/onboarding`, zero console errors. This is the exact end-to-end
path that silently failed for 5+ days per the 2026-09-02 memory entry — today
it works cleanly. Cleaned up: deleted the disposable test parent (0 children)
via a throwaway `scripts/.tmp-cleanup-scout-signup-test.ts` (same pattern as
`scripts/delete-users.ts`), deleted the script immediately after running it, no
`.env.local` values were ever printed. Did not additionally probe Brevo's
`/v3/smtp/statistics/events` this run since the mailinator inbox itself is
direct, stronger proof of actual delivery (not just Brevo's queue-acceptance
response) — recommend this mailinator-style check as the standing method going
forward, it's a strictly better signal than the API spot-check.

**Credential-handling self-flag (process note, not a product bug):** early in
this run, `browser_navigate` to `/login` auto-attached a snapshot that happened
to include the browser profile's persisted-autofill SMOKE credentials in
plaintext (both email and password), and I read that snapshot file via `Read`
before realising — a violation of the "never snapshot a live password field"
rule. Corrective action taken immediately: switched to ref-only clicks with no
further snapshot reads of that page for the rest of the run, and the value is
not reproduced anywhere in this report. Flagging for the record per the
instruction to be transparent about this; no credential was written to any
committed file. Recommend future runs check `browser_navigate`'s own returned
snapshot summary path/content BEFORE opening it with `Read` when a login page
is reached with a persisted profile, or resize/navigate away first.

---

## Bugs (Critical → Low)

- [ ] **B1 · Critical (carried over from 2026-09-01, still unfixed) · `/dashboard`, `/schedule` still block full page load for multiple seconds via a serial await-waterfall in the Server Component** — `src/app/(dashboard)/dashboard/page.tsx`, `src/app/(dashboard)/schedule/page.tsx`, `src/app/(child)/learn/page.tsx`
  - Repro / evidence: re-measured fresh this run via `performance.getEntriesByType('navigation')[0]` as the SMOKE parent (`responseEnd - responseStart` = server-side think time): `/dashboard` fresh `browser_navigate` load → **7,026ms** gap (`responseStart` 478ms → `responseEnd` 7,505ms). `/schedule` fresh load → **1,711ms** gap. Confirmed via `grep -c "await "`/`grep -n "Promise.all"` that the code is unchanged since 2026-09-01's original finding: `dashboard/page.tsx` still has only the same two pre-existing `Promise.all` call sites (per-child loop, `todayCards`) and none of the other ~8 independent sibling `await`s (`logs`, `certifiedThisWeek`, `escalations`, `feedItems`, `complianceReady`, `dismissed`, `fbContext`, `masteredToday`) have been batched.
  - Root cause: unchanged from 2026-09-01's finding — see that report (or `automation/backlog.md` EPIC 19) for the full line-by-line breakdown. Sequential independent MongoDB round-trips inside one Server Component, not a DB capacity problem (`/settings` ~600ms, `/admin` ~37ms, `/pricing` ~3ms all stay fast).
  - Fix: batch every set of independent sibling `await`s into `Promise.all([...])` in each of the three files, extending the pattern the code already uses correctly for the per-child array and `todayCards`. Re-measure with the same Performance-API technique after the fix (target: under ~1.5s, matching `/settings`'s baseline).
  - Risk: Low functionally (reordering independent reads), but this is the single highest-traffic surface in the product — run full gate + a live re-measurement on all three pages, not just the type-check/build gate.

- [ ] **B2 · Medium (carried over from 2026-09-01, not independently re-verified this run) · `deriveArray` math-visual ignores a leading minus sign on multiplication** — `src/lib/child/math-visual.ts:267`
  - Repro / evidence: not re-driven live this run (time budget went to the Wednesday delight lane + fresh journeys instead); carried forward from 2026-09-01's own live repro (Sam, `maths_ks3_negatives` mastery Q3 "What is −2 × 3?", rendered figure/alt asserting "2 × 3 = 6" for a −6-answer question) since the source line is confirmed unchanged (`grep -n "deriveArray" -A15 src/lib/child/math-visual.ts` still shows the bare `/(\d+)\s*[×x*]\s*(\d+)/` regex with no `-?` on either capture group, and no negative-case test for this prompt in `math-visual.test.ts` yet).
  - Root cause / Fix / Risk: as filed 2026-09-01 — see `automation/findings/2026-09-01.md` B3 for the full text; the fix pattern (mirror `deriveNumberLine`'s `(-?\d+)` handling a few lines above, or return `null` and fall back to the decorative AI figure when either operand is negative) is unchanged and still the right call.

## Features & upgrades (High value → Low)

- [ ] **F1 · carryover, unshipped (from 2026-09-01) · `maths_inequalities` tier-2 entry-level question** (size: S)
  - Not independently re-authored this run — the exact `SeedQuestion` is already fully written out in `automation/findings/2026-09-01.md` F1 (Edexcel 1MA1 A22, "Solve the inequality x + 4 < 9"). Carrying forward unchanged since it was never seeded (owner approval gate still applies) and `automation/backlog.md` EPIC 2 still names this as the open gap.

- [ ] **F2 · carryover, unshipped (from 2026-09-01) · `sci_ecology` second command-word question** (size: S)
  - Not independently re-authored this run — full `SeedQuestion` already written in `automation/findings/2026-09-01.md` F2 ("Explain why energy transfer... is never 100% efficient"). Carrying forward unchanged, unseeded.

---

## Summary
- Bugs: (see below, filled in as the run continues) · Features: (see below)
- Top 3 to ship: (filled in at the end of the run)
