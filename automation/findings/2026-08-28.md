# Scout findings — 2026-08-28

DECISION: all

<!--
  How to steer tonight's Mechanic run (edit the line above during the day):
    all           → build EVERY item below (default)
    B1, F2, F5    → build ONLY these IDs
    skip          → build nothing tonight
  Leave it as `all` and do nothing to run fully autonomous.
  IDs: B# = bug fix, F# = feature / upgrade. Checkboxes are set by Mechanic.
-->

Runtime: playwright (MCP tools only) · Base URL: https://edway.uk

Day-of-week focus: Friday → polish rubric (B-polish) deep-dive, plus the standing max-depth child pass (all 4 interaction types + resilience) and every-day lanes (bugs, curriculum, security, delight) regardless of focus. No owner priority idea was supplied this run — ranking below is Scout's own judgement of value. Yesterday's 2026-08-27 report (4 bugs + 8 features, 12 items) was fully shipped by Mechanic overnight — re-verified several of those fixes live this run (see notes inline) and did not re-propose anything from it.

Desktop pass confirmed at `innerWidth` **1280** (`browser_resize` 1280×800). Mobile pass confirmed at `innerWidth` **390**, `scrollWidth` **380** (no horizontal overflow) on `/`, `/dashboard`, `/learn` hub and a lesson page. Whole-page scroll confirmed via full accessibility-tree snapshots (footer content present) on marketing pages.

Credential handling: all test-account credentials (`SMOKE_EMAIL/PASSWORD`, `ADMIN_EMAIL/PASSWORD`, `TUTOR_EMAIL/PASSWORD`) were read from `.env.local` via a single targeted `grep` per account, directly into this session's context, then typed straight into `browser_type`/`browser_type`-equivalent fill actions — never written to any file. One incidental exposure: the `/login` page's browser-profile autofill (a persisted pattern noted in prior runs) surfaced the SMOKE parent's plaintext password in an early accessibility snapshot before I registered the pattern this run; I did not repeat it (switched to submitting the pre-filled form via a CSS selector click with no further snapshot of the password field, and typed the admin/tutor credentials directly into selectors without ever re-snapshotting a filled password field). Noted in memory for future runs to avoid entirely by clearing the password field or navigating with a fresh incognito-style context before snapshotting `/login`. All screenshots were taken to `.playwright-mcp/`-relative paths and deleted before finishing; `git status --porcelain` confirmed clean.

---

## Bugs (Critical → Low)

- [x] **B1 · Critical/High · The English "plural of X" derived visual teaches the WRONG spelling rule for irregular plurals — live-reproduced on the exact question a child answers, actively contradicting the correct MCQ option** — `src/lib/child/english-visual.ts:43-58` (`pluralRuleFor`) and `:72-84` (`deriveEnglishVisual`'s plural branch)
  - Repro / evidence: as Ivy, reached the third mastery question of `eng_grammar`: *"Pick the correct plural of 'child'."* (options: childs / **children** / childes / childrens — correct is B, "children", an irregular plural). The question figure rendered word-building tiles reading **"c h i l d + s"** with the caption **"Most words just add s"** — directly asserting the regular +s rule for a word whose correct plural is the irregular "children", visually endorsing the WRONG answer (A, "childs") sitting right next to the correct one. Screenshot reviewed live before cleanup (`.playwright-mcp` scratch, deleted). A second live instance exists in the bank: `curriculum.seed.bands.ts:320`, *"The plural of 'leaf' is:"* (correct: **leaves**) — `pluralRuleFor("leaf")` also falls through to the default "add s" rule (ends in neither `ch/sh/s/x/z` nor consonant+y), so this question's figure would show "leaf + s" / "Most words just add s" too, again contradicting its own correct answer.
  - Root cause: `pluralRuleFor` (lines 48-58) is a purely suffix-based heuristic (`ch/sh/s/x/z` → `+es`; consonant+`y` → `+ies`; else → `+s`) with **zero knowledge of English irregular plurals** (child→children, leaf→leaves, man→men, woman→women, mouse→mice, foot→feet, tooth→teeth, person→people, goose→geese, ox→oxen, knife→knives, wife→wives, life→lives, wolf→wolves, half→halves, shelf→shelves, thief→thieves…) or of the `-f/-fe → -ves` pattern. Every "plural of X" prompt in the bank is routed through it with no exception list, so any irregular base silently gets a confidently-wrong "just add s/es/ies" figure instead of the file's own documented safe fallback (returning `null` so the caller shows the decorative AI image, "never a wrong or misleading figure" per the file's own doc comment at line 12).
  - Fix: add a small curated exclusion Set of known irregular/exception bases (at minimum `child`, `leaf`, plus the common GCSE/KS2-relevant list above and the `-f/-fe` family) checked at the top of the plural branch in `deriveEnglishVisual`; when `base` (lower-cased) is in that set, return `null` immediately (matches the file's own existing safe-fallback contract — no code path currently does this, so it's a net-new guard, not a refactor of working logic). Add unit tests asserting `deriveEnglishVisual("eng_grammar", "Pick the correct plural of 'child'.")` returns `null`, and that a genuinely regular word (e.g. "box", "church", "baby") still returns the correct `plural_rule` spec (both already covered by the module's existing tests — verify they still pass).
  - Risk: low — an additive early-return guard in one pure, already-unit-tested function; verify live that the "plural of child" and "plural of leaf" questions now render the plain (non-misleading) decorative fallback instead of the wrong tile figure, and that "plural of box"/"plural of church"/"plural of baby" (all regular) are unaffected.

- [x] **B2 · Medium/High · The parent dashboard's "today's quest" checklist shows an already-certified topic as an outstanding, unchecked to-do — live-reproduced, and directly undermines the "prove with evidence" promise for the parent overseeing GCSE readiness** — `src/lib/db/repo.ts:1763-1837` (`todayCard`)
  - Repro / evidence: Ivy certified **Inference & Language** (`eng_ks3_reading`) yesterday (27 Aug — confirmed via her certificate: "Awarded 27 August 2026", and the dashboard's own "Recent activity" feed: "Mastered Inference & Language · Ivy · first try 🎉 · Yesterday"). Her weekly plan (approved for the week of 24-30 Aug) still assigns `eng_ks3_reading` to Tuesday AND Friday. Today (Friday), the parent dashboard's "Good afternoon" summary read **"Ivy: 1 quest left today"** and her per-child card listed **"Inference & Language · English"** as an unchecked, clickable, non-strikethrough row (`components/dashboard/today-card.tsx:104-108`) — exactly as if it were still to do. Meanwhile Ivy's OWN child hub (`/learn`), computed independently, correctly shows the real next uncertified GCSE topic (`eng_grammar`, "Start here · today's plan") — so the parent and the child are shown two DIFFERENT "today's task" topics for the same subject, and the parent's is the stale, already-mastered one. Re-confirmed a second time later in the session after approving a fresh plan for Sam Test (a different child, same symptom class): the "1 quest left today" line and "Inference & Language" row for Ivy were still present.
  - Root cause: `todayCard`'s quest-building loop (lines 1794-1801) computes `done: doneTags.has(it.topic_tag)`, where `doneTags` comes from `todaysCompletedTopicTags` (line 1664) — a set of topics with a `LessonLogDoc` completed **strictly today** (`timestamp_end >= londonDayStart()`). It never checks the topic's actual competence state, even though the function already fetches full `CompetenceDoc`s into `comps` (line 1773) for a *different* purpose (the `certifiedToday` line and `pausedTags`). A topic certified on any PRIOR day (yesterday, or earlier in the week before a plan edit) is therefore always shown as "not done" for every day the (unedited) plan still lists it, regardless of how long ago it was actually mastered.
  - Fix: build a `certifiedTags = new Set(comps.filter(c => c.state === "certified").map(c => c.topic_tag))` alongside the existing `pausedTags`, and change the quest map to `done: doneTags.has(it.topic_tag) || certifiedTags.has(it.topic_tag)`. This makes the dashboard checklist and the "N quests left today" summary agree with the topic's real state, not just "completed in the last 24h". No schema change; `comps` is already fetched in this exact function.
  - Risk: low — a pure boolean-OR addition using data already in scope; add a unit-equivalent check (or a small repo-level test if one exists for `todayCard`) asserting a quest whose topic is `certified` (but not completed today) reports `done: true`. Live-verify on Ivy's dashboard that "Inference & Language" now shows checked/strikethrough (or the card reaches its "All done for today" state if that was her only remaining quest).

- [x] **B3 · Medium · A "Linear Algebra" mastery question is actually a Quadratics-topic skill (expanding double brackets to get an x² term) — duplicates content that already exists, correctly scoped, under the separate `maths_quadratics` topic one grade-band higher** — `src/lib/data/curriculum.seed.ts:219`
  - Repro / evidence: live-reproduced while re-attempting Ivy's already-certified **Linear Algebra** (`maths_algebra_linear`, Grade 4-5, summary "Expressions, linear equations and rearranging formulae") mastery check — question 3 read *"Expand (x + 2)(x + 3)."* (options: **x² + 5x + 6** / x² + 6x + 5 / x² + 5x + 5 / x² + 6). Expanding a double bracket produces a QUADRATIC expression (an x² term) — this is explicitly the subject of the separate `maths_quadratics` topic (Grade 5-7, summary "**Expanding**, factorising and solving quadratic equations"), which already has its OWN, nearly-identical question testing the exact same skill: `curriculum.seed.extra.ts:59`, *"Expand (x + 5)(x − 2)."* → `x² + 3x − 10`. So a child working through the LOWER-graded, prerequisite topic (Linear Algebra explicitly lists `maths_geometry`-style single-bracket expansion in its own pool — line 216, `"Expand 2(x + 3)."` — but this one item jumps straight to double-bracket/quadratic expansion) meets content one grade-band ahead of what the topic promises, duplicated with the topic that's supposed to own it.
  - Root cause: authoring/categorisation slip in the original seed data — this one tuple was placed in the `maths_algebra_linear` array (line 214-219) instead of `maths_quadratics` (line 233 onward, same file). It correctly re-derives (`x·x + 3x + 2x + 6 = x² + 5x + 6` ✓) so it is not a correctness bug — it is a topic/grade-band placement bug.
  - Fix: remove this tuple from `maths_algebra_linear`'s array (line 219) — `maths_quadratics` already tests this exact skill with a different worked example, so no replacement is needed in Linear Algebra. Because the seed script upserts by natural key (`topic_tag` + `prompt`) and never deletes, simply deleting the line will leave the existing `{topic_tag: "maths_algebra_linear", prompt: "Expand (x + 2)(x + 3)."}` row **orphaned** in the live `questions` collection — Mechanic should follow the established retire pattern (per `automation/backlog.md` EPIC 1: delete the specific orphaned document by its natural key after removing it from the seed file, not just reword it) rather than leave it live but unseeded.
  - Risk: low — a one-line removal from a seed array plus one explicit orphan-delete on the natural key; verify post-fix that `maths_algebra_linear`'s mastery pool no longer contains an x² question and that `maths_quadratics` is unaffected (its own, separate expand-brackets item stays exactly as-is).

## Features & upgrades (High value → Low)

- [x] **F1 · curriculum (spec gap, EPIC 3) · A new `maths_simultaneous` topic — Edexcel 1MA1 spec point A19/A20 (solving pairs of linear equations algebraically) currently has ZERO coverage anywhere in the question bank** (size: M)
  - The win: `grep -in "simultaneous" src/lib/data/curriculum.seed*.ts` returns nothing at all. Simultaneous equations is one of the most reliably-examined Higher-tier (and increasingly Foundation-tier) Edexcel 1MA1 algebra topics, and it is the natural next slice named as EPIC 3's own "next step" in `automation/backlog.md` after `maths_transformations` closed the same class of gap last run. Follows the exact accepted precedent (topic entry + worked example + 3 checked starters).
  - Topic entry (for `curriculum.seed.ts`'s `SEED_TOPICS`, after `maths_transformations`, order 14):
    ```
    { subject: "mathematics", topic_tag: "maths_simultaneous", title: "Simultaneous Equations", summary: "Solving pairs of linear equations algebraically and graphically.", key_stage: 4, working_grade_band: "Grade 5–7", order: 14, prerequisite_tags: ["maths_algebra_linear", "maths_graphs"] },
    ```
  - Worked example (for `WORKED_EXAMPLES_BY_TOPIC`):
    ```
    maths_simultaneous: {
      title: "Solve x + y = 7 and x − y = 1",
      scenario: "Two numbers add to 7. Their difference is 1. Find both numbers.",
      steps: [
        { line: "Add the two equations together — this cancels out y.", visual: { label: "Add", value: "2x = 8" } },
        { line: "Divide both sides by 2 to find x.", visual: { label: "x", value: "4" } },
        { line: "Substitute x = 4 back into the first equation: 4 + y = 7, so y = 3.", visual: { label: "y", value: "3" } },
      ],
      yourTurn: "Try solving x + y = 10 and x − y = 2.",
    },
    ```
  - Starter questions (verbatim `SeedQuestion` shape, all hand re-derived and checked):
    ```
    {
      topic_tag: "maths_simultaneous",
      subject: "mathematics",
      tier: 3,
      key_stage: 4,
      kind: "practice",
      prompt: "Solve the simultaneous equations: x + y = 9 and x − y = 3. Find the value of x.",
      options: ["x = 6", "x = 3", "x = 9", "x = 12"],
      correct_index: 0,
      explanation: "Add the two equations to eliminate y: (x + y) + (x − y) = 9 + 3, so 2x = 12, and x = 6.",
      hints: ["Add the two equations together — the y terms will cancel out.", "2x = 9 + 3 = 12. Now divide both sides by 2."],
      misconceptions: ["", "This is the value of y once you substitute back (9 − 6 = 3), not x.", "This is the right-hand side of the first equation (9), not the solved value of x.", "This is 2x (12) before dividing by 2 — you still need the final step."],
    },
    {
      topic_tag: "maths_simultaneous",
      subject: "mathematics",
      tier: 4,
      key_stage: 4,
      kind: "practice",
      prompt: "Solve the simultaneous equations: y = x + 2 and 2x + y = 11. Find the value of x.",
      options: ["x = 3", "x = 5", "x = 4", "x = 9"],
      correct_index: 0,
      explanation: "Substitute y = x + 2 into the second equation: 2x + (x + 2) = 11, so 3x + 2 = 11, giving 3x = 9 and x = 3.",
      hints: ["Replace y in the second equation with the expression 'x + 2' from the first equation.", "2x + x + 2 = 11 simplifies to 3x + 2 = 11 — solve for x from there."],
      misconceptions: ["", "This is the value of y once you substitute back (x + 2 = 3 + 2 = 5), not x.", "A slip when simplifying 3x + 2 = 11 — go through each step again.", "This is 3x (9) before dividing by 3 — you still need the final step."],
    },
    {
      topic_tag: "maths_simultaneous",
      subject: "mathematics",
      tier: 5,
      key_stage: 4,
      kind: "mastery",
      prompt: "Which pair of values satisfies BOTH simultaneous equations: 3x + y = 9 and x + 2y = 8?",
      options: ["x = 2, y = 3", "x = 3, y = 2", "x = 1, y = 6", "x = 4, y = −3"],
      correct_index: 0,
      explanation: "Check each equation: 3(2) + 3 = 9 ✓ and 2 + 2(3) = 8 ✓. Both equations are satisfied only by x = 2, y = 3.",
      hints: ["Test each pair in BOTH equations — a solution must satisfy both, not just one.", "Try x = 2, y = 3 first: does it work in the first equation? Does it also work in the second?"],
      misconceptions: ["", "This pair fails the first equation: 3(3) + 2 = 11, not 9.", "This pair satisfies the first equation (3(1) + 6 = 9) but fails the second: 1 + 2(6) = 13, not 8.", "This pair satisfies the first equation (3(4) − 3 = 9) but fails the second: 4 + 2(−3) = −2, not 8."],
    }
    ```
  - Risk: none at authoring stage — hand re-derived and checked (2x=12→x=6,y=3 ✓; 3x=9→x=3,y=5 ✓; the verify-style mastery item checked all four candidate pairs against both equations by hand). Exactly one defensible correct option in each. Owner approval gate via `DECISION:` still applies before seeding; Mechanic should also wire `maths_simultaneous` into any hard-coded subject-topic-count lists (mock-gate floor, roadmap ordering) the same way `maths_transformations` was wired in last run.

- [x] **F2 · curriculum (real spec gap inside an existing topic) · `maths_statistics` has zero combined/dependent-event probability content — the classic "pick two, without replacement" tree-diagram style question, Edexcel 1MA1 P8/P9** (size: S)
  - The win: read the full live question bank for `maths_statistics` via the admin Curriculum CMS (8 questions total: mean, range, mode, median, one simple single-event probability "work out", "impossible event" fact, and two P(single roll) items). Every one of them is a SINGLE-stage calculation — none test combining two events (multiplying along branches, adjusting the denominator when an item isn't replaced), which is a core, heavily-examined GCSE Statistics & Probability skill and currently has NO representation anywhere in the bank. This also gives the topic its second genuine command-word ("Work out") item at mastery tier, closing its entry on `automation/backlog.md` EPIC 2's remaining single-coverage list with a real new sub-skill rather than a rephrasing.
  - Question (verbatim `SeedQuestion` shape):
    ```
    {
      topic_tag: "maths_statistics",
      subject: "mathematics",
      tier: 5,
      key_stage: 4,
      kind: "mastery",
      prompt: "A bag contains 4 red counters and 6 blue counters. A counter is picked at random and NOT replaced. A second counter is then picked. Work out the probability that both counters are blue.",
      options: ["1/3", "9/25", "3/5", "5/9"],
      correct_index: 0,
      explanation: "P(first blue) = 6/10. Since the counter isn't replaced, P(second blue) = 5/9. Multiply along the branches: 6/10 × 5/9 = 30/90 = 1/3.",
      hints: ["Multiply the probability of the first event by the probability of the second, remembering one counter is now gone.", "After picking one blue counter, there are 9 counters left and only 5 are blue."],
      misconceptions: ["", "This treats the counter as replaced (6/10 × 6/10 = 9/25) — but it was NOT replaced, so the second fraction must change.", "This is only the probability of the FIRST pick being blue, not both picks together.", "This is only the probability of the SECOND pick being blue (given the first was already blue), not the combined probability of both."],
    }
    ```
  - Risk: none at authoring stage — hand re-derived (6/10 × 5/9 = 30/90 = 1/3 ✓; with-replacement trap 6/10×6/10=9/25 ✓ as a genuine, common distractor). Owner approval gate applies.

- [x] **F3 · feature (ties directly to B2) · A distinct "review" framing when a child re-enters Practise/Mastery on a topic they've ALREADY certified, so re-doing a topic never looks or feels identical to earning it for the first time** (size: M)
  - The win: live-reproduced this run — re-attempting Ivy's already-certified `maths_algebra_linear` (via the hub's leftover "Pick up where you left off" resume, and separately via "Practice more" links elsewhere on the hub) shows the EXACT same "Today's lesson" explainer copy and, on a perfect run, the EXACT same "Topic mastered! 🏆 ... this topic is certified! 🎉" trophy screen as a brand-new topic — with no acknowledgement anywhere that this is a repeat. For a child (especially one with a SEND/dyslexia indicator like Ivy, who may rely more on consistent, literal framing) this is genuinely ambiguous: did I just do something new, or did I do the same thing again? It's also the natural product-level companion to B2 — once the dashboard correctly marks an already-certified topic as "done" instead of "to do", the CHILD-facing surface should carry the same distinction, so a parent and child are never shown contradictory signals about the same topic on the same day.
  - Change: thread a `isReview: boolean` (topic already `certified` before this attempt started — the DailyFlow/PracticePlayer already has access to the child's competence state to compute this) through to two presentational spots: (1) the Explainer screen — a small "You've already mastered this — great for keeping it fresh!" chip instead of/alongside "Today's lesson", and (2) the completion screen — swap "Topic mastered!" for something like "Still mastered! ⭐" or "Great review!" when `isReview` is true, keeping the same celebratory tone and animation but not re-claiming a "first mastery" moment that already happened. No change to the scoring/certification logic itself (B2's `certified_at` guard from 2026-08-27 already ensures the DATA side is safe — this is purely the presentational acknowledgement that was still missing).
  - Risk: low — presentation-only branch on data already available at render time (the topic's competence state is already fetched to gate "Practice more" links); must not change any scoring, hint, or mastery-cap logic — only copy/label on the explainer and completion screens.

- [ ] **F4 · curriculum (EPIC 2 depth) · Second command-word `maths_sequences` mastery question — "Work out" a term's position from its value (nth-term back-solving)** (size: S)
  - The win: `maths_sequences` is on `automation/backlog.md`'s remaining single-command-word-coverage list. Its existing items use "Find the next term" / "What is the nth term" / "Find the Nth term" (all forward calculations); this adds the reverse direction — given a term's VALUE, work out its POSITION — a distinct, commonly-examined GCSE sub-skill (solving a linear equation derived from the nth-term rule) rather than a rephrasing of an existing item.
  - Question (verbatim `SeedQuestion` shape):
    ```
    {
      topic_tag: "maths_sequences",
      subject: "mathematics",
      tier: 4,
      key_stage: 4,
      kind: "mastery",
      prompt: "A sequence has nth term 4n − 1. Work out which term of the sequence equals 39.",
      options: ["n = 10", "n = 9", "n = 39", "n = 4"],
      correct_index: 0,
      explanation: "Set 4n − 1 = 39. Add 1 to both sides: 4n = 40. Divide by 4: n = 10.",
      hints: ["Set the nth term rule equal to 39 and solve the equation for n.", "4n − 1 = 39 means 4n = 40 — now divide by 4."],
      misconceptions: ["", "This comes from a small arithmetic slip when solving 4n = 40 — check the division again.", "This mistakes the VALUE of the term (39) for its POSITION in the sequence (n) — they are not the same thing.", "This mistakes the coefficient of n (4) for the answer — you still need to solve the equation."],
    }
    ```
  - Risk: none — hand-derived (4×10−1=39 ✓). Owner approval gate applies.
  - **BLOCKED (Mechanic, 2026-08-28):** `maths_sequences` already has a reverse-direction (VALUE, find POSITION) mastery item, seeded since the original curriculum bank: prompt "Which term of 3n + 1 equals 31?" (options n=9/10/11/8, correct n=10). This finding's premise ("existing items use... all forward calculations") is stale: the reverse skill is already tested, and the proposed new item's final answer (n=10) is identical to the existing item's, with only the coefficients (3n+1 vs 4n−1) and target value (31 vs 39) changed. Per the established duplicate-content precedent (2026-08-23's blocked sci_energy item), transcribing this would seed a near-duplicate a child could see twice testing the identical sub-skill and identical final answer. Not built tonight; flagging for the owner or a future Scout pass to author a genuinely distinct maths_sequences mastery item (e.g. a quadratic-ish or geometric-sequence variant, or a different command word entirely) if more coverage is wanted.

- [x] **F5 · curriculum (EPIC 2 depth) · Second command-word `sci_cells` mastery question — "Calculate" a microscope magnification (AQA 8464 required-practical skill)** (size: S)
  - The win: `sci_cells` is on the remaining single-command-word-coverage list. Magnification calculation (image size ÷ actual size, with a mandatory unit conversion) is one of the most reliably-examined AQA Biology required-practical numeracy skills and currently has no representation in the bank at all for this topic.
  - Question (verbatim `SeedQuestion` shape):
    ```
    {
      topic_tag: "sci_cells",
      subject: "science",
      tier: 4,
      key_stage: 4,
      kind: "mastery",
      prompt: "A cell has an actual width of 20 micrometres (μm). Its image under a microscope measures 100 millimetres (mm) wide. Calculate the magnification of the image.",
      options: ["×5,000", "×5", "×500", "×50,000"],
      correct_index: 0,
      explanation: "Convert units first: 100 mm = 100,000 micrometres (μm). Magnification = image size ÷ actual size = 100,000 ÷ 20 = ×5,000.",
      hints: ["Magnification = image size ÷ actual size — but both must be in the SAME units first.", "Convert 100 mm into micrometres before dividing: 1 mm = 1,000 μm."],
      misconceptions: ["", "This comes from dividing 100 ÷ 20 without converting millimetres to micrometres first — the units must match.", "This is off by a factor of 10 — check the division 100,000 ÷ 20 again.", "This uses the wrong conversion factor for millimetres to micrometres (should be ×1,000, not ×10,000)."],
    }
    ```
  - Risk: none — hand-derived (100mm = 100,000μm; 100,000 ÷ 20 = 5,000 ✓). Owner approval gate applies.
  - **NOTE (Mechanic, 2026-08-28):** this finding's "no representation in the bank at all" premise is slightly stale: `sci_cells` already has a same-unit magnification item from 2026-08-14 ("A cell has a real width of 0.05 mm... measures 10 mm... ×200", both already in mm, no conversion needed). Built anyway because today's item tests a genuinely different, additional sub-skill (μm to mm cross-unit conversion, the classic AQA magnification exam trap the existing item doesn't cover) and produces a different final answer (×5,000 vs ×200), so it does not meet the disqualifying bar from the 2026-08-23 duplicate-content precedent (identical skill AND identical final answer).

- [ ] **F6 · curriculum (EPIC 2 depth) · Second command-word `eng_devices` mastery question — "Explain the effect" of a metaphor, the AQA 8700 language-analysis command word** (size: S)
  - The win: `eng_devices` is on the remaining single-command-word-coverage list, and its existing items are pure identification ("which phrase is a simile/metaphor"). AQA English Language Paper 1/2 consistently asks candidates to EXPLAIN the effect of a device, not just spot it — a distinct, higher-order skill. Framed as MCQ so it stays unambiguous and machine-gradable while still testing the "effect" reasoning, not just recognition.
  - Question (verbatim `SeedQuestion` shape):
    ```
    {
      topic_tag: "eng_devices",
      subject: "english",
      tier: 4,
      key_stage: 4,
      kind: "mastery",
      prompt: "Which sentence best explains the EFFECT of the metaphor 'The classroom was a zoo'?",
      options: [
        "It suggests the classroom was chaotic and noisy, like the atmosphere of a zoo full of animals.",
        "It means the classroom literally contained wild animals.",
        "It compares the classroom to a zoo using the word 'like' or 'as'.",
        "It shows the teacher's opinion of the students."
      ],
      correct_index: 0,
      explanation: "A metaphor states one thing IS another to transfer its qualities. Saying the classroom 'was a zoo' suggests chaos and noise, like the atmosphere of a zoo full of animals — that is the effect created for the reader.",
      hints: ["Think about what a zoo is usually like — what images and feelings does it bring to mind?", "The 'effect' is what the comparison makes the reader FEEL or IMAGINE, not a literal, word-for-word description."],
      misconceptions: ["", "A metaphor is not literal — no real animals are involved. This misreads the figurative language as fact.", "This describes what a SIMILE does (using 'like' or 'as') — a metaphor states something IS something else, with no 'like' or 'as'.", "This goes beyond what the metaphor itself supports — the sentence tells us nothing about the teacher's opinion."],
    }
    ```
  - Risk: none at authoring stage — a standard, unambiguous GCSE English technique question with exactly one defensible correct option (the other three are a literal misreading, a simile/metaphor mix-up, and an unsupported overreach respectively). Owner approval gate applies.

- [ ] **F7 · delight (small, standing lane) · Give the certificate page a warm arrival instead of an instant, static render — the one moment in the child flow with zero motion** (size: S)
  - The win: every other meaningful child-flow arrival animates (the Topic-mastered trophy screen has a star-burst + Eddie, the reteach screen has a calm fade, hint cards slide in) — but landing on `/learn/certificate?topic=...` (reached via "Save my certificate" from the celebration screen) renders the whole certificate article instantly, fully formed, with no entrance motion at all (confirmed live this run on two fresh certificates). It's a proud, once-per-topic moment for the child (and the LA-evidence document a parent later downloads) and currently feels flatter than everything leading up to it.
  - Change: wrap the certificate `<article>` in a gentle `motion.article` (`framer-motion`, already installed and used everywhere else in `(child)`) — e.g. `initial={{ opacity: 0, scale: 0.97, y: 8 }}` → `animate={{ opacity: 1, scale: 1, y: 0 }}`, ~400-500ms, matching the calm, un-hurried pace of the rest of the child UI; the tamper-evident hash text can fade in a beat after the main card as a small secondary flourish. `useReducedMotion`-gated to an instant appearance as everywhere else in `(child)`.
  - Risk: none — presentational only, one component, no logic/data change; must not delay the certificate content being readable/printable (motion should not block `@media print`).

- [ ] **F8 · security / stack hygiene · Bump the in-range ("Wanted") dependency versions** (size: S)
  - The win: `npm outdated` shows `next` 15.5.23→15.5.24, `@next/bundle-analyzer` 15.5.18→15.5.24, `@types/node` 22.19.19→22.20.1, `lucide-react` 1.34.0→1.35.0, `posthog-js` 1.386.6→1.422.2 all already satisfy the declared `package.json` semver ranges — routine patch/minor hygiene, not the staged Next.js 15→16 major migration tracked separately in `automation/backlog.md` EPIC 7 (left untouched, as always). `npm audit` is at 0 vulnerabilities both prod-only and full-tree this run, so this is purely a freshness bump, not a CVE fix.
  - Change: `npm update` (or bump each individually), then the standard gate (`npm run type-check && npm test && npm run lint && npm run build`).
  - Risk: low — all in-range patch/minor bumps; re-run `npm audit` after to confirm 0 vulnerabilities still holds, and a quick live console-error smoke on `/`, `/dashboard`, and a child lesson (lucide-react icons + PostHog init are the two client-visible surfaces touched).

---

## Summary
- Bugs: 3 (1 Critical/High · 1 Medium/High · 1 Medium) · Features: 8
- Top 3 to ship: **B1** (the English "plural of X" derived visual actively teaches the WRONG grammar rule for irregular plurals — live-reproduced on "child"→children, contradicting the correct MCQ answer right next to it, plus a second live instance on "leaf"→leaves; this is the worst class of finding possible here, a child-facing figure that is confidently wrong), **B2** (the parent dashboard shows an already-certified topic as a stale, unchecked "quest" because `todayCard` only checks TODAY's completions, never the topic's real certified state — live-reproduced on Ivy's actual account, and directly undermines the "prove with evidence" parent-trust promise), **F1** (a total, zero-coverage Edexcel 1MA1 spec gap — Simultaneous Equations — closed with a topic entry, worked example and 3 checked starter questions, the named EPIC 3 next-step from `automation/backlog.md`).

This run drove the full child learning loop at maximum depth per the standing brief: completed a topic from Learn → Practise (mixed right/wrong, AI "Why isn't that right?" explanation) → Mastery → certificate (`eng_grammar`, fresh certification, first attempt); separately drove an already-certified topic (`maths_algebra_linear`) through a DELIBERATE mastery fail (2/3, reteach screen + Eddie confirmed) then a fresh pass to re-certify — and confirmed **live** that the 2026-08-27 B2 fix (`certified_at` must not move on re-mastery) holds: the certificate still reads "Awarded 26 August 2026", unchanged, after today's re-mastery. All four interaction types were completed this run: `mcq` (correct + wrong + AI distractor explanation), `fill_blank` (correct, resumed after a real page refresh mid-lesson at the exact step with score intact, survived a rapid double-click on Check-answer with no double-scoring), `tap_reveal` (reveal-then-select gesture split), `drag_drop` (both tap-to-place AND full keyboard Tab+Enter placement). Every celebration and wrong-answer moment stayed inside the calm-wrong law (soft amber/violet tints, never red; star-burst/confetti on correct; Eddie present on both the Topic-mastered and reteach screens). Every standing B-journeys flow ran start-to-finish and PASSED: parent oversight (dashboard → Ivy's child profile → confirmed Q3 2026 portfolio already filed + downloadable per-topic certificates), plan/schedule (approved a fresh week for Sam Test live — the dashboard immediately reflected the real assigned topic, re-confirming EPIC 11's fix holds on a third child), tutor (empty queue, silo holds, read-only), admin (overview → finance → escalations → curriculum CMS, all read-only, zero destructive clicks, live activity feed correctly reflected this session's real completions within minutes). Mock exam correctly stayed honestly locked for Maths ("You've done your Maths mock for this week... next one unlocks on 31 August"). Zero console errors and zero failed network requests across the entire session, in both viewports (1280×800 and 390×844, confirmed `scrollWidth` ≤ `innerWidth` on every page checked). Static checks all clean: `type-check`, `lint`, and `npm audit` (0 vulnerabilities, prod-only and full-tree) all green; security headers (CSP/HSTS/X-Frame-Options/Permissions-Policy) unchanged and strong via `curl -IL`.
