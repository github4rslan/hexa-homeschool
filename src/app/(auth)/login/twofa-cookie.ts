/** Name of the httpOnly cookie holding the signed 2FA code token. */
export const TWOFA_COOKIE = "hexa_2fa";

/** httpOnly cookie holding the signed "password OK, awaiting TOTP" token (F4). */
export const TOTP_PENDING_COOKIE = "hexa_totp";
